---
title: "Topic 211 — E-Mail Services (نسخه عمیق و کامل)"
exam: LPIC-2 / 202-450
weights: "211.1 (4) + 211.2 (2) + 211.3 (2)"
os_target: "Arch Linux / Omarchy"
tags: [lpic2, email, postfix, dovecot, spf, dkim, deep-dive]
---

# Topic 211: E-Mail Services — راهنمای کامل و عمیق

---

# بخش اول — 211.1: استفاده از سرور ایمیل (MTA)

## ۱.۱) سفر یک ایمیل — از دکمه‌ی Send تا صندوق گیرنده

قبل از هر دستوری، بیا کل مسیری که یک ایمیل طی می‌کند را ببینیم، چون این نقشه‌ی ذهنی، فهم همه‌ی اجزای این Topic (MTA/MDA/MUA) را ساده می‌کند:

```
[کاربر فرستنده در Outlook/Thunderbird/Gmail]  ← MUA (Mail User Agent)
        │  (پروتکل SMTP، پورت 587 با احراز هویت)
[سرور SMTP فرستنده — مثلاً Postfix]           ← MTA (Mail Transfer Agent)
        │  (پروتکل SMTP، پورت 25، بین سرورها)
        │  (مسیر پیدا می‌شود از طریق رکورد MX دامنه‌ی گیرنده — که در Topic 207 دیدیم!)
[سرور SMTP گیرنده — مثلاً Postfix سازمان گیرنده]  ← MTA
        │
[تحویل محلی به صندوق کاربر]                   ← MDA (Mail Delivery Agent، مثل Procmail)
        │  (ذخیره در فرمت mbox یا Maildir)
[کاربر گیرنده با IMAP/POP3 می‌خواند]            ← MUA + سرور Dovecot
```

- **MUA** (Mail User Agent) — برنامه‌ای که کاربر مستقیم با آن کار می‌کند (Outlook، Thunderbird، یا یک وب‌میل)
- **MTA** (Mail Transfer Agent) — نرم‌افزار سرور که ایمیل را بین سرورها **منتقل** می‌کند (Postfix، Sendmail، Exim)
- **MDA** (Mail Delivery Agent) — نرم‌افزاری که ایمیل رسیده را واقعاً در صندوق محلی کاربر **تحویل** می‌دهد (اغلب فیلترینگ هم انجام می‌دهد)

## ۱.۲) نصب و ساختار Postfix

```bash
sudo pacman -S postfix
```
فایل تنظیمات اصلی: `/etc/postfix/main.cf`

```conf
myhostname = mail.example.com
mydomain = example.com
myorigin = $mydomain
inet_interfaces = all
mydestination = $myhostname, localhost.$mydomain, localhost, $mydomain
mynetworks = 127.0.0.0/8, 192.168.1.0/24
relayhost =
```

بیایید هر پارامتر را عمیق باز کنیم چون این‌ها اغلب دقیقاً همان چیزهایی هستند که یک ادمین ایمیل تازه‌کار اشتباه می‌کند:

- **`myhostname`** — نام کامل و معتبر (FQDN) این سرور؛ این نامی است که سرور خودش را در مکالمات SMTP معرفی می‌کند
- **`mydestination`** — لیست دامنه‌هایی که این سرور خودش را **مقصد نهایی** برایشان می‌داند (یعنی برای این دامنه‌ها، ایمیل باید محلی تحویل داده شود، نه فوروارد شود جای دیگر)
- **`mynetworks`** — کدام شبکه‌ها/IP ها **مجاز به relay** (استفاده از این سرور برای فرستادن ایمیل به هرجای دیگر، نه فقط دریافت) هستند — این یکی از حیاتی‌ترین تنظیمات امنیتی کل Postfix است.
- **`relayhost`** — اگر خالی باشد، سرور خودش مستقیم تلاش می‌کند ایمیل را با جست‌وجوی رکورد MX مقصد تحویل دهد؛ اگر مقداری داشته باشد (مثلاً یک سرور SMTP سازمانی مرکزی)، همه‌ی ایمیل‌های خروجی از طریق آن relay سرور فرستاده می‌شوند (رایج در شبکه‌های سازمانی که همه‌ی ایمیل باید از یک نقطه‌ی مرکزی خارج شود).

## ۱.۳) خطر Open Relay — دقیقاً همان الگوی امنیتی تکرارشونده‌ی این دوره

> ⚠️ **هشدار امنیتی بسیار جدی:** اگر `mynetworks` را اشتباه پیکربندی کنی (مثلاً به‌جای `127.0.0.0/8, 192.168.1.0/24`، اشتباهاً `0.0.0.0/0` بنویسی که یعنی «همه‌ی دنیا»)، سرور تو به یک **Open Relay** تبدیل می‌شود — یعنی هر کسی از هر جای اینترنت می‌تواند از سرور تو برای فرستادن ایمیل (اغلب اسپم یا فیشینگ به میلیون‌ها نفر) استفاده کند، به‌جای IP خودشان.
>
> این دقیقاً همان الگویی است که در Topic 207 (DNS Open Resolver) و Topic 208 (Squid Open Proxy) هم دیدیم — یک الگوی امنیتی تکرارشونده در کل دوره‌ی LPIC-2: **هر سرویسی که می‌تواند به‌عنوان واسط بین دو طرف دیگر عمل کند (relay/resolve/proxy)، باید محدودیت دسترسی صریح داشته باشد، وگرنه به ابزاری برای مهاجمان تبدیل می‌شود.**
>
> عواقب واقعی یک Open Relay: در عرض چند ساعت، دامنه‌ی سرور تو در **Blacklist های ایمیل جهانی** (مثل Spamhaus) قرار می‌گیرد — که یعنی حتی بعد از رفع مشکل، ممکن است **هفته‌ها** طول بکشد تا از این لیست‌ها خارج شوی و ایمیل‌های واقعی سازمانت دوباره به‌درستی تحویل داده شوند.

## ۱.۴) مدیریت صف و دستورات عملیاتی

```bash
sudo postfix check           # بررسی صحت تنظیمات و مجوزهای فایل‌ها
sudo postfix reload           # اعمال تغییرات main.cf بدون قطع سرویس
mailq                          # نمایش صف ایمیل‌های در انتظار ارسال (چرا معلق مانده‌اند؟)
sudo postsuper -d ALL          # حذف همه‌ی ایمیل‌های معلق در صف (احتیاط! غیرقابل‌بازگشت)
sudo postsuper -r ALL           # (requeue) تلاش مجدد برای ارسال همه‌ی ایمیل‌های صف
sudo postconf -n                # نمایش فقط تنظیماتی که از پیش‌فرض تغییر کرده‌اند (خلاصه‌ی مفیدتر از دیدن کل main.cf)
```

### فایل‌های نگاشت (Lookup Tables) و `postmap`

Postfix از فایل‌های نگاشت برای کارهای مختلف استفاده می‌کند (مثلاً `virtual` برای نگاشت آدرس‌های مجازی، `access` برای کنترل دسترسی سفارشی):
```
# /etc/postfix/virtual
sales@example.com    john@example.com, mary@example.com
```
بعد از هر ویرایش فایل متنی، باید آن را به فرمت دیتابیس باینری (که Postfix واقعاً در زمان اجرا سریع می‌خواند) کامپایل کنی:
```bash
sudo postmap /etc/postfix/virtual
sudo postfix reload
```
> ⚠️ فراموش کردن `postmap` بعد از ویرایش، یکی از رایج‌ترین اشتباهات مبتدیان Postfix است — تغییرات فایل متنی اعمال نمی‌شوند تا وقتی دیتابیس باینری دوباره ساخته شود.

## ۱.۵) SPF/DKIM/DMARC — مثلث ضد جعل دامنه، عمیق

این سه مکانیزم با هم یک سیستم دفاعی کامل در برابر **جعل هویت فرستنده** (Email Spoofing) می‌سازند — موضوعی که مستقیماً به حملات فیشینگ در دنیای Cyber Security مرتبط است.

### SPF — چه کسی مجاز است از طرف دامنه‌ی من ایمیل بفرستد؟

**SPF** (Sender Policy Framework) یک رکورد **TXT** در DNS دامنه است که می‌گوید «فقط این IP ها/سرورها مجازند از طرف این دامنه ایمیل بفرستند»:
```
example.com.  TXT  "v=spf1 mx a ip4:203.0.113.0/24 -all"
```
- `mx` — یعنی سرورهایی که در رکوردهای MX همین دامنه تعریف شده‌اند هم مجازند
- `ip4:203.0.113.0/24` — این بازه‌ی IP هم مجاز است
- `-all` — **هر چیز دیگری رد شود** (Hard Fail)؛ نسخه‌ی نرم‌تر `~all` (Soft Fail، یعنی مشکوک علامت بزن ولی رد نکن)

وقتی یک سرور گیرنده ایمیلی را دریافت می‌کند که ادعا می‌کند از `example.com` است، رکورد SPF آن دامنه را می‌خواند و بررسی می‌کند آیا IP فرستنده‌ی واقعی (که در لایه‌ی SMTP، نه در هدر قابل‌جعل «From»، دیده می‌شود) در این لیست مجاز هست یا نه.

### DKIM — امضای رمزنگاری‌شده‌ی محتوای ایمیل

**DKIM** (DomainKeys Identified Mail) مسئله‌ی دیگری را حل می‌کند: حتی اگر IP فرستنده مجاز باشد (SPF پاس شود)، چطور مطمئن شویم **محتوای** ایمیل در مسیر دستکاری نشده؟ DKIM با یک جفت کلید (خصوصی/عمومی، دقیقاً مثل مفهومی که در DNSSEC از Topic 207 دیدیم) کار می‌کند:
1. سرور فرستنده با کلید **خصوصی** خودش، یک امضای دیجیتال از بخش‌های مهم ایمیل (هدرها + بدنه) می‌سازد و آن را به‌عنوان یک هدر جدید (`DKIM-Signature`) اضافه می‌کند.
2. سرور گیرنده، کلید **عمومی** مربوطه را از یک رکورد DNS خاص دامنه‌ی فرستنده می‌خواند (معمولاً چیزی مثل `selector._domainkey.example.com`) و امضا را تأیید می‌کند.
3. اگر امضا معتبر باشد، یعنی ایمیل از لحظه‌ی امضا شدن تا الان **دست‌نخورده** مانده و واقعاً از دامنه‌ی ادعاشده آمده.

### DMARC — سیاست نهایی: با ایمیل‌هایی که SPF/DKIM را رد می‌کنند چه کار کنیم؟

**DMARC** (Domain-based Message Authentication, Reporting & Conformance) این دو مکانیزم را به هم وصل می‌کند و یک **سیاست صریح** تعریف می‌کند:
```
_dmarc.example.com.  TXT  "v=DMARC1; p=reject; rua=mailto:dmarc-reports@example.com"
```
- **`p=`** (Policy) — چه کاری با ایمیل‌هایی که هم SPF هم DKIM را رد می‌کنند انجام شود: `none` (فقط گزارش کن، کاری نکن — معمولاً اولین قدم موقع پیاده‌سازی)، `quarantine` (به اسپم بفرست)، `reject` (کاملاً رد کن، حتی تحویل نده)
- **`rua=`** — آدرسی که گزارش‌های تجمیعی روزانه/هفتگی درباره‌ی ایمیل‌های رد/قبول‌شده به آن فرستاده شود — این گزارش‌ها به ادمین کمک می‌کنند ببیند آیا کسی دارد سعی می‌کند دامنه‌شان را جعل کند.

> **چرا این مثلث این‌قدر برای Cyber Security مهم است؟** چون بسیاری حملات فیشینگ سازمانی (Business Email Compromise) دقیقاً با جعل هدر «From» یک ایمیل به نظر می‌رسند از مدیرعامل یا یک همکار معتبر آمده‌اند. یک پیاده‌سازی درست و سخت‌گیرانه‌ی SPF+DKIM+DMARC (با `p=reject`) عملاً جلوی این نوع جعل مستقیم دامنه را می‌گیرد.

## ۱.۶) نکات مهم آزمون برای 211.1

- ✅ زنجیره‌ی MUA → MTA → MDA را دقیق تفکیک کن.
- ✅ `mynetworks` نادرست = Open Relay — یکی از جدی‌ترین اشتباهات پیکربندی در کل این Topic.
- ✅ بعد از ویرایش هر فایل نگاشت (مثل `virtual`)، همیشه `postmap` سپس `postfix reload`.
- ✅ SPF (کدام IP مجاز)، DKIM (امضای محتوا)، DMARC (سیاست نهایی + گزارش) را از هم تفکیک کن — این سه با هم کار می‌کنند، هرکدام یک لایه‌ی متفاوت.

---

# بخش دوم — 211.2: تحویل محلی ایمیل (Local Mail Delivery)

## ۲.۱) MDA — کجای زنجیره قرار می‌گیرد

وقتی Postfix (MTA) ایمیلی را برای یک کاربر محلی دریافت کرد، خودش مسئول **تصمیم‌گیری هوشمند** درباره‌ی محتوا (مثل فیلتر کردن اسپم، دسته‌بندی بر اساس موضوع) نیست — این کار به یک MDA اختصاصی واگذار می‌شود.

## ۲.۲) Procmail — فیلترینگ مبتنی بر قانون

```bash
sudo pacman -S procmail
```
فایل قوانین در `~/.procmailrc` (هر کاربر قوانین خودش را دارد):
```conf
:0:
* ^Subject:.*invoice
$HOME/Mail/invoices

:0
* ^From:.*noreply@spam-domain\.com
/dev/null
```

بیایید سینتکس را باز کنیم:
- `:0` — شروع یک «recipe» (دستورالعمل) جدید؛ `:0:` (با دو نقطه) یعنی از یک قفل‌فایل (lockfile) هم استفاده کن تا در صورت پردازش همزمان چند ایمیل، تداخلی پیش نیاید
- `* ^Subject:.*invoice` — شرط: خط Subject باید شامل کلمه‌ی «invoice» باشد (عبارت با‌قاعده/regex)
- خط بعدی — مقصد: یا یک پوشه‌ی خاص، یا (در مثال دوم) `/dev/null` که یعنی **کاملاً دور بینداز** (روش رایج برای حذف خودکار اسپم شناخته‌شده بدون حتی نگه‌داشتنش)

## ۲.۳) SpamAssassin — فیلتر هوشمند اسپم

```bash
sudo pacman -S spamassassin
sudo systemctl enable --now spamassassin
```
SpamAssassin برخلاف Procmail (که قوانین صریح و دستی است)، از یک سیستم **امتیازدهی** استفاده می‌کند: ده‌ها قانون مختلف (آدرس IP فرستنده در بلک‌لیست‌های شناخته‌شده، کلمات مشکوک در متن، فرمت HTML مشکوک، عدم تطابق SPF) هرکدام امتیاز مثبت یا منفی می‌دهند؛ اگر مجموع امتیاز از یک آستانه (معمولاً ۵) بگذرد، ایمیل اسپم علامت می‌خورد.

```bash
spamassassin -t < email.txt              # تست یک ایمیل خاص و دیدن امتیاز دقیق + دلیل هر قانونی که فعال شده
sa-learn --spam ~/Mail/spam/               # آموزش دستی فیلتر بیزی (Bayesian) با نمونه‌های واقعی اسپم
sa-learn --ham ~/Mail/inbox/                 # آموزش با نمونه‌های ایمیل معتبر (ham، اصطلاح مقابل spam)
```
**فیلتر بیزی چیست؟** یک الگوریتم یادگیری آماری ساده که یاد می‌گیرد کدام کلمات/الگوها در ایمیل‌های اسپم **این کاربر خاص** بیشتر تکرار می‌شوند، در مقابل ایمیل‌های معتبرش — این یعنی فیلتر با گذشت زمان و آموزش بیشتر، دقیق‌تر و شخصی‌سازی‌شده‌تر می‌شود.

## ۲.۴) mbox در برابر Maildir — عمیق‌تر از فقط تعریف

- **mbox** — تمام ایمیل‌های یک صندوق در **یک فایل متنی بزرگ** پشت هم چیده می‌شوند (با یک خط جداکننده‌ی خاص بین هر ایمیل). مشکل: اگر دو پردازه هم‌زمان بخواهند این فایل را بنویسند (مثلاً یک ایمیل جدید می‌رسد درست همان لحظه‌ای که کاربر دارد ایمیلی را حذف می‌کند)، بدون قفل‌گذاری دقیق، خطر **خرابی کل فایل** (نه فقط یک ایمیل) وجود دارد.
- **Maildir** — هر ایمیل یک **فایل جداگانه** در یک ساختار پوشه‌ای استاندارد است:
  - `new/` — ایمیل‌های جدید که هنوز خوانده نشده‌اند
  - `cur/` — ایمیل‌هایی که حداقل یک‌بار توسط یک کلاینت دیده شده‌اند
  - `tmp/` — فایل‌های موقت حین نوشتن (تا نوشتن کامل تمام نشود، به `new/` منتقل نمی‌شوند — این خودش یک تکنیک اتمیک بودن است)

  چون هر ایمیل فایل جداست، نوشتن همزمان چند ایمیل مختلف **هیچ خطر تداخلی** ندارد (هرکدام فایل خودش را دارد) — این دلیل اصلی است که Maildir امروز فرمت ترجیحی اکثر سرورهای مدرن (از جمله Dovecot) است.

## ۲.۵) نکات مهم آزمون برای 211.2

- ✅ Procmail از قوانین صریح (regex + مقصد) استفاده می‌کند؛ خط `:0:` (با قفل) در برابر `:0` (بدون قفل).
- ✅ SpamAssassin از سیستم امتیازدهی + فیلتر بیزی قابل‌آموزش (`sa-learn --spam/--ham`) استفاده می‌کند.
- ✅ تفاوت بنیادین mbox (یک فایل، خطر تداخل) و Maildir (فایل جدا به‌ازای هر ایمیل، ساختار new/cur/tmp، ایمن‌تر برای همزمانی).

---

# بخش سوم — 211.3: مدیریت ایمیل از راه دور (Dovecot)

## ۳.۱) چرا Dovecot جدا از Postfix است

نکته‌ی مفهومی مهم: **Postfix ایمیل ارسال/دریافت می‌کند** (MTA)؛ **Dovecot به کاربر اجازه می‌دهد صندوقش را از راه دور بخواند** (POP3/IMAP سرور). این دو کاملاً جدا و مستقل هستند — می‌توانی Postfix را بدون Dovecot داشته باشی (اگر مثلاً فقط سرور relay داخلی هستی) یا برعکس.

## ۳.۲) POP3 در برابر IMAP — تفاوت بنیادین که باید عمیق بفهمی

این یکی از مهم‌ترین تفاوت‌های مفهومی کل Topic 211 است:

**POP3** (Post Office Protocol v3) طراحی شده برای زمانی که کاربر فقط از **یک** دستگاه به ایمیلش دسترسی داشت: کلاینت به سرور وصل می‌شود، **تمام** ایمیل‌های جدید را دانلود می‌کند، و به‌طور پیش‌فرض آن‌ها را از سرور **حذف** می‌کند (هرچند اکثر کلاینت‌های مدرن گزینه‌ای دارند که کپی را روی سرور نگه دارند). این یعنی اگر بعداً از یک دستگاه دیگر به همان صندوق وصل شوی، ایمیل‌هایی که قبلاً دانلود شده‌اند دیگر آنجا نیستند.

**IMAP** (Internet Message Access Protocol) طراحی شده برای دنیای امروز که کاربر از چند دستگاه (گوشی، لپ‌تاپ، تبلت) همزمان به یک صندوق دسترسی دارد: ایمیل‌ها **روی سرور می‌مانند**، و کلاینت فقط یک **آینه‌ی همگام** از وضعیت سرور نشان می‌دهد — اگر یک ایمیل را روی گوشی بخوانی، وقتی لپ‌تاپ را باز کنی، آن ایمیل آنجا هم «خوانده‌شده» نشان داده می‌شود، چون هر دو کلاینت با همان حالت واقعی روی سرور همگام‌اند.

| ویژگی | POP3 | IMAP |
|---|---|---|
| پورت معمولی | 110 | 143 |
| پورت رمزنگاری‌شده (SSL/TLS) | 995 | 993 |
| محل ذخیره‌ی اصلی | معمولاً دانلود و حذف از سرور | همیشه روی سرور |
| همگام‌سازی چند دستگاه | خیر | بله |

## ۳.۳) پیکربندی Dovecot

```bash
sudo pacman -S dovecot
```
فایل تنظیمات اصلی: `/etc/dovecot/dovecot.conf` (که خودش معمولاً فایل‌های تفکیکی در `/etc/dovecot/conf.d/*.conf` را include می‌کند)

```conf
protocols = imap pop3
mail_location = maildir:~/Maildir

ssl = required
ssl_cert = </etc/letsencrypt/live/example.com/fullchain.pem
ssl_key = </etc/letsencrypt/live/example.com/privkey.pem
```

- **`protocols`** — کدام پروتکل‌ها فعال باشند (می‌توانی فقط IMAP را فعال کنی و POP3 را کاملاً غیرفعال بگذاری اگر نمی‌خواهی از آن پشتیبانی کنی)
- **`mail_location`** — کجا و با چه فرمتی صندوق‌ها ذخیره شده‌اند؛ باید دقیقاً با همان چیزی که MDA (مثل Procmail) استفاده می‌کند هماهنگ باشد
- **`ssl = required`** — اجبار اتصال رمزنگاری‌شده؛ گزینه‌ی `yes` هم رمزنگاری را فعال می‌کند اما اتصال رمزنگاری‌نشده را هم مجاز نگه می‌دارد (کمتر امن)

```bash
sudo doveconf -n              # نمایش تنظیمات فعال (غیر پیش‌فرض) — معادل مفهومی postconf -n در Postfix
telnet localhost 143            # تست دستی خام IMAP (بدون رمزنگاری، فقط برای دیباگ/آزمایش)
openssl s_client -connect localhost:993     # تست اتصال IMAPS رمزنگاری‌شده، همراه نمایش جزئیات گواهی TLS
```

> ⚠️ **هشدار امنیتی:** هرگز POP3/IMAP بدون SSL/TLS (پورت‌های 110/143 خام) را روی اینترنت عمومی باز نگه ندار — رمز عبور کاربر در این حالت به‌صورت کاملاً متن‌باز منتقل می‌شود؛ دقیقاً همان هشداری که درباره‌ی Basic Authentication در Apache (Topic 208) دادیم — این یک الگوی امنیتی دیگر است که در کل دوره تکرار می‌شود: **هر پروتکل احراز هویتی که رمزنگاری ندارد، باید همیشه پشت TLS اجرا شود.**

## ۳.۴) نکات مهم آزمون برای 211.3

- ✅ Dovecot جدا از Postfix است — یکی MTA (ارسال بین سرورها)، دیگری سرور دسترسی کاربر نهایی.
- ✅ تفاوت بنیادین POP3 (دانلود+حذف، تک‌دستگاه) و IMAP (نگه‌داری روی سرور، چنددستگاهی همگام) را دقیق بدان — سؤال کلاسیک آزمون.
- ✅ شماره‌پورت‌ها را حفظ کن: POP3=110/995(SSL)، IMAP=143/993(SSL).
- ✅ `mail_location` باید با فرمت واقعی صندوق‌ها (که MDA نوشته) هماهنگ باشد.
- ✅ هرگز احراز هویت بدون رمزنگاری روی اینترنت عمومی.

---

## خلاصه‌ی جدولی نهایی کل Topic 211

| زیرمبحث | مفهوم کلیدی | جزئیات |
|---|---|---|
| 211.1 | زنجیره | MUA → MTA(Postfix) → MDA |
| 211.1 | خطر امنیتی | `mynetworks` نادرست → Open Relay → Blacklist |
| 211.1 | نگاشت | `postmap` بعد از هر ویرایش فایل نگاشت |
| 211.1 | ضد جعل | SPF(کدام IP)، DKIM(امضای محتوا)، DMARC(سیاست+گزارش) |
| 211.2 | Procmail | `:0:` (با قفل)، regex + مقصد |
| 211.2 | SpamAssassin | امتیازدهی + فیلتر بیزی (`sa-learn`) |
| 211.2 | ذخیره‌سازی | mbox(یک‌فایل) vs Maildir(new/cur/tmp) |
| 211.3 | نقش Dovecot | سرور دسترسی کاربر (نه ارسال بین سرورها) |
| 211.3 | POP3 vs IMAP | دانلود+حذف vs نگه‌داری+همگام‌سازی |
| 211.3 | پورت‌ها | 110/995 (POP3)، 143/993 (IMAP) |

## مایندمپ متنی کامل

```
Topic 211: E-Mail Services
│
├── 211.1 Using a Mail Server (Postfix/MTA)
│   ├── زنجیره: MUA→MTA→MDA
│   ├── main.cf: myhostname, mydestination, mynetworks, relayhost
│   ├── ⚠️ mynetworks اشتباه → Open Relay → Blacklist جهانی
│   ├── mailq, postsuper -d/-r, postconf -n
│   ├── فایل نگاشت + postmap (فراموش نکن!)
│   └── SPF(IP مجاز) + DKIM(امضا) + DMARC(سیاست/گزارش)
│
├── 211.2 Local Mail Delivery
│   ├── Procmail: ~/.procmailrc، :0: (قفل)، regex→مقصد
│   ├── SpamAssassin: امتیازدهی + sa-learn --spam/--ham (بیزی)
│   └── mbox(یک فایل، خطر تداخل) vs Maildir(new/cur/tmp، ایمن)
│
└── 211.3 Remote Mail Access (Dovecot)
    ├── جدا از Postfix (نقش متفاوت)
    ├── POP3(110/995): دانلود+حذف، تک‌دستگاه
    ├── IMAP(143/993): نگه‌داری روی سرور، چنددستگاهی
    ├── mail_location باید با MDA هماهنگ باشد
    └── ⚠️ همیشه SSL/TLS برای احراز هویت
```
