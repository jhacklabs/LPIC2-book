---
title: "Topic 204 — Advanced Storage Device Administration (نسخه عمیق و کامل)"
exam: LPIC-2 / 201-450
weights: "204.1 (3) + 204.2 (2) + 204.3 (3)"
os_target: "Arch Linux / Omarchy"
tags: [lpic2, raid, lvm, iscsi, deep-dive]
---

# Topic 204: Advanced Storage Device Administration — راهنمای کامل و عمیق

---

# بخش اول — 204.1: پیکربندی RAID

## ۱.۱) مسئله‌ای که RAID حل می‌کند

هر دیسک سخت‌افزاری، هرقدر هم باکیفیت، روزی خراب می‌شود — این یک واقعیت آماری است، نه احتمال. **MTBF** (Mean Time Between Failures) هر دیسک عددی محدود است. سؤال این نیست که «آیا دیسک خراب می‌شود»، بلکه «چه زمانی» و «آیا وقتی خراب شد، داده از دست می‌رود یا نه».

**RAID** (Redundant Array of Independent Disks) یعنی چند دیسک فیزیکی را طوری کنار هم بچینی که کرنل (یا یک کنترلر سخت‌افزاری) آن‌ها را به‌عنوان **یک** دستگاه منطقی به بقیه‌ی سیستم نشان بدهد، با یکی از دو هدف: **سرعت بیشتر** یا **تحمل خرابی** (یا هر دو، بسته به سطح RAID).

### RAID نرم‌افزاری در برابر سخت‌افزاری

- **RAID سخت‌افزاری** — یک کارت کنترلر اختصاصی (با پردازنده و حافظه‌ی خودش) کل منطق RAID را مدیریت می‌کند؛ کرنل فقط یک دیسک معمولی می‌بیند. سریع‌تر (چون بار پردازشی روی CPU اصلی نمی‌افتد) اما گران‌تر و وابسته به آن کارت خاص (اگر کارت بسوزد، پیدا کردن یک کارت دقیقاً سازگار برای بازیابی داده سخت است).
- **RAID نرم‌افزاری** — کرنل خودش (از طریق `mdadm` و ماژول `md`) منطق RAID را پیاده می‌کند. ارزان‌تر، مستقل از سخت‌افزار خاص (می‌توانی آرایه را به هر سیستم لینوکسی دیگر منتقل کنی)، اما کمی بار پردازشی روی CPU اصلی می‌گذارد (که در CPU های امروزی معمولاً ناچیز است). این چیزی است که در LPIC-2 با آن کار می‌کنیم.

## ۱.۲) سطوح RAID — عمیق و با مثال محاسباتی

### RAID 0 — Striping (بدون افزونگی)

داده به قطعات کوچک (به نام **Stripe**) تقسیم می‌شود و به‌صورت متناوب روی همه‌ی دیسک‌ها نوشته می‌شود. اگر ۴ دیسک ۱ ترابایتی داری، ظرفیت کل ۴ ترابایت است (بدون هدررفت) و سرعت خواندن/نوشتن تقریباً ۴ برابر یک دیسک تنها می‌شود (چون همزمان از هر ۴ دیسک می‌خوانی/می‌نویسی).

**اما:** اگر **حتی یکی** از این ۴ دیسک خراب شود، **کل داده‌ی آرایه از بین می‌رود** — نه فقط سهم آن دیسک، بلکه همه‌چیز، چون هر فایل بین همه‌ی دیسک‌ها پخش شده. احتمال خرابی کل آرایه، مجموع احتمال خرابی هر دیسک است — یعنی RAID 0 در واقع **قابلیت اطمینان را کاهش می‌دهد**، فقط سرعت را افزایش می‌دهد. کاربرد مناسب: کش‌های موقت، رندر ویدیو، جایی که سرعت مهم است و داده جای دیگری هم بکاپ دارد.

### RAID 1 — Mirroring (آینه‌کاری)

هر بایت داده روی **همه‌ی** دیسک‌های آرایه (معمولاً ۲ تا) به‌طور کامل و همزمان کپی می‌شود. اگر یک دیسک از ۲ تا خراب شود، سیستم بدون هیچ وقفه‌ای با دیسک باقی‌مانده کار می‌کند — فقط باید دیسک خراب را عوض کنی و آرایه دوباره sync شود.

ظرفیت مؤثر: اگر ۲ دیسک ۱ ترابایتی داری، فضای قابل‌استفاده فقط **۱ ترابایت** است (نصف مجموع، چون یکی کپی دیگری است). سرعت **نوشتن** تقریباً برابر یک دیسک تنهاست (چون باید هر دو نسخه نوشته شوند)، اما سرعت **خواندن** می‌تواند بهتر باشد چون کرنل می‌تواند هوشمندانه از هر دو دیسک به‌صورت موازی بخواند.

### RAID 5 — Striping با Parity توزیع‌شده

RAID 5 نیاز به حداقل **۳ دیسک** دارد. داده مثل RAID 0 روی همه‌ی دیسک‌ها stripe می‌شود، اما علاوه‌بر آن، برای هر گروه از stripe ها، یک بلوک **parity** (اطلاعات محاسبه‌شده با عملیات XOR روی بلوک‌های داده) هم ذخیره می‌شود — و این بلوک parity به‌جای این‌که همیشه روی یک دیسک ثابت باشد، به‌صورت **چرخشی** بین همه‌ی دیسک‌ها توزیع می‌شود (این «توزیع‌شده» بودن نقطه‌ی تمایز RAID 5 با RAID 3/4 قدیمی‌تر است که parity را روی یک دیسک ثابت نگه می‌داشتند و آن دیسک را به یک گلوگاه تبدیل می‌کردند).

**چطور parity به تحمل خطا کمک می‌کند؟** عملیات XOR این ویژگی ریاضی جالب را دارد: اگر `A XOR B XOR C = P`، آنگاه اگر یکی از A، B، C را از دست بدهی، می‌توانی آن را از بقیه‌ی مقادیر و P دوباره محاسبه کنی (مثلاً `A = B XOR C XOR P`). پس اگر **یک** دیسک از آرایه خراب شود، کرنل می‌تواند محتوای گمشده را از بقیه‌ی دیسک‌ها + parity بازسازی کند.

ظرفیت مؤثر RAID 5 با N دیسک به اندازه‌ی یک دیسک هرگز از دست می‌رود: `(N-1) × اندازه‌ی هر دیسک`. مثلاً ۴ دیسک ۱ ترابایتی → ۳ ترابایت قابل‌استفاده (معادل یک دیسک برای parity «هزینه» می‌شود، هرچند واقعاً روی یک دیسک خاص نیست بلکه پخش است).

> ⚠️ نکته‌ی مهم درباره‌ی RAID 5 در دنیای واقعی امروز: با رشد حجم دیسک‌ها (۸ ترابایت، ۱۶ ترابایت)، زمان **بازسازی** (rebuild) یک آرایه‌ی RAID 5 بعد از تعویض یک دیسک خراب می‌تواند خیلی طولانی شود (گاهی روزها)، و در طول این مدت اگر **یک دیسک دیگر** هم خراب شود (که با دیسک‌های بزرگ و قدیمی محتمل‌تر است)، کل آرایه از دست می‌رود. برای همین در دنیای حرفه‌ای امروز، برای آرایه‌های بزرگ اغلب **RAID 6** (که با ۲ بلوک parity مستقل، تحمل خرابی همزمان ۲ دیسک را دارد) یا **RAID 10** ترجیح داده می‌شود — اما این‌ها فراتر از سرفصل رسمی LPIC-2 هستند، فقط باید بدانی چرا وجود دارند.

## ۱.۳) ساخت و مدیریت RAID با mdadm

```bash
sudo pacman -S mdadm
sudo mdadm --create /dev/md0 --level=1 --raid-devices=2 /dev/sdb1 /dev/sdc1
```
- `--create` — ساخت یک آرایه‌ی جدید
- `/dev/md0` — نام دستگاه منطقی جدید (md مخفف Multiple Devices)
- `--level=1` — سطح RAID (0, 1, 5, 6, 10 و غیره)
- `--raid-devices=2` — تعداد دیسک‌های فعال در آرایه

```bash
cat /proc/mdstat
```
این فایل مجازی وضعیت **زنده** تمام آرایه‌های RAID روی سیستم را نشان می‌دهد — از جمله درصد پیشرفت اگر آرایه در حال sync یا rebuild باشد:
```
Personalities : [raid1]
md0 : active raid1 sdc1[1] sdb1[0]
      976630464 blocks super 1.2 [2/2] [UU]
```
`[UU]` یعنی هر دو دیسک «Up» (سالم و همگام) هستند. اگر یک دیسک خراب باشد، می‌بینی `[U_]` — این یعنی هشدار فوری برای تعویض دیسک.

```bash
sudo mdadm --detail /dev/md0
```
گزارش کامل‌تر: وضعیت هر دیسک به‌طور جداگانه، UUID آرایه، سطح RAID، ظرفیت.

### شبیه‌سازی خرابی دیسک و بازیابی

```bash
sudo mdadm /dev/md0 --fail /dev/sdc1        # علامت‌گذاری دستی یک دیسک به‌عنوان خراب (برای تست)
sudo mdadm /dev/md0 --remove /dev/sdc1       # حذف فیزیکی از آرایه
# ... دیسک فیزیکی را عوض کن ...
sudo mdadm /dev/md0 --add /dev/sdd1          # افزودن دیسک جایگزین؛ rebuild خودکار شروع می‌شود
```

### فایل تنظیمات و پایداری بعد از ری‌بوت

برای این‌که آرایه بعد از هر بوت خودکار دوباره جمع شود (assemble)، باید تعریفش در فایل تنظیمات ذخیره شود:
```bash
sudo mdadm --detail --scan >> /etc/mdadm.conf
```
این دستور UUID آرایه را استخراج و در فایل می‌نویسد؛ initramfs موقع بوت این فایل را می‌خواند تا بداند کدام دیسک‌ها باید دوباره به هم متصل شوند.

```bash
sudo mdadm --stop /dev/md0                          # متوقف کردن (مثلاً قبل از جابه‌جایی فیزیکی)
sudo mdadm --assemble /dev/md0 /dev/sdb1 /dev/sdc1   # دوباره جمع کردن دستی
sudo mdadm --assemble --scan                          # جمع کردن خودکار همه‌ی آرایه‌های تعریف‌شده در mdadm.conf
```

### نوع پارتیشن Legacy

در جدول پارتیشن‌بندی سنتی MBR، نوع `0xFD` («Linux raid autodetect») به کرنل می‌گفت این پارتیشن بخشی از یک آرایه‌ی RAID است و باید در بوت خودکار شناسایی شود. با GPT و initramfs مدرن این نوع‌گذاری دیگر لازم نیست، اما آزمون هنوز این عدد را می‌پرسد.

## ۱.۴) نکات مهم آزمون برای 204.1

- ✅ سطوح RAID را با محاسبه‌ی ظرفیت مؤثر تمرین کن: RAID 0 = مجموع کامل (بدون افزونگی)، RAID 1 = نصف مجموع، RAID 5 = `(N-1) × اندازه‌ی هر دیسک`.
- ✅ حداقل تعداد دیسک: RAID 0 حداقل ۲، RAID 1 دقیقاً ۲ (یا بیشتر برای mirror های چندگانه)، RAID 5 حداقل ۳.
- ✅ نماد `[UU]` در `/proc/mdstat` را بشناس و بدانی `[U_]` یعنی یک دیسک خراب.
- ✅ فایل `/etc/mdadm.conf` برای پایداری آرایه بعد از ری‌بوت ضروری است.
- ✅ نوع پارتیشن legacy برای RAID: `0xFD`.

---

# بخش دوم — 204.2: دسترسی به دستگاه‌های ذخیره‌سازی

## ۲.۱) از IDE تا NVMe — تکامل رابط‌های ذخیره‌سازی

برای فهم چرا ابزارهای مختلف (`hdparm`, `sdparm`, `nvme`) وجود دارند، باید تاریخچه‌ی کوتاهی از رابط‌های دیسک بدانی:

- **IDE/PATA** — قدیمی‌ترین، رابط موازی، سرعت محدود
- **SATA** — جانشین سریالی IDE، امروز رایج‌ترین رابط برای HDD و SSD معمولی، از پروتکل ATA (که ریشه در IDE دارد) استفاده می‌کند
- **SCSI/SAS** — رابط سنتی سرورها و ذخیره‌سازی سازمانی، پروتکل کاملاً متفاوت از ATA
- **NVMe** — جدیدترین نسل، طراحی‌شده مخصوص SSD های مبتنی بر فلش که مستقیم به گذرگاه PCIe وصل می‌شوند (نه از طریق کنترلر SATA میانی) — سرعت به‌مراتب بالاتر چون صف‌های فرمان موازی بسیار بیشتری پشتیبانی می‌کند (NVMe تا ۶۵۵۳۶ صف با ۶۵۵۳۶ فرمان در هر صف، در برابر تنها ۱ صف با ۳۲ فرمان در AHCI/SATA سنتی).

این تفاوت پروتکلی دقیقاً دلیل وجود ابزارهای جداست:

```bash
sudo hdparm -I /dev/sda        # اطلاعات کامل برای دیسک‌های ATA/SATA
sudo hdparm -tT /dev/sda        # تست سرعت (T=cached، t=uncached واقعی از دیسک)
sudo hdparm -W 0 /dev/sda        # غیرفعال کردن write-cache (برای اطمینان بیشتر از یکپارچگی داده، به قیمت کارایی کمتر)
```

```bash
sdparm --all /dev/sda           # مشابه hdparm ولی برای دستگاه‌های مبتنی بر پروتکل SCSI (شامل بسیاری دیسک‌های SATA مدرن هم از طریق لایه‌ی ترجمه SCSI-ATA)
```

```bash
sudo nvme list                   # لیست تمام دستگاه‌های NVMe شناسایی‌شده
sudo nvme smart-log /dev/nvme0    # اطلاعات سلامت (معادل SMART برای NVMe، اما با متریک‌های اختصاصی فلش مثل "درصد سایش")
sudo nvme id-ctrl /dev/nvme0       # اطلاعات کامل کنترلر
```
یک متریک مهم در `nvme smart-log` که در SATA وجود ندارد: **percentage_used** — درصدی که نشان می‌دهد چقدر از عمر مفید سلول‌های فلش (بر اساس چرخه‌های نوشتن) مصرف شده — این یک مفهوم اختصاصی SSD/فلش است (HDD های مکانیکی چنین محدودیتی ندارند).

## ۲.۲) TRIM — چرا SSD ها به آن نیاز دارند

این یکی از مفاهیمی است که خیلی‌ها سطحی می‌دانند اما عمق آن برای درک واقعی SSD حیاتی است.

**مسئله:** در HDD مکانیکی، وقتی یک فایل را حذف می‌کنی، سیستم‌عامل فقط اشاره‌گر آن را از جدول فایل‌سیستم پاک می‌کند — بلاک‌های فیزیکی همچنان با داده‌ی قدیمی پر هستند اما «آزاد» علامت خورده‌اند، و دیسک هیچ نیازی به دانستن این ندارد چون هر نوشتن جدید مستقیم روی همان مکان فیزیکی انجام می‌شود (overwrite مستقیم ممکن است).

در SSD (حافظه‌ی فلش NAND) این‌طور نیست: فلش نمی‌تواند مستقیم یک بلاک را overwrite کند — باید اول کل یک **بلوک بزرگ** (Erase Block، معمولاً چند مگابایت) را کاملاً **پاک** کند، و بعد بنویسد. اگر SSD نداند کدام صفحات دیگر «واقعاً» استفاده نمی‌شوند (چون سیستم‌عامل هرگز به آن گفته)، هنگام نیاز به نوشتن باید ابتدا صفحات معتبر باقی‌مانده در آن بلوک را به جای دیگری منتقل کند، بعد کل بلوک را پاک کند، بعد بنویسد — این فرآیند به نام **Write Amplification** شناخته می‌شود و هم کارایی را کاهش می‌دهد و هم عمر فلش را کوتاه‌تر می‌کند.

**TRIM** دستوری است که سیستم‌عامل به SSD می‌فرستد تا صریحاً بگوید «این بلاک‌های خاص دیگر داده‌ی معتبر ندارند، هر وقت خواستی می‌توانی پاکشان کنی» — این به کنترلر SSD (که کارش «Garbage Collection» نام دارد) اجازه می‌دهد از قبل و در زمان بیکاری، این بلوک‌ها را پاک‌سازی کند، طوری که وقتی واقعاً نیاز به نوشتن سریع باشد، بلوک‌های آماده در دسترس باشند.

```bash
sudo fstrim -v /                        # اجرای دستی TRIM روی فایل‌سیستم ریشه، با گزارش (verbose) چقدر فضا آزاد شد
sudo fstrim -av                          # روی تمام فایل‌سیستم‌هایی که از TRIM پشتیبانی می‌کنند
sudo systemctl enable --now fstrim.timer  # اجرای خودکار هفتگی (روش توصیه‌شده به‌جای mount option پیوسته "discard")
```
> نکته: گزینه‌ی mount به نام `discard` هم وجود دارد که TRIM را **بلافاصله** بعد از هر حذف فایل اجرا می‌کند (به‌جای دوره‌ای)، اما این می‌تواند کمی کارایی نوشتن را کاهش دهد؛ برای همین امروز اجرای دوره‌ای با `fstrim.timer` روش ترجیحی است.

## ۲.۳) iSCSI — دیسک از راه دور، از طریق شبکه‌ی معمولی

**iSCSI** (Internet SCSI) به تو اجازه می‌دهد فرمان‌های پروتکل SCSI را روی یک شبکه‌ی TCP/IP معمولی (نه یک کابل اختصاصی Fibre Channel گران‌قیمت) بفرستی — یعنی یک سرور می‌تواند به یک دیسک روی سروری دیگر (یا یک آرایه‌ی ذخیره‌سازی اختصاصی، یعنی یک SAN) دقیقاً مثل یک دیسک محلی متصل شود و با آن کار کند.

دو نقش اصلی در معماری iSCSI:
- **Target** — سمت سرور، جایی که دیسک واقعی/فضای ذخیره‌سازی قرار دارد (این را «ارائه‌دهنده‌ی ذخیره‌سازی» فرض کن)
- **Initiator** — سمت کلاینت، سروری که می‌خواهد به آن دیسک از راه دور متصل شود

```bash
sudo pacman -S open-iscsi
sudo systemctl enable --now iscsid
```

فرآیند اتصال (initiator):
```bash
sudo iscsiadm -m discovery -t sendtargets -p 192.168.1.100
```
این دستور به آدرس مشخص‌شده وصل می‌شود و می‌پرسد «چه Target هایی برای من در دسترس داری؟» — پاسخ لیستی از **IQN** ها (iSCSI Qualified Names — شناسه‌ی یکتای هر Target، شبیه یک نام دامنه‌ی معکوس مثل `iqn.2024-01.com.example:storage.disk1`) است.

```bash
sudo iscsiadm -m node --login
```
اتصال واقعی به تمام Target های کشف‌شده. بعد از این، دیسک راه‌دور مثل یک `/dev/sdX` معمولی در سیستم ظاهر می‌شود — کاملاً شفاف برای بقیه‌ی ابزارها (می‌توانی رویش LVM بسازی، فرمت کنی، mount کنی).

```bash
sudo iscsiadm -m session          # لیست تمام session های فعال iSCSI
sudo iscsiadm -m node --logout     # قطع اتصال
```
فایل تنظیمات initiator: `/etc/iscsi/iscsid.conf`. شناسه‌ی خود initiator (که یک IQN منحصربه‌فرد است) در `/etc/iscsi/initiatorname.iscsi` ذخیره می‌شود.

### مفاهیم کلیدی SAN که باید بشناسی

- **LUN** (Logical Unit Number) — روی یک آرایه‌ی ذخیره‌سازی مشترک که خودش شاید ده‌ها ترابایت فضای فیزیکی دارد، فضای ذخیره‌سازی به «واحدهای منطقی» تقسیم می‌شود که هرکدام یک عدد LUN دارند — هر سروری که به آن SAN وصل می‌شود، فقط به LUN های اختصاص‌یافته به خودش دسترسی دارد (این جداسازی معمولاً با یک مکانیزم امنیتی به نام **LUN Masking** یا **Zoning** کنترل می‌شود).
- **WWID/WWN** (World Wide Identifier/Name) — یک شناسه‌ی یکتای جهانی برای یک دستگاه ذخیره‌سازی، مستقل از این‌که از کدام مسیر شبکه به آن وصل شده‌ای — این برای multipath (وقتی چند مسیر شبکه‌ی فیزیکی مختلف به یک دیسک وجود دارد، برای redundancy) اهمیت دارد چون WWID تضمین می‌کند سیستم می‌داند دو مسیر مختلف در واقع به **همان** دیسک فیزیکی می‌رسند.

پروتکل‌های جایگزین/مرتبط که باید فقط در حد آگاهی بشناسی:
- **AoE** (ATA over Ethernet) — پروتکلی ساده‌تر و سبک‌تر از iSCSI که مستقیم روی Ethernet کار می‌کند (بدون لایه‌ی IP)، ساده‌تر ولی کمتر قابل مسیریابی (routable) از iSCSI
- **FCoE** (Fibre Channel over Ethernet) — تلاش برای اجرای پروتکل Fibre Channel (استاندارد سنتی SAN های سازمانی بسیار بزرگ) روی زیرساخت شبکه‌ی Ethernet معمولی، به‌جای کابل‌کشی اختصاصی Fibre Channel گران

## ۲.۴) نکات مهم آزمون برای 204.2

- ✅ `hdparm` برای ATA/SATA، `sdparm` برای SCSI، `nvme` برای NVMe — هرکدام پروتکل متفاوت.
- ✅ TRIM را از نظر مفهومی عمیق بدان: چرا SSD نمی‌تواند overwrite مستقیم کند، Write Amplification چیست.
- ✅ `fstrim.timer` (دوره‌ای، توصیه‌شده) در برابر گزینه‌ی mount با `discard` (بلادرنگ، هزینه‌ی کارایی).
- ✅ نقش‌های iSCSI: Target (سرور) در برابر Initiator (کلاینت)، شناسایی با IQN.
- ✅ فرآیند: `discovery` → `--login` → دیسک به‌صورت `/dev/sdX` عادی ظاهر می‌شود.
- ✅ LUN (تقسیم منطقی فضا)، WWID/WWN (شناسه‌ی یکتای دستگاه، مهم برای multipath) را تعریف کن.

---

# بخش سوم — 204.3: LVM (Logical Volume Manager)

## ۳.۱) مسئله‌ای که LVM حل می‌کند

تصور کن یک سرور را با پارتیشن‌بندی سنتی راه‌اندازی کرده‌ای: `/` روی یک پارتیشن ۵۰ گیگابایتی، `/home` روی یک پارتیشن ۱۰۰ گیگابایتی. شش ماه بعد، `/home` پر می‌شود اما `/` هنوز جا دارد. با پارتیشن‌بندی سنتی، **نمی‌توانی** به‌سادگی فضا را از یکی به دیگری منتقل کنی — پارتیشن‌ها روی دیسک به‌صورت فیزیکی و پیوسته تعریف شده‌اند؛ برای تغییر اندازه باید کل دیسک را دوباره پارتیشن‌بندی کنی (با ریسک از دست دادن داده) یا یک دیسک کاملاً جدید اضافه کنی که دیگر به همان mount point وصل نمی‌شود مگر با کارهای دستی پیچیده.

**LVM** یک لایه‌ی انتزاعی **بین** پارتیشن‌های فیزیکی و فایل‌سیستم‌ها قرار می‌دهد که این محدودیت را کاملاً از بین می‌برد.

## ۳.۲) سه لایه‌ی LVM — عمیق

```
Physical Volume (PV)  ← یک یا چند دیسک/پارتیشن فیزیکی خام
        │
Volume Group (VG)      ← "استخر" مشترک فضا، ترکیب چند PV
        │
Logical Volume (LV)     ← یک "پارتیشن مجازی" که از فضای VG برش می‌خورد
        │
فایل‌سیستم (ext4/xfs/...) روی LV ساخته می‌شود
```

### Physical Volume (PV)

```bash
sudo pvcreate /dev/sdb1 /dev/sdc1
```
هر دیسک یا پارتیشنی که می‌خواهی جزء استخر LVM باشد، باید اول به PV تبدیل شود — این کار یک متادیتای کوچک LVM در ابتدای دستگاه می‌نویسد.

```bash
sudo pvs                    # خلاصه‌ی همه‌ی PV ها
sudo pvdisplay /dev/sdb1     # جزئیات کامل یک PV خاص
sudo pvremove /dev/sdb1       # حذف متادیتای LVM (بازگرداندن به یک دستگاه بلاکی خام معمولی)
```

### Volume Group (VG)

```bash
sudo vgcreate vg_data /dev/sdb1 /dev/sdc1
```
یک VG می‌تواند از **چند** PV تشکیل شود — یعنی می‌توانی فضای دو دیسک فیزیکی جداگانه را در یک استخر مشترک ترکیب کنی. این یعنی یک Logical Volume که بعداً از این VG می‌سازی، می‌تواند **بزرگ‌تر از هر دیسک فیزیکی تنها** باشد (چون از هر دو دیسک همزمان فضا می‌گیرد).

```bash
sudo vgs                          # خلاصه‌ی همه VG ها
sudo vgdisplay vg_data              # جزئیات کامل
sudo vgextend vg_data /dev/sdd1      # افزودن یک PV جدید به یک VG موجود (بزرگ‌کردن استخر)
sudo vgreduce vg_data /dev/sdc1       # حذف یک PV از VG (فقط اگر خالی از داده باشد)
```

### Logical Volume (LV)

```bash
sudo lvcreate -L 10G -n lv_home vg_data
```
`-L 10G` یعنی دقیقاً ۱۰ گیگابایت از فضای موجود در `vg_data` را به این LV اختصاص بده. یا می‌توانی با درصد کار کنی:
```bash
sudo lvcreate -l 50%FREE -n lv_home vg_data     # ۵۰ درصد از فضای *آزادِ باقی‌مانده* در VG
sudo lvcreate -l 100%FREE -n lv_data vg_data     # تمام فضای باقی‌مانده
```
(توجه: `-L` حرف بزرگ برای اندازه‌ی مطلق مثل `10G`، `-l` حرف کوچک برای واحد اکستنت/درصد)

```bash
sudo lvs                        # خلاصه‌ی همه‌ی LV ها
sudo lvdisplay /dev/vg_data/lv_home
```

## ۳.۳) تغییر اندازه‌ی پویا — چرا LVM واقعاً به‌کار می‌آید

```bash
sudo lvextend -L +5G /dev/vg_data/lv_home
sudo resize2fs /dev/vg_data/lv_home
```
اینجا یک نکته‌ی بسیار مهم است که باید عمیق بفهمی: `lvextend` فقط **کانتینر منطقی** (LV) را بزرگ می‌کند — این کاری در سطح LVM است. اما فایل‌سیستمی که **داخل** آن LV نشسته (ext4/xfs) از این تغییر **خبر ندارد** تا وقتی که خودش را دستی بزرگ کنی. برای همین همیشه این دو قدم با هم می‌آیند:
1. `lvextend` — بزرگ کردن ظرف
2. `resize2fs` (برای ext) یا `xfs_growfs` (برای XFS) — بزرگ کردن محتوای داخل ظرف تا کل فضای جدید را ببیند

می‌توانی این دو مرحله را در یک دستور ترکیب کنی:
```bash
sudo lvextend -L +5G -r /dev/vg_data/lv_home
```
پرچم `-r` (resize) خودکار بعد از extend، دستور resize مناسب فایل‌سیستم را هم صدا می‌زند — این روش امروز ترجیح داده می‌شود چون خطای فراموش کردن قدم دوم را از بین می‌برد.

> ⚠️ **هشدار جدی درباره‌ی کوچک‌کردن (Shrink):** ترتیب برعکس است! برای کوچک‌کردن یک LV، **اول باید فایل‌سیستم را کوچک کنی، بعد LV را**:
> ```bash
> sudo umount /home
> sudo resize2fs /dev/vg_data/lv_home 20G     # اول فایل‌سیستم را کوچک کن
> sudo lvreduce -L 20G /dev/vg_data/lv_home    # بعد ظرف را کوچک کن
> ```
> اگر ترتیب را برعکس کنی (اول ظرف را کوچک کنی)، ممکن است داده‌هایی که هنوز در فضای «حذف‌شده» بودند از بین بروند یا فایل‌سیستم به‌شدت خراب شود. **همچنین: XFS اصلاً قابل shrink نیست** (همان‌طور که در Topic 203 دیدیم) — پس این عملیات فقط برای ext قابل انجام است.

## ۳.۴) Snapshot در LVM — چطور کار می‌کند (و چرا با Btrfs فرق دارد)

```bash
sudo lvcreate -L 2G -s -n lv_home_snap /dev/vg_data/lv_home
```
پرچم `-s` یعنی این LV جدید یک **snapshot** از LV مبدأ است. برخلاف Btrfs (که ذاتاً Copy-on-Write است و snapshot تقریباً رایگان است)، LVM سنتی (thick provisioning) از یک مکانیزم متفاوت به نام **Copy-on-Write در سطح LVM** استفاده می‌کند: وقتی LV اصلی تغییر می‌کند، **قبل از** overwrite، بلوک قدیمی به فضای اختصاصی snapshot کپی می‌شود (نه برعکس مثل Btrfs). این یعنی:
- اندازه‌ی snapshot باید از قبل تخمین زده شود (`-L 2G` در مثال بالا) — این فضا فقط برای نگه‌داشتن **تغییراتی** که از لحظه‌ی گرفتن snapshot اتفاق می‌افتد کافی است.
- اگر تغییرات بیشتر از فضای تخصیص‌یافته به snapshot باشد، snapshot **پر** می‌شود و از کار می‌افتد (invalid می‌شود) — این یک تفاوت مهم و محدودیت واقعی نسبت به Btrfs.

کاربرد رایج: قبل از یک آپدیت پرریسک، یک snapshot می‌گیری؛ اگر آپدیت مشکل داشت، LV اصلی را با محتوای snapshot جایگزین می‌کنی (`lvconvert --merge`)؛ اگر همه‌چیز خوب بود، snapshot را حذف می‌کنی تا فضا آزاد شود.

## ۳.۵) فعال‌سازی و فایل تنظیمات

```bash
sudo vgchange -ay vg_data       # فعال‌سازی (activate) تمام LV های یک VG — لازم بعد از بوت یا بعد از اتصال یک دیسک خارجی حاوی LVM
sudo vgchange -an vg_data        # غیرفعال‌سازی
```
فایل تنظیمات اصلی LVM: `/etc/lvm/lvm.conf` — شامل تنظیماتی مثل این‌که کدام دستگاه‌ها اصلاً باید به‌عنوان PV احتمالی اسکن شوند (`filter`)، تنظیمات پشتیبان‌گیری خودکار از متادیتا، و غیره.

هر LV علاوه‌بر مسیر استاندارد `/dev/vg_data/lv_home`، از طریق device mapper هم قابل‌دسترسی است:
```
/dev/mapper/vg_data-lv_home
```
این دو مسیر واقعاً به یک دستگاه اشاره می‌کنند — اولی یک symlink خواناتر برای انسان است.

## ۳.۶) نکات مهم آزمون برای 204.3

- ✅ ترتیب ساخت را حفظ کن: `pvcreate` → `vgcreate` → `lvcreate`.
- ✅ ترتیب **بزرگ‌کردن**: اول `lvextend`، بعد `resize2fs`/`xfs_growfs` (یا هر دو با `-r`).
- ✅ ترتیب **کوچک‌کردن** برعکس است: اول `resize2fs` (کوچک‌تر)، بعد `lvreduce` — و فقط برای ext، نه XFS.
- ✅ یک VG می‌تواند از چند PV تشکیل شود؛ یک LV می‌تواند بزرگ‌تر از هر دیسک تنها باشد.
- ✅ تفاوت مکانیزم snapshot در LVM سنتی (فضای از‌پیش‌تخصیص‌یافته، می‌تواند پر و invalid شود) با Btrfs (CoW ذاتی، رایگان).
- ✅ `vgchange -ay`/`-an` برای فعال/غیرفعال‌سازی، مهم برای سناریوهای انتقال دیسک بین سیستم‌ها.

---

## خلاصه‌ی جدولی نهایی کل Topic 204

| زیرمبحث | مفهوم کلیدی | جزئیات |
|---|---|---|
| 204.1 | RAID 0 | Striping، بدون افزونگی، سرعت بالا، خطر بالا |
| 204.1 | RAID 1 | Mirroring، ظرفیت = نصف، تحمل خرابی ۱ دیسک |
| 204.1 | RAID 5 | Parity توزیع‌شده (XOR)، حداقل ۳ دیسک، ظرفیت=(N-1)×دیسک |
| 204.1 | ابزار | `mdadm --create/--detail/--assemble`, `/proc/mdstat`, `/etc/mdadm.conf` |
| 204.2 | پروتکل‌ها | `hdparm`(ATA)، `sdparm`(SCSI)، `nvme`(NVMe) |
| 204.2 | TRIM | Write Amplification، `fstrim.timer` vs `discard` |
| 204.2 | iSCSI | Target(سرور)/Initiator(کلاینت)، IQN، `iscsiadm` |
| 204.2 | SAN مفاهیم | LUN، WWID/WWN، AoE/FCoE (آگاهی) |
| 204.3 | سه لایه | PV → VG → LV |
| 204.3 | تغییر اندازه | Extend: LV اول؛ Reduce: FS اول (فقط ext) |
| 204.3 | Snapshot | فضای از‌پیش‌تخصیص‌یافته، ریسک پر شدن |

## مایندمپ متنی کامل

```
Topic 204: Advanced Storage Device Administration
│
├── 204.1 RAID
│   ├── سخت‌افزاری vs نرم‌افزاری (mdadm)
│   ├── RAID 0: Striping، بدون افزونگی
│   ├── RAID 1: Mirroring، ظرفیت=۱ دیسک
│   ├── RAID 5: Parity توزیع‌شده (XOR)، (N-1)×دیسک
│   ├── mdadm: create/detail/fail/remove/add/assemble
│   ├── /proc/mdstat: [UU] سالم / [U_] خراب
│   └── /etc/mdadm.conf (پایداری بعد بوت)
│
├── 204.2 Storage Access
│   ├── تکامل رابط‌ها: IDE→SATA→SCSI/SAS→NVMe
│   ├── hdparm/sdparm/nvme (هرکدام پروتکل خودش)
│   ├── TRIM: چرا SSD نیاز دارد (Write Amplification)
│   │   └── fstrim.timer (دوره‌ای) vs discard (بلادرنگ)
│   └── iSCSI: Target/Initiator, IQN
│       ├── discovery → login → /dev/sdX عادی
│       └── LUN, WWID/WWN, AoE/FCoE
│
└── 204.3 LVM
    ├── PV (pvcreate) → VG (vgcreate) → LV (lvcreate)
    ├── Extend: lvextend سپس resize2fs/xfs_growfs (یا -r)
    ├── Reduce: resize2fs سپس lvreduce (فقط ext، نه XFS)
    ├── Snapshot: فضای محدود از‌پیش، ریسک پر شدن
    └── vgchange -ay/-an، /etc/lvm/lvm.conf
```
