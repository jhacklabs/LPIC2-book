---
title: "Topic 201 — Linux Kernel (نسخه عمیق و کامل)"
exam: LPIC-2 / 201-450
weights: "201.1 (2) + 201.2 (3) + 201.3 (4)"
os_target: "Arch Linux / Omarchy"
tags: [lpic2, kernel, modules, compiling, deep-dive]
---

# Topic 201: Linux Kernel — راهنمای کامل و عمیق

> این نسخه‌ی گسترش‌یافته‌ی درس 201 است. برخلاف نسخه‌ی قبلی که یک خلاصه‌ی فشرده بود، اینجا قرار است **بفهمی چرا هر چیز همانطور کار می‌کند که کار می‌کند** — نه فقط دستور را حفظ کنی. سه زیرمبحث (201.1، 201.2، 201.3) هرکدام به‌عنوان یک بخش کامل و مستقل، با عمق کافی برای واقعاً فهمیدن (نه فقط رد شدن از آزمون) پوشش داده می‌شوند.

---

# بخش اول — 201.1: اجزای کرنل (Kernel Components)

## ۱.۱) مفهوم کلی: کرنل دقیقاً چیست؟

بیا از صفر شروع کنیم. وقتی کامپیوترت را روشن می‌کنی، پردازنده (CPU) فقط بلد است دستورهای ماشین را از حافظه بخواند و اجرا کند — همین. پردازنده نمی‌داند «فایل» چیست، نمی‌داند «پروسه» چیست، نمی‌داند دو برنامه چطور باید به‌نوبت از یک هسته‌ی پردازنده استفاده کنند بدون اینکه به هم دیگر آسیب بزنند. این‌ها همه مفاهیمی هستند که **کرنل** آن‌ها را می‌سازد و به بقیه‌ی سیستم (برنامه‌ها، کاربر) عرضه می‌کند.

به زبان ساده‌تر: کرنل یک **لایه‌ی واسط** بین سخت‌افزار خام (پردازنده، RAM، دیسک، کارت شبکه) و برنامه‌های کاربردی (فایرفاکس، پایتون، bash) است. این لایه‌ی واسط سه وظیفه‌ی اصلی دارد:

1. **مدیریت پردازه‌ها (Process Management)** — تصمیم می‌گیرد کدام برنامه، چه زمانی، روی کدام هسته‌ی CPU اجرا شود (Scheduler). وقتی می‌گویی «۱۰ تا تب کروم باز است و همه هم‌زمان کار می‌کنند»، این توهم را کرنل با جابه‌جایی فوق‌سریع بین پردازه‌ها (Context Switching) ایجاد می‌کند — واقعاً هم‌زمان نیستند، فقط آنقدر سریع جابه‌جا می‌شوند که برای انسان همزمان به‌نظر می‌رسد.

2. **مدیریت حافظه (Memory Management)** — به هر پردازه یک فضای آدرس مجازی (Virtual Address Space) اختصاص می‌دهد طوری که هر برنامه فکر می‌کند کل حافظه‌ی سیستم مال خودش است، در حالی که در پس‌زمینه کرنل این آدرس‌های مجازی را به آدرس‌های فیزیکی واقعی RAM نگاشت (map) می‌کند و مطمئن می‌شود دو برنامه نمی‌توانند به حافظه‌ی همدیگر دستبرد بزنند.

3. **مدیریت دستگاه‌ها و I/O (Device & I/O Management)** — درایورهای سخت‌افزاری بخشی از کرنل (یا ماژول‌های قابل‌بارگذاری در آن) هستند که یاد گرفته‌اند چطور با یک تراشه‌ی خاص (مثلاً کنترلر SATA یا کارت شبکه‌ی Intel) صحبت کنند، و این پیچیدگی را از برنامه‌ها پنهان می‌کنند — برنامه فقط `write()` صدا می‌زند، نمی‌داند پشت صحنه چه اتفاقی برای نوشتن روی SSD می‌افتد.

### تشبیه: کرنل مثل یک مدیر ساختمان اداری

فرض کن یک ساختمان اداری بزرگ داری با صدها اتاق (منابع سخت‌افزاری) و صدها مستأجر (برنامه‌ها). اگر هر مستأجر مستقیم به تأسیسات ساختمان (برق، آب، آسانسور) دسترسی داشت، هرج‌ومرج می‌شد — دو نفر همزمان سعی می‌کردند از یک آسانسور استفاده کنند، یکی برق طبقه‌ی دیگری را قطع می‌کرد. کرنل همان **مدیر ساختمان** است: مستأجرها (برنامه‌ها) فقط با مدیر (کرنل، از طریق System Call ها) صحبت می‌کنند، و مدیر پشت صحنه هماهنگی واقعی با تأسیسات (سخت‌افزار) را انجام می‌دهد.

## ۱.۲) چرا این تفکیک مهم است؟ (User Space vs Kernel Space)

پردازنده‌های مدرن x86/ARM از مفهومی به نام **Ring** یا **Privilege Level** پشتیبانی می‌کنند. کرنل در **Ring 0** (بالاترین سطح دسترسی، به آن **Kernel Space** هم می‌گویند) اجرا می‌شود و می‌تواند مستقیم به سخت‌افزار دستور بدهد. برنامه‌های معمولی (فایرفاکس، bash، pip) در **Ring 3** (**User Space**) اجرا می‌شوند و **هیچ** دسترسی مستقیمی به سخت‌افزار ندارند.

وقتی یک برنامه می‌خواهد کاری انجام دهد که نیاز به سخت‌افزار دارد (مثلاً یک فایل بخواند)، باید یک **System Call** (فراخوانی سیستمی) صادر کند — یعنی یک درخواست رسمی به کرنل بفرستد («لطفاً این فایل را برایم باز کن»)، و کرنل تصمیم می‌گیرد آیا این درخواست مجاز است یا نه (آیا این کاربر اجازه‌ی خواندن این فایل را دارد؟) و اگر مجاز بود، کار را انجام می‌دهد.

**چرا این مهم است؟** چون این جداسازی، پایه‌ی **امنیت** کل سیستم لینوکس است. اگر فایرفاکس باگ داشته باشد و کرش کند، فقط خودش کرش می‌کند — نمی‌تواند به حافظه‌ی برنامه‌ی دیگری یا کل سیستم آسیب بزند، چون اصلاً دسترسی مستقیم به آن ندارد. این دقیقاً برخلاف سیستم‌عامل‌های قدیمی (مثل DOS) است که همه‌چیز در یک فضای مشترک اجرا می‌شد و یک باگ می‌توانست کل سیستم را از کار بیندازد.

از منظر Cyber Security: بسیاری از حملات جدی (privilege escalation) دقیقاً تلاش برای **شکستن این مرز** هستند — یعنی یک مهاجم که فقط دسترسی User Space دارد، سعی می‌کند از طریق یک آسیب‌پذیری (باگ در یک System Call، یا یک درایور کرنل با باگ) به Kernel Space برسد، چون اگر به آنجا برسد، کنترل کامل سیستم را در دست می‌گیرد.

## ۱.۳) معماری کرنل لینوکس: مونولیتیک با ماژول‌های قابل‌بارگذاری

در دنیای طراحی سیستم‌عامل، دو فلسفه‌ی اصلی وجود دارد:

- **Microkernel** (میکروکرنل) — فقط حداقل ضروری‌ترین چیزها (مدیریت حافظه‌ی پایه، ارتباط بین پردازه‌ای) در Kernel Space هستند؛ درایورها و فایل‌سیستم و بقیه در User Space اجرا می‌شوند (مثال: QNX، سیستم‌عامل‌های میکروکرنلی مثل MINIX 3).
- **Monolithic Kernel** (کرنل مونولیتیک) — همه‌چیز (درایورها، فایل‌سیستم، شبکه، زمان‌بند) در یک فضای واحد Kernel Space اجرا می‌شود. سریع‌تر است (چون نیازی به رد و بدل کردن پیام بین اجزای مختلف نیست) اما اگر یک درایور باگ داشته باشد، می‌تواند کل کرنل را کرش کند (Kernel Panic).

**لینوکس یک کرنل مونولیتیک است**، اما با یک ویژگی هوشمندانه که آن را انعطاف‌پذیر می‌کند: **ماژول‌های قابل‌بارگذاری (Loadable Kernel Modules — LKM)**. یعنی به‌جای اینکه همه‌ی درایورهای ممکن (برای هزاران مدل کارت گرافیک، شبکه، دیسک) همیشه در حافظه بارگذاری شده باشند (که حجم عظیمی از RAM را هدر می‌دهد)، کرنل می‌تواند در زمان اجرا (runtime) فقط ماژول‌های لازم را بارگذاری یا حذف کند — این دقیقاً همان چیزی است که در بخش 201.3 با `modprobe`/`insmod`/`rmmod` کار می‌کنیم.

> این معماری را «Monolithic-Modular» هم می‌نامند — از نظر عملکرد مثل مونولیتیک سریع است، از نظر انعطاف‌پذیری شبیه میکروکرنل عمل می‌کند.

## ۱.۴) شماره‌گذاری نسخه‌ی کرنل (Kernel Versioning)

این نکته‌ای‌ست که خیلی از دانشجویان LPIC روی آن گیج می‌شوند. فرمت نسخه‌ی کرنل لینوکس به این شکل است:

```
6.9.3-arch1-1
│ │ │  │
│ │ │  └── شماره build اختصاصی توزیع (اینجا Arch)
│ │ └───── تگ توزیع (arch1 یعنی پچ‌های Arch)
│ └─────── PATCHLEVEL (نسخه‌ی فرعی)
└───────── VERSION (نسخه‌ی اصلی)
```

قبل از نسخه‌ی ۳.۰ (تا حدود سال ۲۰۱۱)، یک قانون قدیمی وجود داشت: عدد وسط (PATCHLEVEL) اگر **زوج** بود یعنی نسخه‌ی **پایدار (stable)**، اگر **فرد** بود یعنی نسخه‌ی **آزمایشی/توسعه (development)** بود. مثلاً کرنل‌های ۲.۴.x پایدار بودند، ۲.۵.x توسعه‌ای بودند (که بعداً به ۲.۶ تبدیل شدند). **از نسخه‌ی ۳.۰ به بعد این قانون کنار گذاشته شد** — امروز اعداد صرفاً ترتیبی هستند و زوج/فرد بودن معنایی ندارد. این نکته دقیقاً سؤال کلاسیک آزمون LPIC است: «آیا کرنل ۴.۹ پایدار است یا توسعه‌ای؟» — جواب: این سؤال دیگر معنا ندارد، چون قانون قدیمی حذف شده.

می‌توانی خودت این را روی سیستمت ببینی:
```bash
uname -r
```

## ۱.۵) مستندات و منابع کرنل — کجا باید دنبال جواب بگردی؟

وقتی سورس کرنل را دانلود می‌کنی (در بخش ۲۰۱.۲ دقیق می‌بینیم)، یک پوشه‌ی عظیم به نام `Documentation/` در ریشه‌ی آن هست که خودش صدها فایل متنی دارد — این **مرجع رسمی و قابل‌اعتمادترین منبع** برای هر سؤال فنی درباره‌ی رفتار کرنل است، چون مستقیم توسط کسانی نوشته شده که کد را نوشته‌اند.

ساختار کلی این پوشه که باید بشناسی:
- `Documentation/admin-guide/` — راهنمای مدیران سیستم (سیس‌کتل‌ها، پارامترهای بوت)
- `Documentation/process/` — فرآیند مشارکت در توسعه‌ی کرنل
- `Documentation/filesystems/` — جزئیات هر فایل‌سیستم
- `Documentation/networking/` — پشته‌ی شبکه
- `Documentation/driver-api/` — رابط برنامه‌نویسی برای نوشتن درایور

همچنین فایل‌های زیر مستقیماً در ریشه‌ی سورس هستند و هر کدام نقش خاصی دارند:
- `README` — نمای کلی و راهنمای شروع سریع
- `MAINTAINERS` — فهرست افرادی که مسئول هر بخش از کد هستند (اگر بخواهی باگی گزارش کنی، باید بدانی به کدام ایمیل بفرستی)
- `CREDITS` — فهرست مشارکت‌کنندگان
- `Kconfig` (در زیرپوشه‌های مختلف) — تعریف گزینه‌های پیکربندی که در `make menuconfig` می‌بینی

### تصاویر کرنل: zImage در برابر bzImage

وقتی کرنل کامپایل می‌شود، خروجی نهایی یک فایل فشرده‌شده است که در بوت لودر (GRUB) به آن اشاره می‌شود. دو فرمت تاریخی وجود دارد:

- **zImage** — فرمت قدیمی، برای کرنل‌های کوچک (کمتر از ۵۱۲ کیلوبایت فشرده)، امروز عملاً منسوخ شده و فقط در معماری‌های embedded خاص دیده می‌شود.
- **bzImage** (big zImage) — فرمت استاندارد امروزی. حرف «b» به معنای «Big» است، نه الگوریتم فشرده‌سازی bzip2 (این یک اشتباه رایج است — سؤال گمراه‌کننده‌ی رایج در آزمون!). bzImage از الگوریتم‌های فشرده‌سازی مختلفی می‌تواند استفاده کند: gzip، bzip2، lzma، xz، lzo، یا zstd — انتخاب الگوریتم در `make menuconfig` مشخص می‌شود.

فایل bzImage معمولاً در `/boot/vmlinuz-<version>` قرار می‌گیرد. نام «vmlinuz» خودش تاریخچه دارد: «vm» از «Virtual Memory» می‌آید (چون یونیکس سنتی از نامی شبیه این استفاده می‌کرد)، و «z» نشان می‌دهد که فایل فشرده (compressed) است.

## ۱.۶) نکات مهم آزمون برای 201.1

- ✅ هرگز فراموش نکن: از کرنل ۳.۰ به بعد، قانون زوج/فرد PATCHLEVEL دیگر معتبر نیست.
- ✅ bzImage یعنی «Big zImage»، **نه** ارتباطی با فشرده‌سازی bzip2 دارد — این رایج‌ترین تله‌ی آزمون در این بخش است.
- ✅ فایل `MAINTAINERS` را بشناس: مشخص می‌کند چه کسی مسئول کدام زیرسیستم است.
- ✅ `Documentation/` رسمی‌ترین منبع فنی است، جلوتر از هر کتاب یا سایت.
- ✅ نام‌گذاری `vmlinuz` در `/boot/` یعنی کرنل فشرده و آماده‌ی بوت.

---

# بخش دوم — 201.2: کامپایل کرنل (Compiling a Kernel)

## ۲.۱) چرا اصلاً باید کرنل را از سورس کامپایل کنیم؟

این سؤالی است که بیشتر مبتدی‌ها می‌پرسند، چون امروز تقریباً هیچ‌کس مجبور نیست کرنل را دستی کامپایل کند — پکیج منیجرها (مثل `pacman` روی Arch) کرنل‌های از‌پیش‌کامپایل‌شده و بهینه ارائه می‌دهند. اما چند دلیل واقعی برای این کار در دنیای حرفه‌ای وجود دارد:

1. **کاهش سطح حمله (Attack Surface Reduction)** — یک کرنل عمومی (generic) که توزیع‌ها ارائه می‌دهند، برای پشتیبانی از **هر نوع سخت‌افزار ممکن** کامپایل شده — یعنی صدها درایور و زیرسیستم که سرور تو هرگز از آن‌ها استفاده نمی‌کند اما همچنان در حافظه بار می‌شوند و می‌توانند نقطه‌ی ورود آسیب‌پذیری باشند. با کامپایل سفارشی، فقط چیزهایی که واقعاً لازم داری را نگه می‌داری.

2. **بهینه‌سازی برای سخت‌افزار خاص** — کرنل عمومی باید روی طیف وسیعی از پردازنده‌ها کار کند (سازگاری حداکثری). یک کرنل سفارشی می‌تواند برای دقیقاً معماری پردازنده‌ی تو (مثلاً بهینه‌سازی‌های خاص AMD Zen4) کامپایل شود و کارایی بهتری بدهد.

3. **پچ‌های امنیتی فوری** — گاهی یک آسیب‌پذیری بحرانی کشف می‌شود و پچ رسمی آن هنوز به توزیع تو نرسیده. اگر بلد باشی کرنل را دستی پچ و کامپایل کنی، نیازی نیست منتظر توزیع بمانی.

4. **قابلیت‌های آزمایشی/سفارشی** — گاهی می‌خواهی یک ویژگی خاص (مثلاً یک فایل‌سیستم آزمایشی، یا یک patch سفارشی مثل CachyOS/Zen scheduler patches) را فعال کنی که در کرنل استاندارد نیست.

5. **سیستم‌های Embedded** — در دنیای IoT و embedded، حافظه‌ی flash محدود است؛ باید کرنل را تا حداقل ممکن کوچک کنی.

## ۲.۲) دریافت سورس کرنل

سورس رسمی کرنل لینوکس از kernel.org توزیع می‌شود، اما در عمل بیشتر مدیران از سورس **توزیع خودشان** استفاده می‌کنند چون شامل پچ‌های اختصاصی همان توزیع هم هست.

روی Arch/Omarchy، بسته‌ی `linux-headers` هدرهای لازم برای کامپایل ماژول‌های خارجی (نه کل کرنل) را می‌دهد:
```bash
sudo pacman -S linux-headers
```

برای کامپایل **کامل** یک کرنل از صفر (سناریوی رایج آزمون LPIC که مستقل از توزیع است)، سورس معمولاً به‌صورت فایل tar فشرده دریافت می‌شود:
```bash
wget https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.9.3.tar.xz
tar xf linux-6.9.3.tar.xz
cd linux-6.9.3/
```

> ⚠️ **هشدار درباره‌ی حجم:** سورس کامل کرنل بعد از استخراج حدود ۲ تا ۳ گیگابایت فضا می‌گیرد و کامپایل کامل آن می‌تواند بین ۲۰ دقیقه تا چند ساعت طول بکشد (بسته به قدرت CPU و تعداد گزینه‌های فعال). قبل از شروع مطمئن شو فضای دیسک کافی داری.

## ۲.۳) پیکربندی کرنل (Kernel Configuration) — قلب فرآیند کامپایل

قبل از کامپایل، باید مشخص کنی **کدام قابلیت‌ها** در کرنل نهایی فعال باشند. این تنظیمات در یک فایل به نام `.config` در ریشه‌ی سورس ذخیره می‌شوند — این فایل هزاران خط دارد، هر خط یک گزینه مثل:
```
CONFIG_EXT4_FS=y
CONFIG_USB_SUPPORT=y
CONFIG_WIRELESS=m
# CONFIG_ISDN is not set
```

هر گزینه سه حالت ممکن دارد:
- `y` (yes) — به‌صورت دائم و مستقیم در کرنل کامپایل می‌شود (Built-in)
- `m` (module) — به‌صورت یک ماژول جدا کامپایل می‌شود که بعداً می‌توان با `modprobe` بارگذاری/حذف کرد
- خالی/`n` — کاملاً غیرفعال و از کرنل نهایی حذف می‌شود

**چرا این تفاوت مهم است؟** اگر یک درایور را `y` کنی، همیشه در حافظه است (حتی اگر استفاده نشود) و کرنل کمی بزرگ‌تر می‌شود اما هیچ تأخیری برای بارگذاری ندارد. اگر `m` کنی، فقط وقتی نیاز باشد بارگذاری می‌شود (صرفه‌جویی در RAM) اما یک گام اضافه (بارگذاری ماژول) لازم دارد. **قانون رایج:** چیزهایی که برای **بوت شدن** ضروری هستند (مثلاً درایور کنترلر دیسکی که ریشه روی آن است) باید `y` باشند، وگرنه سیستم قبل از رسیدن به جایی که بتواند ماژول بارگذاری کند، گیر می‌افتد (دقیقاً همان مشکلی که initramfs حل می‌کند — در بخش بعد می‌بینیم).

### روش‌های مختلف پیکربندی (make targets)

کرنل چندین «رابط کاربری» متفاوت برای ویرایش `.config` ارائه می‌دهد:

```bash
make config
```
قدیمی‌ترین و ابتدایی‌ترین روش — کرنل **هزاران سؤال متوالی** از تو می‌پرسد («آیا از فلان درایور پشتیبانی می‌خواهی؟ [Y/n]») و باید یک‌به‌یک جواب بدهی. عملاً امروز هیچ‌کس از این استفاده نمی‌کند چون طاقت‌فرساست، اما در آزمون به‌عنوان گزینه می‌آید.

```bash
make menuconfig
```
رایج‌ترین روش در عمل — یک رابط متنی تعاملی مبتنی بر **ncurses** با منوهای تودرتو که با فلش‌ها و اسپیس پیمایش می‌شود. این چیزی است که واقعاً استفاده می‌کنی.

```bash
make xconfig
```
رابط گرافیکی مبتنی بر **Qt** — نیاز به محیط دسکتاپ گرافیکی دارد.

```bash
make gconfig
```
رابط گرافیکی مبتنی بر **GTK** — مشابه xconfig ولی با کتابخانه‌ی متفاوت (این تارگت در نسخه‌های جدیدتر کرنل حذف شده، اما در آزمون‌های LPIC هنوز می‌آید).

```bash
make oldconfig
```
این یکی از مهم‌ترین‌هاست برای **ارتقای کرنل**: وقتی یک `.config` قدیمی از نسخه‌ی قبلی کرنل داری و می‌خواهی نسخه‌ی جدیدتر را کامپایل کنی، این دستور تمام تنظیمات قبلی را نگه می‌دارد و **فقط برای گزینه‌های جدیدی که در نسخه‌ی جدید اضافه شده‌اند** از تو سؤال می‌پرسد — به‌جای اینکه از صفر شروع کنی.

```bash
make defconfig
```
از یک پیکربندی پیش‌فرض (تعریف‌شده توسط توسعه‌دهندگان معماری، مثلاً x86_64) استفاده می‌کند — نقطه‌ی شروع خوبی برای کسی که نمی‌خواهد از صفر تنظیم کند.

```bash
make localmodconfig
```
یک ترفند بسیار کاربردی: کرنل فعلی در حال اجرا را بررسی می‌کند (از `lsmod`)، و `.config` جدید را طوری می‌سازد که **فقط ماژول‌هایی که الان واقعاً استفاده می‌شوند** را نگه دارد — بقیه غیرفعال می‌شوند. این سریع‌ترین راه برای ساخت یک کرنل سبک و اختصاصی برای سخت‌افزار خودت است.

```bash
make mrproper
```
**پاک‌سازی کامل** — همه‌ی فایل‌های ساخته‌شده (object files) **و همچنین خود فایل `.config`** حذف می‌شوند. این را وقتی استفاده کن که می‌خواهی کاملاً از صفر شروع کنی.

```bash
make clean
```
پاک‌سازی جزئی‌تر از `mrproper` — فایل‌های object و باینری‌های میانی حذف می‌شوند، **اما `.config` و چند فایل تنظیماتی دیگر نگه داشته می‌شوند**. برای زمانی مناسب است که فقط می‌خواهی دوباره کامپایل کنی بدون از دست دادن تنظیمات.

> ⚠️ **هشدار جدی:** `make mrproper` برگشت‌ناپذیر است برای تنظیمات — اگر `.config` سفارشی ساعت‌ها وقت گذاشته‌ای برایش، **قبل از اجرا حتماً یک کپی بگیر**:
> ```bash
> cp .config .config.backup
> make mrproper
> cp .config.backup .config
> ```

## ۲.۴) فرآیند واقعی کامپایل

بعد از این‌که `.config` را نهایی کردی:

```bash
make -j$(nproc)
```
پرچم `-j` به `make` می‌گوید چند job (کار) را همزمان اجرا کند. `$(nproc)` تعداد هسته‌های منطقی پردازنده را برمی‌گرداند (مثلاً روی یک CPU با ۸ هسته/۱۶ ترد، عدد ۱۶ را می‌دهد). این کامپایل موازی می‌تواند زمان کامپایل را از چند ساعت به چند دقیقه کاهش دهد. **نکته‌ی عملی:** اگر `-j` را بیش از حد بزرگ بزنی (مثلاً `-j100` روی یک سیستم ۴ هسته‌ای)، سیستم ممکن است به‌خاطر کمبود RAM هنگ کند — قانون سرانگشتی معمول `nproc` یا کمی بیشتر است.

این دستور چند مرحله را پشت سر هم انجام می‌دهد:
1. کامپایل تمام فایل‌های `.c` به فایل‌های object (`.o`)
2. لینک کردن همه‌ی این‌ها به یک فایل ELF عظیم به نام `vmlinux` (بدون فشرده‌سازی، حجم بسیار بزرگ)
3. فشرده‌سازی و بسته‌بندی آن به `bzImage`
4. کامپایل جداگانه‌ی هر چیزی که `m` علامت‌گذاری شده، به فایل‌های `.ko` (Kernel Object)

خروجی نهایی معمولاً در این مسیر قرار می‌گیرد:
```
arch/x86/boot/bzImage
```

### نصب ماژول‌ها و کرنل

```bash
sudo make modules_install
```
تمام فایل‌های `.ko` که کامپایل شده‌اند را به مسیر استاندارد `/lib/modules/<kernel-version>/` کپی می‌کند و یک نگاشت وابستگی (dependency map) برایشان می‌سازد.

```bash
sudo make install
```
این دستور چند کار همزمان انجام می‌دهد:
- کپی `bzImage` به `/boot/vmlinuz-<version>`
- کپی فایل `System.map` (نگاشت نمادها/آدرس‌های کرنل، برای عیب‌یابی سطح پایین) به `/boot/`
- کپی `.config` به `/boot/config-<version>` (برای مرجع بعدی)
- **به‌روزرسانی خودکار GRUB** (در بیشتر توزیع‌ها از طریق hook هایی که با پکیج‌های bootloader نصب شده‌اند)

> ⚠️ **هشدار بسیار جدی — این مهم‌ترین هشدار کل این درس است:** هرگز کرنل قدیمی و کارآمد را جایگزین نکن بدون این‌که یک نسخه‌ی fallback در GRUB داشته باشی. اگر کرنل جدید بوت نشود (مثلاً چون یک درایور ضروری را اشتباهی `n` کرده‌ای)، بدون fallback باید از یک Live USB بوت کنی تا مشکل را دستی حل کنی. همیشه:
> 1. یک کپی از `grub.cfg` فعلی نگه دار
> 2. مطمئن شو ورودی کرنل قدیمی هنوز در منوی GRUB هست
> 3. بعد از نصب کرنل جدید، **بدون ری‌استارت**، اول `grub-mkconfig` را اجرا و منو را چک کن

## ۲.۵) initramfs / initrd — چرا لازم است؟

اینجا یکی از مفاهیمی است که خیلی‌ها سطحی می‌فهمند ولی باید عمیق درکش کنی، چون هم برای آزمون و هم برای عیب‌یابی واقعی حیاتی است.

**مسئله:** کرنل برای مونت کردن فایل‌سیستم ریشه (`/`) نیاز به درایور همان فایل‌سیستم (مثلاً ext4) **و** درایور کنترلر دیسک (مثلاً NVMe یا یک درایور RAID نرم‌افزاری) دارد. اما اگر این درایورها را به‌صورت ماژول (`m`) کامپایل کرده باشی (که برای انعطاف‌پذیری معمول است)، این ماژول‌ها **خودشان روی همان فایل‌سیستم ریشه‌ای قرار دارند که هنوز مونت نشده!** یک مسئله‌ی «مرغ و تخم‌مرغ» کلاسیک: برای مونت کردن ریشه به ماژول نیاز داری، اما ماژول روی ریشه‌ی مونت‌نشده است.

**راه‌حل:** یک فایل‌سیستم موقت و کوچک به نام **initramfs** (initial RAM filesystem) یا نسخه‌ی قدیمی‌ترش **initrd** (initial RAM disk) ساخته می‌شود. این یک آرشیو فشرده است که کرنل مستقیماً آن را در RAM بارگذاری می‌کند (بدون نیاز به مونت هیچ دیسکی)، حاوی حداقل ابزار و ماژول‌های لازم (مثل درایور NVMe، ابزار `lvm`، `cryptsetup` اگر ریشه رمزنگاری‌شده باشد) برای این‌که بتواند فایل‌سیستم ریشه‌ی واقعی را پیدا و مونت کند، و سپس کنترل را به آن تحویل دهد (فرآیندی به نام `switch_root`).

روی Arch/Omarchy، ابزار ساخت initramfs به نام **mkinitcpio** است:
```bash
sudo mkinitcpio -p linux-custom
```
پرچم `-p` یعنی از یک preset مشخص (که در `/etc/mkinitcpio.d/` تعریف شده) استفاده کن. تنظیمات اصلی (این‌که چه ماژول‌ها و hook هایی داخل initramfs باشند) در `/etc/mkinitcpio.conf` است، مثل:
```conf
MODULES=(nvme)
HOOKS=(base udev autodetect modconf block filesystems keyboard fsck)
```

در توزیع‌های دیگر معادل‌های این ابزار عبارتند از:
- **mkinitrd** — روش سنتی‌تر، روی برخی توزیع‌های Red Hat-family قدیمی
- **mkinitramfs** — روی Debian/Ubuntu-family (که خودش از ابزار `initramfs-tools` استفاده می‌کند)
- **dracut** — ابزار مدرن‌تر و ماژولار که امروز در Fedora/RHEL و بسیاری توزیع‌های دیگر استاندارد شده

> ⚠️ **هشدار جدی:** اگر بعد از نصب یک ماژول جدید (مثلاً درایور یک RAID سخت‌افزاری که ریشه رویش است) initramfs را بازسازی نکنی، سیستم در بوت بعدی با خطای «Unable to mount root filesystem» یا مشابه آن روبرو می‌شود، چون initramfs قدیمی از وجود آن درایور جدید خبر ندارد.

## ۲.۶) DKMS — وقتی درایور خارج از درخت کرنل است

بعضی درایورها (معروف‌ترین مثال: درایورهای اختصاصی انویدیا، یا برخی درایورهای کارت شبکه‌ی خاص) در سورس رسمی کرنل لینوکس نیستند — یا به‌خاطر لایسنس (انویدیا closed-source است) یا چون هنوز به بالادست (upstream) مرج نشده‌اند. این‌ها را «out-of-tree modules» می‌نامند.

مشکل: هر بار کرنل را ارتقا می‌دهی (که در توزیع‌های rolling-release مثل Arch مرتب اتفاق می‌افتد)، این ماژول‌های خارجی باید **دوباره کامپایل شوند** چون به ساختار داخلی نسخه‌ی خاصی از کرنل وابسته‌اند. اگر این کار دستی باشد، فراموش کردنش یعنی بعد از هر آپدیت کرنل، مثلاً کارت گرافیک از کار می‌افتد.

**DKMS** (Dynamic Kernel Module Support) این مشکل را خودکار می‌کند: وقتی کرنل جدید نصب می‌شود، یک hook خودکار اجرا می‌شود که همه‌ی ماژول‌های ثبت‌شده در DKMS را برای کرنل جدید **خودش دوباره کامپایل** می‌کند — بدون دخالت دستی تو.

```bash
sudo pacman -S dkms
dkms status                 # لیست ماژول‌های تحت مدیریت DKMS و وضعیت‌شان
```

## ۲.۷) نکات مهم آزمون برای 201.2

- ✅ ترتیب دقیق مراحل را حفظ کن: پیکربندی (`menuconfig`/`oldconfig`/...) → `make` → `make modules_install` → `make install` → بازسازی initramfs (اگر لازم) → به‌روزرسانی GRUB.
- ✅ تفاوت `make clean` (نگه‌داشتن `.config`) و `make mrproper` (حذف کامل، شامل `.config`) را دقیق بدان — سؤال بسیار رایج.
- ✅ `make oldconfig` مخصوص ارتقای نسخه با نگه‌داشتن تنظیمات قبلی است.
- ✅ `make localmodconfig` تنظیمات را بر اساس ماژول‌های **در حال اجرای فعلی** (`lsmod`) می‌سازد.
- ✅ initramfs را از نظر مفهومی کامل بفهم: چرا لازم است (مسئله‌ی مرغ و تخم‌مرغ برای مونت ریشه) و کی باید بازسازی شود (بعد از تغییر ماژول‌های مرتبط با بوت).
- ✅ ابزارهای initramfs بر اساس توزیع: `mkinitcpio` (Arch)، `mkinitramfs`/`update-initramfs` (Debian)، `dracut` (Red Hat/Fedora مدرن).
- ✅ DKMS را در حد «چرا لازم است» بشناس، نه پیکربندی عمیق.

---

# بخش سوم — 201.3: مدیریت و عیب‌یابی کرنل در زمان اجرا (Runtime Management & Troubleshooting)

## ۳.۱) چرا این بخش سنگین‌ترین وزن (۴) را در Topic 201 دارد؟

چون این دقیقاً کاری‌ست که یک ادمین **هر روز** انجام می‌دهد — نه کامپایل کرنل (که نادر است)، بلکه مدیریت ماژول‌ها، خواندن لاگ‌های کرنل، و تنظیم پارامترهای runtime. این بخش عملاً «زبان روزمره‌ی گفتگو با کرنل» است.

## ۳.۲) ماژول‌های کرنل: تفاوت عمیق `insmod` و `modprobe`

بیا این تفاوت را دقیق و کامل توضیح بدهم چون سؤال بسیار کلاسیک آزمون است.

### `insmod` — بارگذاری خام و مستقیم

```bash
sudo insmod /lib/modules/$(uname -r)/kernel/drivers/net/dummy.ko
```
`insmod` یک فایل `.ko` مشخص را **دقیقاً همان‌طور که هست** به کرنل تزریق می‌کند. **هیچ** بررسی وابستگی انجام نمی‌دهد — اگر ماژولی که می‌خواهی بارگذاری کنی به ماژول دیگری وابسته باشد که هنوز بارگذاری نشده، `insmod` با خطا شکست می‌خورد و خودت باید دستی وابستگی را پیدا و اول آن را بارگذاری کنی.

### `modprobe` — بارگذاری هوشمند با حل وابستگی

```bash
sudo modprobe dummy
```
توجه کن: اینجا فقط **نام** ماژول را دادیم، نه مسیر کامل فایل! `modprobe` این کار را می‌کند:
1. در یک فایل نگاشت به نام `modules.dep` (که توسط `depmod` ساخته می‌شود) جست‌وجو می‌کند تا مسیر دقیق فایل `.ko` و **همه‌ی وابستگی‌هایش** را پیدا کند
2. تمام وابستگی‌ها را به ترتیب صحیح، **خودکار** بارگذاری می‌کند
3. در نهایت خود ماژول درخواستی را بارگذاری می‌کند

**به همین دلیل، در عمل تقریباً همیشه از `modprobe` استفاده می‌شود، نه `insmod`.** `insmod` بیشتر برای تست دستی یک فایل `.ko` خاص (مثلاً وقتی خودت تازه یک ماژول را کامپایل کرده‌ای و هنوز در سیستم نگاشت وابستگی ندارد) کاربرد دارد.

### `depmod` — ساخت نقشه‌ی وابستگی

```bash
sudo depmod -a
```
پرچم `-a` یعنی برای **تمام** ماژول‌های موجود در `/lib/modules/<version>/` نقشه‌ی وابستگی را بازسازی کن. این کار معمولاً خودکار بعد از نصب یک ماژول جدید (مثلاً بعد از `make modules_install`) اجرا می‌شود، اما اگر دستی یک فایل `.ko` را جابه‌جا کرده باشی، باید خودت `depmod -a` را دستی بزنی وگرنه `modprobe` نمی‌تواند آن را پیدا کند.

### حذف ماژول: `rmmod` در برابر `modprobe -r`

```bash
sudo rmmod dummy
# یا معادل هوشمندتر:
sudo modprobe -r dummy
```
هر دو ماژول را حذف می‌کنند، اما `modprobe -r` علاوه‌بر خود ماژول، **وابستگی‌هایی که دیگر توسط چیز دیگری استفاده نمی‌شوند** را هم پاک‌سازی می‌کند — مشابه تفاوت `apt remove` و `apt autoremove`.

> ⚠️ نکته‌ی مهم: اگر ماژولی در حال استفاده باشد (مثلاً یک درایور فایل‌سیستم که یک پارتیشن با آن مونت شده)، `rmmod`/`modprobe -r` با خطای «module in use» شکست می‌خورد — این یک محافظت امنیتی/پایداری است، نه باگ.

### `lsmod` — چه چیزی الان بارگذاری شده؟

```bash
lsmod
```
خروجی سه ستون دارد: نام ماژول، حجم آن در حافظه، و تعداد+نام ماژول‌های دیگری که به آن وابسته‌اند. جالب است بدانی `lsmod` در واقع فقط یک نمایش خوانا از فایل `/proc/modules` است — یعنی اطلاعاتش مستقیم از کرنل، در لحظه، خوانده می‌شود.

### `modinfo` — همه‌چیز درباره‌ی یک ماژول، بدون بارگذاری آن

```bash
modinfo e1000e
```
این دستور بدون این‌که ماژول را بارگذاری کند، اطلاعات کامل آن را نشان می‌دهد: نویسنده، توضیح، لایسنس، وابستگی‌ها (`depends`)، نام‌های مستعار (`alias` — برای تشخیص خودکار سخت‌افزار)، و **پارامترهای قابل‌تنظیم** آن ماژول (`parm`) که می‌توانی هنگام بارگذاری مقداردهی کنی، مثلاً:
```bash
sudo modprobe e1000e InterruptThrottleRate=3000
```

## ۳.۳) `/proc` و `/sys` — پنجره‌های زنده به دنیای کرنل

این دو فایل‌سیستم مجازی، شاید مهم‌ترین ابزار برای فهم عمیق رفتار کرنل در لحظه باشند، و اغلب به‌اندازه‌ی کافی در دوره‌های آموزشی توضیح داده نمی‌شوند.

### `/proc` — فایل‌سیستم پردازه‌ها و اطلاعات کرنل

`/proc` یک فایل‌سیستم **مجازی** است — یعنی هیچ فایلی در آن واقعاً روی دیسک ذخیره نشده؛ محتوای هر فایل، در لحظه‌ی خواندنش، توسط کرنل **تولید** می‌شود. مثال‌ها:

```bash
cat /proc/cpuinfo      # اطلاعات کامل پردازنده(ها)
cat /proc/meminfo       # وضعیت لحظه‌ای حافظه
cat /proc/version       # نسخه‌ی کرنل و کامپایلری که با آن ساخته شده
cat /proc/modules       # همان چیزی که lsmod نشان می‌دهد
cat /proc/interrupts    # شمارنده‌ی وقفه‌های سخت‌افزاری هر هسته
ls /proc/1234/          # اطلاعات پردازه‌ای با PID مشخص (فایل‌های cmdline, status, fd/, ...)
```

زیرشاخه‌ی بسیار مهم `/proc/sys/` قابل **نوشتن** هم هست — یعنی می‌توانی مستقیم رفتار کرنل را در لحظه تغییر دهی:
```bash
cat /proc/sys/kernel/hostname
echo 1 > /proc/sys/net/ipv4/ip_forward
```
اما نوشتن مستقیم در این فایل‌ها روش خام و کمتر رایج است؛ روش استاندارد و توصیه‌شده استفاده از دستور `sysctl` است (که در واقع پشت صحنه دقیقاً همین فایل‌ها را می‌خواند/می‌نویسد، اما رابط تمیزتر و قابل‌مدیریت‌تری ارائه می‌دهد).

### `sysctl` — رابط استاندارد برای پارامترهای کرنل

```bash
sysctl -a | less                      # نمایش تمام پارامترهای قابل‌تنظیم (صدها خط)
sysctl net.ipv4.ip_forward             # نمایش یک پارامتر خاص
sudo sysctl -w net.ipv4.ip_forward=1   # تغییر موقت (فقط تا ری‌بوت بعدی)
```
نکته‌ی کلیدی: تغییرات با `sysctl -w` یا نوشتن مستقیم در `/proc/sys/` **موقت** هستند و با ری‌استارت از بین می‌روند. برای تغییر **دائمی**، باید در فایل‌های تنظیمات ذخیره کنی:
```bash
# /etc/sysctl.conf  (فایل اصلی، قدیمی‌تر)
# یا فایل‌های جداگانه در /etc/sysctl.d/*.conf (روش مدرن‌تر و توصیه‌شده)
net.ipv4.ip_forward = 1
```
بعد از ویرایش فایل، برای اعمال بدون نیاز به ری‌استارت:
```bash
sudo sysctl -p /etc/sysctl.d/99-custom.conf
```

### `/sys` — مدل دستگاه یکپارچه (Unified Device Model)

اگر `/proc` بیشتر درباره‌ی «پردازه‌ها و اطلاعات کلی کرنل» است، `/sys` (که با فایل‌سیستم `sysfs` مونت شده) دقیقاً درباره‌ی **دستگاه‌های سخت‌افزاری و ساختار درختی آن‌ها** است. این فایل‌سیستم مستقیماً بازتابی از **مدل دستگاه داخلی کرنل** (kobject) است.

```bash
ls /sys/class/net/          # لیست همه‌ی اینترفیس‌های شبکه شناسایی‌شده
cat /sys/class/net/eth0/address     # آدرس MAC
cat /sys/class/power_supply/BAT0/capacity   # درصد باتری (روی لپ‌تاپ)
echo 128 > /sys/class/leds/some_led/brightness  # کنترل مستقیم روشنایی یک LED
```
این چیزی است که بسیاری از ابزارهای گرافیکی (مثل نمایشگر درصد باتری) پشت صحنه می‌خوانند — هیچ جادویی نیست، فقط خواندن یک فایل متنی ساده.

## ۳.۴) `dmesg` و لاگ حلقه‌ای کرنل (Kernel Ring Buffer)

هر پیامی که کرنل تولید می‌کند (از لحظه‌ی بوت — حتی قبل از این‌که هیچ سرویس لاگ‌گیری کاربری بالا بیاید — تا لحظه‌ی حال) در یک بافر حافظه‌ای دایره‌ای به نام **kernel ring buffer** ذخیره می‌شود. `dmesg` این بافر را می‌خواند و نشان می‌دهد:

```bash
dmesg | tail -50
dmesg -T                     # نمایش با timestamp خوانا (به‌جای ثانیه از بوت)
dmesg --level=err,warn        # فقط پیام‌های خطا/هشدار
dmesg -w                      # نمایش زنده، مثل tail -f
sudo dmesg -C                 # پاک کردن بافر (نیاز به دسترسی root)
```

**چرا این «حلقه‌ای» است؟** چون بافر یک اندازه‌ی ثابت دارد؛ وقتی پر شود، قدیمی‌ترین پیام‌ها دور ریخته می‌شوند تا جای پیام‌های جدید باز شود. برای همین اگر سیستم مدت زیادی روشن بوده، پیام‌های لحظه‌ی بوت ممکن است دیگر در `dmesg` نباشند (اما اگر `journald` فعال باشد، می‌توانی با `journalctl -k -b` همان پیام‌های بوت را از لاگ دائمی‌تر بخوانی).

مثال عملی عیب‌یابی: وقتی یک فلش USB وصل می‌کنی و کار نمی‌کند، اولین قدم همیشه این است:
```bash
dmesg -w
# حالا فلش را وصل کن و ببین کرنل چه می‌گوید
```

## ۳.۵) شناسایی سخت‌افزار: `lspci`، `lsusb`

```bash
lspci                    # لیست تمام دستگاه‌های متصل به گذرگاه PCI/PCIe
sudo lspci -k             # همراه با نام ماژول کرنلی که هر دستگاه را مدیریت می‌کند (kernel driver in use)
lspci -vvv                # جزئیات کامل (verbose بسیار زیاد)
lsusb                     # لیست دستگاه‌های USB
lsusb -t                  # نمایش درختی (کدام دستگاه به کدام هاب وصل است)
```
`lspci -k` یکی از مفیدترین دستورات عیب‌یابی است، چون دقیقاً نشان می‌دهد آیا کرنل یک درایور مناسب برای یک تکه سخت‌افزار پیدا کرده یا نه — اگر ستون «Kernel driver in use» خالی باشد، یعنی آن دستگاه بدون درایور مانده.

## ۳.۶) udev — مدیریت پویا و خودکار دستگاه‌ها

قدیم‌ترها (قبل از udev)، فایل‌های دستگاه در `/dev/` به‌صورت **ایستا** و از پیش‌ساخته وجود داشتند — یعنی حتی اگر هزار دستگاه ممکن هرگز به سیستم وصل نمی‌شد، فایل مربوط به آن از قبل در `/dev/` بود. این هم شلوغ بود و هم مشکل امنیتی داشت (وجود فایل دستگاهی برای سخت‌افزاری که اصلاً وجود ندارد).

**udev** این را حل کرد: فایل‌های `/dev/` را **پویا** می‌سازد — دقیقاً وقتی یک دستگاه فیزیکاً وصل می‌شود (Hotplug) یا کرنل آن را شناسایی می‌کند، کرنل یک رویداد (`uevent`) به فضای کاربر می‌فرستد، و `udev` (که امروز بخشی از `systemd` است، به نام `systemd-udevd`) این رویداد را می‌گیرد، بر اساس **قوانین** (rules) تصمیم می‌گیرد چه فایلی در `/dev/` بسازد، با چه نام و مجوزی.

```bash
sudo udevadm monitor              # مشاهده‌ی زنده‌ی رویدادهای udev (وصل/جدا کردن دستگاه)
udevadm info -a -n /dev/sda        # تمام ویژگی‌های یک دستگاه، به‌همراه زنجیره‌ی کامل والدینش در sysfs
sudo udevadm test /sys/class/net/eth0   # شبیه‌سازی رویداد برای یک دستگاه خاص، بدون نیاز به وصل/جدا کردن واقعی
sudo udevadm control --reload      # بارگذاری مجدد قوانین بعد از ویرایش
```

### نوشتن یک قانون udev سفارشی

قوانین در `/etc/udev/rules.d/*.rules` نوشته می‌شوند (قوانین پیش‌فرض توزیع در `/usr/lib/udev/rules.d/` هستند و **نباید مستقیم ویرایش شوند** — دقیقاً همان اصل override که در systemd هم دیدیم). مثال یک قانون که یک نام ثابت به یک فلش USB خاص می‌دهد (بر اساس سریال‌نامبر آن):

```
# /etc/udev/rules.d/99-my-usb.rules
SUBSYSTEM=="block", ATTRS{serial}=="AB12CD34", SYMLINK+="my_backup_drive"
```
این یعنی: هر وقت دستگاهی با این سریال‌نامبر وصل شد، علاوه‌بر نام معمولی‌اش (مثلاً `/dev/sdb1`)، یک symlink به نام `/dev/my_backup_drive` هم برایش بساز — کاربرد عملی: اسکریپت‌های backup که می‌خواهند مطمئن شوند همیشه به **همان** فلش وصل می‌شوند، نه هر فلشی که تصادفاً `/dev/sdb` شده.

## ۳.۷) نکات مهم آزمون برای 201.3

- ✅ تفاوت `insmod` (بدون حل وابستگی، نیاز به مسیر کامل فایل) و `modprobe` (حل خودکار وابستگی، فقط نام ماژول) رایج‌ترین سؤال این بخش است.
- ✅ `depmod -a` باید بعد از هر تغییر دستی در فایل‌های ماژول اجرا شود تا `modprobe` بتواند آن‌ها را پیدا کند.
- ✅ فرق مفهومی `/proc` (پردازه‌ها + اطلاعات کلی کرنل) و `/sys` (مدل دستگاه‌های سخت‌افزاری) را دقیق بدان.
- ✅ تغییرات `sysctl -w` موقتی هستند؛ برای دائمی کردن باید در `/etc/sysctl.conf` یا `/etc/sysctl.d/*.conf` نوشته شوند.
- ✅ `dmesg` بافر **حلقه‌ای** است — پیام‌های قدیمی با پر شدن بافر از بین می‌روند.
- ✅ `lspci -k` ستون «Kernel driver in use» را نشان می‌دهد — کلید تشخیص سخت‌افزار بدون درایور.
- ✅ قوانین udev سفارشی همیشه در `/etc/udev/rules.d/`، نه در مسیر پیش‌فرض `/usr/lib/udev/rules.d/`.

---

## خلاصه‌ی جدولی نهایی کل Topic 201

| زیرمبحث | مفهوم کلیدی | ابزار/فایل اصلی |
|---|---|---|
| 201.1 | Kernel Space vs User Space | Ring 0 / Ring 3, System Calls |
| 201.1 | معماری مونولیتیک-ماژولار | LKM |
| 201.1 | شماره‌گذاری نسخه | `uname -r` (بدون قانون زوج/فرد بعد از v3.0) |
| 201.1 | مستندات رسمی | `Documentation/`, `MAINTAINERS` |
| 201.2 | پیکربندی | `menuconfig`, `oldconfig`, `mrproper` vs `clean` |
| 201.2 | کامپایل و نصب | `make -j`, `modules_install`, `install` |
| 201.2 | initramfs | `mkinitcpio`/`dracut`/`mkinitramfs` |
| 201.2 | ماژول‌های خارجی | DKMS |
| 201.3 | بارگذاری ماژول | `insmod` vs `modprobe`, `depmod -a` |
| 201.3 | اطلاعات لحظه‌ای | `/proc`, `/sys`, `sysctl` |
| 201.3 | لاگ کرنل | `dmesg` (ring buffer) |
| 201.3 | شناسایی سخت‌افزار | `lspci -k`, `lsusb` |
| 201.3 | مدیریت پویای دستگاه | `udevadm`, `/etc/udev/rules.d/` |

## مایندمپ متنی کامل

```
Topic 201: Linux Kernel
│
├── 201.1 Kernel Components
│   ├── User Space (Ring 3) ↔ Kernel Space (Ring 0)
│   │   └── System Calls به‌عنوان تنها دروازه‌ی ارتباط
│   ├── معماری: Monolithic + Loadable Modules
│   ├── Versioning: VERSION.PATCHLEVEL.SUBLEVEL-EXTRAVERSION
│   │   └── بعد از v3.0: قانون زوج/فرد حذف شد
│   ├── مستندات: Documentation/, README, MAINTAINERS, Kconfig
│   └── تصاویر کرنل: zImage (قدیمی) vs bzImage (استاندارد)
│
├── 201.2 Compiling a Kernel
│   ├── چرا کامپایل سفارشی: امنیت، بهینه‌سازی، پچ فوری، embedded
│   ├── پیکربندی (.config)
│   │   ├── y / m / n
│   │   └── config, menuconfig, xconfig, gconfig, oldconfig,
│   │       defconfig, localmodconfig, clean, mrproper
│   ├── کامپایل: make -j$(nproc)
│   ├── نصب: modules_install → install
│   ├── initramfs/initrd (مسئله‌ی مرغ‌وتخم‌مرغ مونت ریشه)
│   │   └── mkinitcpio / dracut / mkinitramfs
│   └── DKMS برای ماژول‌های خارج از درخت (نویدیا و مشابه)
│
└── 201.3 Runtime Management & Troubleshooting
    ├── insmod (خام) vs modprobe (هوشمند) vs rmmod/-r
    ├── depmod -a (بازسازی نگاشت وابستگی)
    ├── lsmod / modinfo
    ├── /proc (پردازه‌ها + کرنل) vs /sys (مدل دستگاه)
    ├── sysctl (موقت vs دائمی در /etc/sysctl.d/)
    ├── dmesg (ring buffer)
    ├── lspci -k / lsusb (شناسایی سخت‌افزار + درایور)
    └── udev: udevadm, قوانین در /etc/udev/rules.d/
```
