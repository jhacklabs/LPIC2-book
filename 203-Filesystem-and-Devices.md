i---
title: "Topic 203 — Filesystem and Devices (نسخه عمیق و کامل)"
exam: LPIC-2 / 201-450
weights: "203.1 (4) + 203.2 (3) + 203.3 (2)"
os_target: "Arch Linux / Omarchy"
tags: [lpic2, filesystem, fstab, btrfs, xfs, luks, deep-dive]
---

# Topic 203: Filesystem and Devices — راهنمای کامل و عمیق

---

# بخش اول — 203.1: عملیات فایل‌سیستم

## ۱.۱) از دستگاه فیزیکی تا فایلی که می‌بینی — چند لایه‌ی واسط

قبل از هر دستوری، باید زنجیره‌ی لایه‌هایی که بین یک فایل و سکتورهای فیزیکی دیسک قرار دارند را بفهمی:

```
فایل که تو می‌بینی (/home/user/photo.jpg)
        │
فایل‌سیستم (ext4/btrfs/xfs) ← ساختار داده‌ای که مشخص می‌کند کدام بلاک‌ها به کدام فایل تعلق دارند
        │
Block Device (/dev/sda1) ← یک رشته از بلاک‌های خام قابل‌آدرس‌دهی
        │
درایور کنترلر دیسک (NVMe/AHCI/...) ← ترجمه‌ی درخواست بلاک به دستورهای سخت‌افزاری واقعی
        │
سکتورهای فیزیکی روی دیسک
```

وقتی می‌گویی `mount /dev/sdb1 /mnt/data`، در واقع به کرنل می‌گویی: «این فایل‌سیستمی که روی این دستگاه بلاکی نوشته شده را، به این نقطه از درخت دایرکتوری موجود متصل کن.» این دقیقاً همان چیزی است که کلمه‌ی «mount» (سوار کردن) به آن اشاره دارد — یک فایل‌سیستم جدا را به درخت دایرکتوری اصلی «سوار» می‌کنی.

## ۱.۲) چرا از UUID استفاده کنیم، نه `/dev/sdX`؟

نام‌های `/dev/sda`, `/dev/sdb` و غیره بر اساس **ترتیب شناسایی** دستگاه‌ها توسط کرنل در زمان بوت تعیین می‌شوند — و این ترتیب **تضمین‌شده نیست**. اگر یک فلش USB جدید وصل کنی، یک دیسک NVMe اضافه کنی، یا حتی گاهی صرفاً به‌خاطر تفاوت‌های زمان‌بندی در تشخیص سخت‌افزار هنگام بوت، ممکن است چیزی که همیشه `/dev/sdb` بوده، دفعه‌ی بعد `/dev/sdc` شود. اگر `fstab` تو بر اساس نام دستگاه نوشته شده باشد، این یعنی سیستم ممکن است اشتباهی پارتیشن غلط را جای دیگری mount کند یا اصلاً بوت نشود.

**UUID** (Universally Unique Identifier) یک شناسه‌ی ۱۲۸ بیتی است که هنگام ساخت فایل‌سیستم (`mkfs`) به‌صورت تصادفی تولید و **در خود فایل‌سیستم ذخیره** می‌شود — یعنی مستقل از این‌که دستگاه با چه نامی شناسایی شود، UUID همیشه ثابت می‌ماند.

```bash
sudo blkid
```
خروجی چیزی شبیه این می‌دهد:
```
/dev/sda1: UUID="a1b2c3d4-e5f6-7890-abcd-ef1234567890" TYPE="ext4"
/dev/sda2: UUID="1234-5678" TYPE="vfat"
```
و در `/etc/fstab`:
```
UUID=a1b2c3d4-e5f6-7890-abcd-ef1234567890  /       ext4  defaults  0  1
UUID=1234-5678                              /boot   vfat  defaults  0  2
```

### فایل fstab — عمیق‌تر روی هر ستون

هر خط fstab شش فیلد دارد که با فاصله/تب از هم جدا می‌شوند:
```
<device>  <mount-point>  <fs-type>  <options>  <dump>  <pass>
```
- **device** — می‌تواند `/dev/sdX`، `UUID=...`، یا `LABEL=...` باشد
- **mount-point** — کجای درخت دایرکتوری mount شود (برای swap مقدار `none` است)
- **fs-type** — نوع فایل‌سیستم (`ext4`, `btrfs`, `xfs`, `vfat`, `swap`)
- **options** — گزینه‌های mount، جدا شده با کاما (مثل `defaults`, `noatime`, `ro`, `user`)
- **dump** — عدد ۰ یا ۱، مربوط به ابزار قدیمی `dump` برای بکاپ‌گیری (امروز عملاً همیشه ۰ است چون کسی از `dump` استفاده نمی‌کند)
- **pass** — ترتیب چک `fsck` در بوت: `0` یعنی هرگز چک نشود، `1` برای فایل‌سیستم ریشه (همیشه اول)، `2` برای بقیه (به ترتیب موازی چک می‌شوند)

> ⚠️ نکته‌ی مهم: اگر `pass` را برای دو فایل‌سیستم مختلف روی **یک دیسک فیزیکی** هر دو `2` بگذاری، ممکن است سیستم سعی کند هم‌زمان دو `fsck` روی یک دیسک فیزیکی اجرا کند که کارایی را به‌شدت پایین می‌آورد (چون هد دیسک باید بین دو ناحیه پرش کند) — بهتر است ترتیب متفاوت بدهی یا از مقدار `0` روی پارتیشن‌های غیرحیاتی استفاده کنی.

## ۱.۳) عملیات mount/umount — جزئیاتی که خیلی‌ها نمی‌دانند

```bash
sudo mount /dev/sdb1 /mnt/data
```
اگر `/etc/fstab` از قبل یک خط برای `/dev/sdb1` داشته باشد، می‌توانی خلاصه‌تر بنویسی:
```bash
sudo mount /mnt/data     # فقط نقطه‌ی mount؛ بقیه از fstab خوانده می‌شود
sudo mount /dev/sdb1     # یا فقط نام دستگاه
```

گزینه‌های رایج mount که باید عمیق بشناسی:
- `ro` — فقط‌خواندنی، هیچ نوشتنی مجاز نیست
- `rw` — خواندن و نوشتن (پیش‌فرض)
- `noexec` — جلوگیری از اجرای فایل‌های اجرایی روی این mount (یک اقدام امنیتی رایج برای `/tmp` یا مسیرهای آپلود کاربر — جلوی اجرای بدافزار آپلودشده را می‌گیرد)
- `nosuid` — نادیده گرفتن بیت‌های SUID/SGID روی این فایل‌سیستم (اقدام امنیتی دیگر، جلوگیری از افزایش سطح دسترسی از طریق باینری‌های SUID روی رسانه‌ی خارجی)
- `nodev` — فایل‌های دستگاه (device files) روی این mount نادیده گرفته می‌شوند
- `noatime` — به‌روزرسانی زمان آخرین دسترسی (access time) هر فایل غیرفعال می‌شود؛ این می‌تواند کارایی را قابل‌توجه بهبود دهد چون هر بار **خواندن** یک فایل بدون این گزینه یعنی یک **نوشتن** اضافه (برای ثبت زمان دسترسی) روی دیسک

```bash
sudo umount /mnt/data
```
اگر با خطای «device is busy» روبرو شدی، یعنی یک پردازه هنوز در آن مسیر باز است. برای پیدا کردنش:
```bash
sudo lsof +D /mnt/data      # چه پردازه‌هایی فایلی در این مسیر باز دارند
sudo fuser -m /mnt/data      # مشابه، خروجی خلاصه‌تر با PID ها
sudo umount -l /mnt/data     # "lazy unmount": mount را فوراً از دید سیستم مخفی می‌کند، اما واقعاً وقتی همه‌ی استفاده‌ها تمام شد unmount نهایی انجام می‌شود
```

## ۱.۴) swap — چرا و چطور

Swap فضایی روی دیسک است که کرنل می‌تواند صفحات حافظه‌ای که مدتی استفاده نشده‌اند را به آنجا منتقل کند تا RAM فیزیکی برای داده‌های فعال‌تر آزاد شود. این هم به‌عنوان «سوپاپ اطمینان» وقتی RAM کم می‌آید عمل می‌کند، و هم اجازه می‌دهد سیستم **hibernate** شود (کل محتوای RAM روی دیسک/swap نوشته شود و کامپیوتر کاملاً خاموش شود، بعد در روشن شدن بعدی همان حالت بازیابی شود).

```bash
sudo mkswap /dev/sdb2          # ساخت فضای swap روی یک پارتیشن
sudo swapon /dev/sdb2           # فعال‌سازی
sudo swapoff /dev/sdb2          # غیرفعال‌سازی (محتوای موجود در swap به RAM منتقل می‌شود اگر جا باشد)
swapon --show                    # نمایش تمام فضاهای swap فعال و اولویتشان
cat /proc/swaps                  # همان اطلاعات، خام‌تر
```
می‌توان به‌جای پارتیشن، از یک **فایل معمولی** به‌عنوان swap استفاده کرد (رایج در سرورهای مجازی یا وقتی نمی‌خواهی پارتیشن‌بندی را دستکاری کنی):
```bash
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

## ۱.۵) systemd و mount — نگاهی به آینده (و حال)

systemd می‌تواند خودش خط‌های fstab را به واحدهای `.mount` تبدیل کند (این خودکار انجام می‌شود، نیازی به کار دستی نیست)، اما می‌توانی مستقیم هم واحد mount بسازی:
```bash
systemctl list-units --type=mount
```
نام فایل واحد mount باید دقیقاً بر اساس مسیر مقصد باشد (با escape کردن `/` به `-`)، مثلاً `/mnt/data` می‌شود `mnt-data.mount`.

## ۱.۶) نکات مهم آزمون برای 203.1

- ✅ UUID را همیشه بر اساس `blkid` استخراج کن، نه حدس بزن.
- ✅ ترتیب دقیق فیلدهای fstab را حفظ کن (device, mount-point, type, options, dump, pass).
- ✅ فرق `noexec`/`nosuid`/`nodev` را از نظر امنیتی بدان — سؤال ترکیبی رایج.
- ✅ `umount -l` (lazy) در برابر `umount` معمولی وقتی device busy است.
- ✅ رابطه‌ی swap با hibernation را بشناس.

---

# بخش دوم — 203.2: نگهداری فایل‌سیستم

## ۲.۱) خانواده‌ی ext — عمیق‌تر از فقط "فرمت کن و برو"

ext4 امروز رایج‌ترین فایل‌سیستم پیش‌فرض در بسیاری توزیع‌هاست، اما مفاهیم داخلی‌اش (که در ابزارهایش منعکس می‌شود) از نسل‌های قبلی (ext2, ext3) به ارث رسیده:

```bash
sudo mkfs.ext4 /dev/sdb1
```
داخل هر فایل‌سیستم ext، فضا به **گروه‌های بلاک (Block Groups)** تقسیم می‌شود — هر گروه شامل یک کپی (یا اشاره به) سوپربلاک، جدول inode مربوط به خودش، و بلاک‌های داده است. این طراحی باعث می‌شود inode ها و داده‌های مرتبط از نظر فیزیکی نزدیک هم روی دیسک باشند (کاهش fragmentation و بهبود کارایی خواندن‌های متوالی).

```bash
sudo tune2fs -l /dev/sdb1
```
`tune2fs -l` پارامترهای فایل‌سیستم را نشان می‌دهد: تعداد کل inode ها، تعداد inode های آزاد، آخرین زمان mount، فاصله‌ی زمانی/تعداد mount بین دو `fsck` اجباری خودکار، و مهم‌تر: **UUID** و **Label**. با همین ابزار می‌توانی این پارامترها را تغییر دهی:
```bash
sudo tune2fs -L mydata /dev/sdb1        # تنظیم Label
sudo tune2fs -c 20 /dev/sdb1             # چک اجباری بعد از هر ۲۰ بار mount
sudo tune2fs -i 0 /dev/sdb1              # غیرفعال‌سازی چک بر اساس بازه‌ی زمانی (0 = هرگز بر اساس زمان)
```

```bash
sudo dumpe2fs /dev/sdb1 | less
```
`dumpe2fs` جزئیات بسیار خام‌تری نشان می‌دهد — از جمله اطلاعات دقیق **هر گروه بلاک** به‌صورت جداگانه (چند بلاک آزاد در هر گروه، کجا سوپربلاک پشتیبان قرار دارد و غیره). این سطح جزئیات معمولاً فقط هنگام دیباگ عمیق آسیب‌دیدگی فایل‌سیستم لازم می‌شود.

```bash
sudo debugfs /dev/sdb1
```
`debugfs` یک محیط **تعاملی و بسیار خطرناک** برای بازرسی و حتی **ویرایش دستی** ساختارهای داخلی یک فایل‌سیستم ext است — می‌تواند برای بازیابی فایل‌های حذف‌شده (undelete) یا تعمیرات دستی خیلی پیشرفته استفاده شود، اما یک اشتباه در این محیط می‌تواند کل فایل‌سیستم را غیرقابل‌بازیابی کند.

> ⚠️ **هشدار جدی:** `debugfs` را فقط وقتی استفاده کن که کاملاً می‌دانی چه می‌کنی، و همیشه اول با پرچم `-c` (فقط بازرسی، بدون امکان نوشتن تصادفی) شروع کن.

### fsck برای ext — بازسازی سوپربلاک خراب

اگر سوپربلاک اصلی (که در ابتدای فایل‌سیستم قرار دارد و اطلاعات حیاتی مثل نوع/اندازه‌ی فایل‌سیستم را نگه می‌دارد) خراب شود، فایل‌سیستم اصلاً قابل‌شناسایی نخواهد بود. خوشبختانه ext چند **کپی پشتیبان** از سوپربلاک در گروه‌های بلاک مختلف نگه می‌دارد:
```bash
sudo mke2fs -n /dev/sdb1          # نمایش (بدون فرمت واقعی!) مکان تمام سوپربلاک‌های پشتیبان
sudo fsck.ext4 -b 32768 /dev/sdb1  # تلاش برای تعمیر با استفاده از یک سوپربلاک پشتیبان مشخص
```

## ۲.۲) Btrfs — فلسفه‌ی متفاوت: Copy-on-Write

Btrfs (اغلب «باتر اف‌اس» تلفظ می‌شود) یک فایل‌سیستم مدرن با فلسفه‌ی کاملاً متفاوت از ext است: **Copy-on-Write** (CoW). یعنی وقتی یک فایل را تغییر می‌دهی، Btrfs بلاک‌های تغییریافته را **در جای جدید** می‌نویسد (نه جای‌گذاری روی بلاک‌های قدیمی)، و فقط بعد از این‌که نوشتن کامل و موفق بود، اشاره‌گرهای متادیتا را به‌روزرسانی می‌کند تا به بلاک‌های جدید اشاره کنند. این طراحی دو مزیت بزرگ می‌دهد:

1. **مقاومت در برابر خرابی** — چون هرگز داده‌ی قدیمی مستقیم overwrite نمی‌شود، اگر برق در وسط نوشتن قطع شود، فایل‌سیستم همیشه یا در حالت قدیمی سالم یا در حالت جدید سالم است — هرگز در یک حالت نیمه‌نوشته‌ی خراب گیر نمی‌افتد.
2. **Snapshot ارزان و فوری** — چون داده‌ها overwrite نمی‌شوند، گرفتن یک snapshot از کل فایل‌سیستم فقط یعنی «یک اشاره‌گر جدید به وضعیت فعلی متادیتا بساز» — این عملاً آنی است و در ابتدا هیچ فضای اضافه‌ای مصرف نمی‌کند (فقط وقتی فایل‌های اصلی بعداً تغییر کنند، فضای اضافی برای نگه‌داشتن نسخه‌ی قدیمی مصرف می‌شود).

```bash
sudo mkfs.btrfs /dev/sdb1
sudo mount /dev/sdb1 /mnt
sudo btrfs subvolume create /mnt/@home
```
**subvolume** در Btrfs یک مفهوم کلیدی است: یک زیردرخت مستقل که می‌تواند جداگانه mount شود، جداگانه snapshot بگیرد، و حتی سهمیه‌ی فضای مستقل (quota) داشته باشد — شبیه یک «فایل‌سیستم داخل فایل‌سیستم» ولی بدون سربار واقعی جدا کردن پارتیشن.

```bash
sudo btrfs subvolume snapshot /mnt/@home /mnt/@home_snapshot_$(date +%Y%m%d)
```
این snapshot دقیقاً یکی از دلایلی است که Omarchy/Arch مدرن آن را جذاب می‌کند: قبل از هر آپدیت بزرگ سیستم، یک snapshot می‌گیری؛ اگر آپدیت سیستم را خراب کرد، می‌توانی فوراً به snapshot قبلی برگردی (rollback) — دقیقاً همان کاری که ابزارهایی مثل `timeshift` یا `snapper` روی Btrfs انجام می‌دهند.

```bash
sudo btrfs filesystem show          # نمایش تمام فایل‌سیستم‌های Btrfs و دیسک‌های تشکیل‌دهنده‌شان
sudo btrfs filesystem df /mnt        # استفاده‌ی فضا، تفکیک‌شده بر اساس نوع (data/metadata/system)
sudo btrfs scrub start /mnt          # اسکن کامل و تعمیر خودکار خطاهای checksum (Btrfs برای هر بلاک checksum نگه می‌دارد!)
```

## ۲.۳) XFS — طراحی‌شده برای مقیاس بزرگ

XFS از اوایل دهه‌ی ۹۰ توسط SGI برای سیستم‌های سرور بزرگ با فایل‌های عظیم (چند ترابایتی) طراحی شده و امروز پیش‌فرض بسیاری توزیع‌های Red Hat-family است. نقطه‌ی قوت اصلی‌اش کارایی بسیار بالا در فایل‌های بزرگ و I/O موازی است.

```bash
sudo mkfs.xfs /dev/sdb1
xfs_info /mnt/data                    # نمایش پارامترهای فایل‌سیستم (معادل مفهومی tune2fs -l برای ext)
```

> ⚠️ نکته‌ی مهم و تفاوت اساسی با ext: **اندازه‌ی یک فایل‌سیستم XFS را نمی‌توان کوچک کرد** (shrink) — فقط می‌توان بزرگ کرد (`xfs_growfs`). این محدودیت طراحی است، نه یک باگ. اگر نیاز به کوچک کردن داری، تنها راه: بکاپ کامل → حذف فایل‌سیستم → ساخت مجدد با اندازه‌ی کوچک‌تر → بازگرداندن بکاپ.

```bash
sudo xfs_repair /dev/sdb1
```
**نکته‌ی بسیار مهم:** بر خلاف `fsck.ext4` که می‌توان با پرچم `-f` روی فایل‌سیستمی که فکر می‌کند «تمیز» است اجبار به چک کرد، `xfs_repair` **باید روی فایل‌سیستم unmount شده** اجرا شود و اصلاً معادل مستقیم دستور `fsck` عمل نمی‌کند. در واقع XFS خودش دارای یک مکانیزم **journal** قدرتمند است که بیشتر ناسازگاری‌های ساده را در زمان mount خودکار حل می‌کند؛ `xfs_repair` فقط برای خرابی‌های جدی‌تر لازم است.

```bash
sudo xfsdump -f /backup/data.xfsdump /mnt/data     # بکاپ‌گیری در سطح فایل‌سیستم (نه فایل‌به‌فایل مثل tar، بلکه با آگاهی از ساختار XFS)
sudo xfsrestore -f /backup/data.xfsdump /mnt/restored
```

## ۲.۴) پایش سلامت فیزیکی دیسک: SMART

SMART (Self-Monitoring, Analysis, and Reporting Technology) یک سیستم داخلی در تراشه‌ی کنترلر خود دیسک (نه بخشی از کرنل لینوکس) است که آمار سلامت فیزیکی را ثبت می‌کند — تعداد بخش‌های بد (bad sectors)، دمای دیسک، تعداد ساعت کارکرد، تعداد خطای خواندن/نوشتن.

```bash
sudo pacman -S smartmontools
sudo smartctl -i /dev/sda           # اطلاعات پایه (مدل، سریال، پشتیبانی از SMART)
sudo smartctl -a /dev/sda            # گزارش کامل تمام attribute ها
sudo smartctl -H /dev/sda            # فقط نتیجه‌ی کلی PASSED/FAILED
sudo smartctl -t short /dev/sda       # اجرای یک self-test کوتاه (چند دقیقه)
sudo smartctl -t long /dev/sda        # self-test کامل (می‌تواند ساعت‌ها طول بکشد روی دیسک‌های بزرگ)
sudo smartctl -l selftest /dev/sda    # نتایج تست‌های قبلی
```
**چرا این برای Cyber Security هم مهم است؟** چون قبل از این‌که یک دیسک به‌طور کامل خراب شود (که می‌تواند یعنی از دست رفتن داده یا حتی از کار افتادن یک سرویس حیاتی)، معمولاً علائم هشداردهنده در SMART attribute هایی مثل `Reallocated_Sector_Ct` یا `Current_Pending_Sector` ظاهر می‌شود — پایش خودکار این‌ها (با `smartd`) می‌تواند هفته‌ها قبل از خرابی کامل هشدار بدهد.

```bash
sudo systemctl enable --now smartd
cat /etc/smartd.conf     # تنظیمات پایش خودکار و اعلان
```

## ۲.۵) نکات مهم آزمون برای 203.2

- ✅ Btrfs = Copy-on-Write → snapshot ارزان و فوری، مقاومت بالا در برابر خرابی برق.
- ✅ XFS **نمی‌تواند shrink شود**، فقط grow — این محدودیت طراحی کلاسیک آزمون است.
- ✅ `xfs_repair` باید روی فایل‌سیستم unmount شده اجرا شود.
- ✅ `tune2fs -l` برای ext مثل `xfs_info` برای XFS — هر دو پارامترهای فایل‌سیستم را نشان می‌دهند.
- ✅ `debugfs` بسیار قدرتمند و بسیار خطرناک است — فقط برای دیباگ عمیق.
- ✅ SMART مستقل از فایل‌سیستم، سطح سلامت فیزیکی دیسک را پایش می‌کند.

---

# بخش سوم — 203.3: گزینه‌های پیشرفته

## ۳.۱) AutoFS — mount فقط وقتی واقعاً لازم است

تصور کن یک سازمان با ده‌ها کاربر داری که هرکدام یک home directory روی یک سرور NFS مرکزی دارند. اگر بخواهی همه‌ی این‌ها را از قبل، همیشه، روی هر ورک‌استیشن mount نگه داری، هم منابع شبکه هدر می‌رود (هر mount یک اتصال فعال است) و هم اگر سرور NFS موقتی از دسترس خارج شود، تمام آن mount ها به حالت "stale" (خراب/معلق) می‌روند و حتی دستورهای ساده مثل `ls /` ممکن است هنگ کنند.

**AutoFS** این را حل می‌کند: فایل‌سیستم فقط **در لحظه‌ی اولین دسترسی** mount می‌شود، و بعد از یک بازه‌ی بی‌فعالیتی مشخص، خودکار unmount می‌شود.

```bash
sudo pacman -S autofs
```
فایل اصلی `/etc/auto.master` — نقشه‌ی کلی که مشخص می‌کند کدام مسیر پایه به کدام فایل نقشه‌ی فرعی وصل است:
```
/mnt/nfs   /etc/auto.nfs   --timeout=60
```
و در `/etc/auto.nfs`:
```
projects   -rw,soft   fileserver.local:/export/projects
```
یعنی وقتی کسی سعی کند به `/mnt/nfs/projects` دسترسی پیدا کند، AutoFS خودکار سهم `/export/projects` را از `fileserver.local` mount می‌کند؛ اگر ۶۰ ثانیه (طبق `--timeout=60`) کسی از آن استفاده نکند، خودکار unmount می‌شود.

```bash
sudo systemctl enable --now autofs
sudo systemctl reload autofs      # بعد از تغییر فایل‌های نقشه
```

## ۳.۲) رمزنگاری دیسک: LUKS/dm-crypt — عمیق

این مهم‌ترین بخش از نظر Cyber Security در کل این Topic است، پس بیایید عمیق برویم.

### چطور رمزنگاری دیسک واقعاً کار می‌کند؟

**dm-crypt** یک ماژول کرنل است که در لایه‌ی **device mapper** قرار می‌گیرد — یعنی یک دستگاه بلاکی مجازی جدید می‌سازد که هر نوشتن رویش، رمزنگاری‌شده روی دستگاه واقعی زیرینش ذخیره می‌شود، و هر خواندن، رمزگشایی‌شده به لایه‌ی بالا تحویل داده می‌شود — کاملاً شفاف برای فایل‌سیستمی که رویش نشسته (ext4/btrfs نمی‌دانند و اهمیتی نمی‌دهند که زیرشان رمزنگاری شده).

**LUKS** (Linux Unified Key Setup) یک لایه‌ی استاندارد **بالای** dm-crypt است که مدیریت کلید را حل می‌کند. مسئله‌ای که LUKS حل می‌کند: اگر می‌خواهی رمز عبورت را تغییر بدهی، بدون LUKS باید کل دیسک را دوباره با کلید جدید رمزنگاری کنی (که برای یک دیسک ترابایتی می‌تواند ساعت‌ها طول بکشد). LUKS این را با یک ترفند هوشمند حل می‌کند:

1. یک **کلید اصلی تصادفی** (Master Key) یک‌بار در زمان ساخت تولید می‌شود که واقعاً برای رمزنگاری/رمزگشایی داده استفاده می‌شود.
2. این کلید اصلی خودش با رمز عبور تو رمزنگاری می‌شود و در یکی از **۸ اسلات کلید** (Key Slots) ذخیره می‌شود.
3. وقتی رمز عبورت را عوض می‌کنی، فقط این نسخه‌ی رمزنگاری‌شده‌ی کوچک کلید اصلی دوباره رمزنگاری می‌شود — کلید اصلی و در نتیجه کل داده‌ی دیسک **دست‌نخورده** می‌ماند. این عملیات چند میلی‌ثانیه طول می‌کشد، نه ساعت‌ها.

این معماری همچنین یعنی می‌توانی **چند رمز عبور مختلف** برای یک دیسک داشته باشی (مثلاً یک رمز برای خودت، یک رمز جداگانه برای یک همکار IT که در مواقع اضطراری دسترسی دارد) — هرکدام در یک اسلات جدا:

```bash
sudo cryptsetup luksFormat /dev/sdb1          # ساخت اولیه، اسلات ۰ با رمز عبور اول
sudo cryptsetup luksAddKey /dev/sdb1           # افزودن یک رمز عبور دوم (اسلات جدید)
sudo cryptsetup luksDump /dev/sdb1              # نمایش تمام اسلات‌های فعال و متادیتای LUKS
sudo cryptsetup luksRemoveKey /dev/sdb1         # حذف یک اسلات کلید خاص (با وارد کردن آن رمز)
sudo cryptsetup luksKillSlot /dev/sdb1 2        # حذف اجباری اسلات شماره‌ی ۲ (بدون نیاز به دانستن رمزش، اگر یک اسلات دیگر معتبر داری)
```

### چرخه‌ی کامل استفاده

```bash
sudo cryptsetup luksFormat /dev/sdb1
sudo cryptsetup luksOpen /dev/sdb1 secure_data
```
بعد از `luksOpen`، یک دستگاه مجازی جدید در `/dev/mapper/secure_data` ظاهر می‌شود — این همان چیزی است که از این به بعد مثل یک دیسک معمولی با آن کار می‌کنی:
```bash
sudo mkfs.ext4 /dev/mapper/secure_data
sudo mount /dev/mapper/secure_data /mnt/secure
# ... کار با فایل‌ها ...
sudo umount /mnt/secure
sudo cryptsetup luksClose secure_data
```
بعد از `luksClose`، دیسک دوباره فقط یک بلوب کاملاً رمزنگاری‌شده و غیرقابل‌خواندن است — حتی نوع فایل‌سیستم داخلش (ext4 یا btrfs) هم از بیرون قابل‌تشخیص نیست.

> ⚠️ **هشدار بسیار جدی:** اگر رمز عبور **همه‌ی** اسلات‌های LUKS را فراموش کنی، **هیچ راهی برای بازیابی داده وجود ندارد** — نه شرکت سازنده‌ی دیسک، نه هیچ متخصصی نمی‌تواند کمکی کند، چون کلید اصلی صرفاً یک عدد تصادفی رمزنگاری‌شده است که بدون رمز عبور صحیح، از نظر ریاضی غیرقابل‌بازیابی است (فرض بر این‌که الگوریتم رمزنگاری AES مورد استفاده هنوز شکسته نشده — که در حال حاضر نشده).

### header backup — نکته‌ای که خیلی‌ها فراموش می‌کنند

متادیتای LUKS (شامل تمام اسلات‌های کلید) در یک ناحیه‌ی کوچک به نام **LUKS header** در ابتدای پارتیشن ذخیره می‌شود. اگر این header آسیب ببیند (مثلاً به‌طور تصادفی overwrite شود)، حتی با رمز عبور صحیح هم نمی‌توانی داده را باز کنی، چون کلید اصلی رمزنگاری‌شده دیگر در دسترس نیست. برای همین حرفه‌ای‌ها همیشه یک بکاپ از header می‌گیرند:
```bash
sudo cryptsetup luksHeaderBackup /dev/sdb1 --header-backup-file /safe/location/luks-header.bak
sudo cryptsetup luksHeaderRestore /dev/sdb1 --header-backup-file /safe/location/luks-header.bak
```

## ۳.۳) فایل‌سیستم‌های CD-ROM: ISO9660 و افزونه‌هایش

**ISO9660** استاندارد پایه‌ای فایل‌سیستم برای رسانه‌های نوری (CD/DVD) است، اما این استاندارد اصلی خودش محدودیت‌های شدیدی دارد (نام فایل حداکثر ۸ کاراکتر + ۳ کاراکتر پسوند، بدون حروف کوچک، بدون permission یونیکسی). برای همین سه افزونه توسعه داده شده که باید بشناسی:

- **Joliet** — افزونه‌ی مایکروسافت برای پشتیبانی از نام‌های فایل طولانی و یونیکد، تا ویندوز بتواند فایل‌ها را با نام کامل ببیند.
- **Rock Ridge** — افزونه‌ی دنیای یونیکس/لینوکس که متادیتای POSIX (مالکیت، permission، symlink) را به ISO9660 اضافه می‌کند.
- **El Torito** — افزونه‌ای که بوت‌پذیری را ممکن می‌کند؛ بدون این افزونه، یک سی‌دی فقط قابل mount کردن است، نه بوت شدن.

```bash
mkisofs -o output.iso -J -R -o output.iso /path/to/files/
```
پرچم `-J` یعنی افزونه‌ی Joliet را اضافه کن، `-R` یعنی Rock Ridge را اضافه کن — امروز تقریباً همیشه هر دو با هم استفاده می‌شوند تا ایزو هم روی ویندوز هم روی لینوکس درست کار کند.

**UDF** (Universal Disk Format) جانشین مدرن‌تر ISO9660 است — امروز استاندارد DVD/Blu-ray و همچنین فلش‌های USB با فرمت جدید است، چون محدودیت‌های ISO9660 (مثل حداکثر حجم فایل) را ندارد.

## ۳.۴) نکات مهم آزمون برای 203.3

- ✅ AutoFS: فایل اصلی `/etc/auto.master` که به فایل‌های نقشه‌ی فرعی (نام دلخواه) اشاره می‌کند.
- ✅ مکانیزم LUKS با ۸ اسلات کلید و یک کلید اصلی مجزا را عمیق بفهم — این پایه‌ی چندین سؤال است.
- ✅ ترتیب دقیق: `luksFormat` → `luksOpen` → `mkfs` → `mount` ... → `umount` → `luksClose`.
- ✅ اهمیت `luksHeaderBackup` — از دست دادن header یعنی از دست دادن داده حتی با رمز درست.
- ✅ افزونه‌های ISO9660: Joliet (نام طولانی ویندوزی)، Rock Ridge (POSIX یونیکس)، El Torito (بوت‌پذیری).

---

## خلاصه‌ی جدولی نهایی کل Topic 203

| زیرمبحث | مفهوم کلیدی | ابزار/فایل اصلی |
|---|---|---|
| 203.1 | چرا UUID | `blkid`, پایداری در برابر تغییر ترتیب `/dev/sdX` |
| 203.1 | ساختار fstab | ۶ فیلد: device, mount-point, type, options, dump, pass |
| 203.1 | گزینه‌های امنیتی mount | `noexec`, `nosuid`, `nodev` |
| 203.1 | swap | `mkswap`, `swapon/off`, ارتباط با hibernation |
| 203.2 | ext internals | Block Groups, `tune2fs`, `dumpe2fs`, `debugfs` |
| 203.2 | Btrfs = CoW | subvolume, snapshot ارزان، `scrub` |
| 203.2 | XFS | فقط grow (نه shrink)، `xfs_repair` (unmounted) |
| 203.2 | سلامت فیزیکی | `smartctl`, `smartd` |
| 203.3 | AutoFS | `/etc/auto.master`, mount بر اساس تقاضا |
| 203.3 | LUKS/dm-crypt | Master Key + ۸ Key Slot، `luksHeaderBackup` |
| 203.3 | ISO9660 افزونه‌ها | Joliet, Rock Ridge, El Torito, UDF |

## مایندمپ متنی کامل

```
Topic 203: Filesystem and Devices
│
├── 203.1 Operating
│   ├── لایه‌ها: فایل → فایل‌سیستم → Block Device → درایور → فیزیکی
│   ├── چرا UUID: پایداری نام دستگاه
│   ├── fstab: device/mountpoint/type/options/dump/pass
│   ├── گزینه‌های امنیتی: noexec/nosuid/nodev/noatime
│   └── swap: mkswap/swapon/off + hibernation
│
├── 203.2 Maintaining
│   ├── ext: Block Groups, tune2fs, dumpe2fs, debugfs
│   ├── Btrfs: Copy-on-Write → snapshot ارزان + مقاومت خرابی
│   │   └── subvolume, scrub (checksum خودکار)
│   ├── XFS: فقط grow، xfs_repair (unmounted!)
│   └── SMART: smartctl -a/-H/-t, smartd
│
└── 203.3 Advanced Options
    ├── AutoFS: auto.master → auto.* (mount on-demand)
    ├── LUKS/dm-crypt
    │   ├── Master Key + 8 Key Slots
    │   ├── luksFormat→luksOpen→mkfs→mount...umount→luksClose
    │   └── luksHeaderBackup/Restore (حیاتی!)
    └── ISO9660 + Joliet/Rock Ridge/El Torito, UDF
```
