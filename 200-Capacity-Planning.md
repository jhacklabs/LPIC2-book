---
title: "Topic 200 — Capacity Planning"
exam: LPIC-2 / 201-450
weights: "200.1 (وزن 6) + 200.2 (وزن 2)"
os_target: "Arch Linux / Omarchy"
tags: [lpic2, capacity-planning, monitoring, performance]
---

# Topic 200: Capacity Planning

> این درس هر دو زیرمبحث رسمی را پوشش می‌دهد:
> **200.1 Measure and Troubleshoot Resource Usage** (وزن 6 — سنگین‌ترین زیرمبحث کل Topic 200)
> **200.2 Predict Future Resource Needs** (وزن 2)

---

## ۱) مفهوم کلی

Capacity Planning یعنی توانایی **اندازه‌گیری دقیق مصرف منابع سخت‌افزاری و شبکه** (CPU، حافظه، دیسک، پهنای‌باند)، **تشخیص گلوگاه (bottleneck)** وقتی سیستم کند می‌شود، و **پیش‌بینی** اینکه چه زمانی منابع فعلی کم می‌آورند.

در LPIC-1 یاد گرفتی چطور یک سیستم را اداره کنی. در LPIC-2 وارد نقش «کسی که مسئول سلامت یک سرور واقعی زیر بار کاری واقعی است» می‌شوی. اینجا دیگر صرفاً دستور زدن مهم نیست؛ مهم این است که از روی خروجی چند ابزار، بفهمی **کجای سیستم دارد داد می‌زند**.

چهار منبع اصلی که باید بلد باشی اندازه بگیری:
- **CPU** — چقدر پردازنده مشغول است و چرا (user، system، iowait، steal)
- **Memory** — چقدر RAM آزاد است، چقدر cache/buffer است، آیا swap در حال استفاده شدید است
- **Disk I/O** — سرعت خواندن/نوشتن، صف انتظار دیسک
- **Network I/O** — پهنای‌باند مصرفی، بسته‌های ازدست‌رفته، ترافیک هر کلاینت

---

## ۲) چرا این مبحث مهم است؟

تصور کن مدیر یک رستوران هستی. اگر فقط وقتی مشتری شاکی شد بفهمی که آشپزخانه‌ات کند است، خیلی دیر شده. یک مدیر خوب **قبل از شلوغی** می‌داند کدام ایستگاه (گریل، سرخ‌کن، صندوق) به مرز ظرفیتش نزدیک می‌شود و کجا باید نیرو/تجهیزات اضافه کند.

سرور هم دقیقاً همینطور است:
- اگر ندانی CPU 90% مصرف iowait دارد (یعنی منتظر دیسک است، نه واقعاً پردازش می‌کند)، ممکن است اشتباهاً CPU قوی‌تر بخری در حالی که مشکل از دیسک کند بوده.
- اگر رشد مصرف دیسک را پیگیری نکنی، یک روز صبح از خواب بیدار می‌شوی و سرور production به‌خاطر پر شدن `/var` از کار افتاده.
- در دنیای واقعی، مدیریت (management) از تو می‌خواهد که **قبل از خرید سخت‌افزار جدید، عدد و مدرک بیاوری**، نه حدس.

این مهارت مستقیماً به Cyber Security هم وصل است: خیلی از حملات (DoS، cryptomining مخفی، exfiltration حجیم داده) اولین ردشان در **رفتار غیرعادی منابع سیستم** دیده می‌شود — CPU بالای غیرمنتظره، ترافیک شبکه ناگهانی، یا فضای دیسک که سریع‌تر از حد معمول پر می‌شود.

---

## ۳) مثال‌های واقعی + مثال روی سیستم خودم (Omarchy/Arch)

**مثال واقعی صنعتی:** یک تیم DevOps متوجه می‌شود سرعت پاسخ‌دهی API هر روز ساعت ۲ بعدازظهر افت می‌کند. با `sar` تاریخچه CPU را چک می‌کنند و می‌بینند iowait دقیقاً همان ساعت اوج می‌گیرد — چون یک job پشتیبان‌گیری (backup) با cron همان موقع اجرا می‌شود و دیسک را اشغال می‌کند. راه‌حل: جابه‌جایی زمان backup، نه خرید سرور جدید.

**مثال روی سیستم خودت:** روی Omarchy، بیشتر این ابزارها (`iostat`, `vmstat`, `sar`, `mpstat`) از پکیج **sysstat** می‌آیند که پیش‌فرض نصب نیست (برخلاف برخی توزیع‌های دیگر که از قبل دارند). پس اولین قدم عملی نصب آن با `pacman` است.

---

## ۴) دستورات کامل + توضیح دقیق گزینه‌ها

### نصب پیش‌نیاز روی Arch/Omarchy

```bash
sudo pacman -S sysstat
```
پکیج `sysstat` شامل `iostat`, `mpstat`, `sar`, `sadc` است. برای فعال‌سازی جمع‌آوری خودکار آمار (لازم برای اینکه `sar` تاریخچه داشته باشد):

```bash
sudo systemctl enable --now sysstat
```

> ⚠️ **هشدار امنیتی/عملیاتی خفیف:** فعال کردن `sysstat` باعث می‌شود هر ۱۰ دقیقه یک snapshot از وضعیت سیستم در `/var/log/sa/` ذخیره شود. روی دیسک‌های خیلی کوچک (SSDهای کم‌ظرفیت) این لاگ‌ها را هم باید مانیتور کنی که فضا اشغال نکنند.

---

### الف) اندازه‌گیری CPU

```bash
top
htop
mpstat 1 5
```
- `top` / `htop`: نمای زنده و لحظه‌ای مصرف CPU به تفکیک پردازه. `htop` تعاملی‌تر است و روی Arch با `sudo pacman -S htop` نصب می‌شود.
- `mpstat 1 5`: آمار CPU را هر ۱ ثانیه، به تعداد ۵ بار چاپ می‌کند. ستون‌های کلیدی:
  - `%usr` — زمان صرف‌شده برای برنامه‌های کاربر
  - `%sys` — زمان صرف‌شده در هسته (kernel)
  - `%iowait` — پردازنده بیکار مانده چون منتظر دیسک بوده (**اگر بالا باشد، مشکل از CPU نیست، از I/O است**)
  - `%steal` — فقط در محیط‌های مجازی معنا دارد؛ زمانی که هایپروایزر منابع را از تو گرفته

```bash
uptime
```
سه عدد load average (۱، ۵، ۱۵ دقیقه) را نشان می‌دهد. قانون سرانگشتی: اگر این عدد از تعداد هسته‌های CPU بیشتر شود، سیستم صف پردازش دارد.

```bash
nproc
```
تعداد هسته‌های موجود را می‌دهد تا بتوانی load average را تفسیر کنی.

---

### ب) اندازه‌گیری حافظه (Memory)

```bash
free -h
vmstat 1 5
```
- `free -h`: نمایش خوانا (human-readable) از RAM کل، استفاده‌شده، free، buffer/cache، و swap.
- `vmstat 1 5`: هر ثانیه یک خط، ۵ بار. ستون‌های مهم:
  - `si` / `so` — swap in / swap out. اگر مدام غیر صفر باشند یعنی RAM کم است و سیستم دارد به شدت از دیسک به‌جای RAM استفاده می‌کند (فاجعه برای کارایی).
  - `b` — تعداد پردازه‌های blocked (منتظر I/O)

---

### ج) اندازه‌گیری دیسک I/O

```bash
iostat -x 1 5
iotop
```
- `iostat -x 1 5`: نمای گسترده (extended) از هر دیسک. مهم‌ترین ستون: `%util` (چقدر درصد زمان، دیسک مشغول بوده — نزدیک ۱۰۰% یعنی گلوگاه دیسک داری) و `await` (میانگین زمان انتظار هر I/O، به میلی‌ثانیه).
- `iotop`: مثل `top` ولی برای I/O دیسک؛ نشان می‌دهد **کدام پردازه** بیشترین خواندن/نوشتن را انجام می‌دهد. نیاز به `sudo` دارد چون به آمار سطح-kernel دسترسی می‌خواهد.
  نصب روی Arch: `sudo pacman -S iotop`

---

### د) اندازه‌گیری شبکه

```bash
ss -s
ss -tunap
iptraf-ng
```
- `ss -s` — خلاصه آماری از سوکت‌های باز (جایگزین مدرن `netstat` که در LPIC-1 دیدی).
- `ss -tunap` — لیست کامل سوکت‌های TCP/UDP فعال همراه با پردازه صاحب هر سوکت (نیاز به `sudo` برای دیدن پردازه‌های بقیه‌ی کاربران).
- `iptraf-ng` — رابط متنی تعاملی برای دیدن ترافیک لحظه‌ای هر اینترفیس و هر اتصال. روی Arch: `sudo pacman -S iptraf-ng` (از AUR یا مخزن community، بسته به پیکربندی مخازن).

```bash
lsof -i
```
لیست تمام پردازه‌هایی که پورت/سوکت شبکه باز کرده‌اند — بسیار کاربردی برای پیدا کردن یک سرویس مشکوک که پورتی باز نگه داشته.

---

### ه) تاریخچه و همبستگی (Correlation) — قلب 200.2

```bash
sar -u 1 3      # تاریخچه CPU
sar -r 1 3      # تاریخچه Memory
sar -d 1 3      # تاریخچه دیسک
sar -n DEV 1 3  # تاریخچه شبکه
```
`sar` تنها ابزاری در این لیست است که **تاریخچه** نگه می‌دارد (به شرط فعال بودن سرویس sysstat که بالاتر گفتیم). این دقیقاً همان چیزی است که برای **پیش‌بینی روند رشد مصرف (200.2)** لازم داری — می‌توانی گزارش‌های چند روز/چند هفته گذشته را بکشی بیرون و روند رشد را ببینی:

```bash
sar -r -f /var/log/sa/sa15   # آمار حافظه‌ی روز پانزدهم ماه
```

---

## ۵) نکات مهم آزمون LPIC (Exam Tips)

- ✅ آزمون معمولاً بین **iostat، vmstat، mpstat، sar** گیج‌کننده سؤال می‌پرسد — تفاوت کلیدی: `sar` تنها ابزاری با قابلیت تاریخچه (historical) است، بقیه فقط لحظه‌ای (real-time) هستند.
- ✅ به تفاوت `%iowait` (مشکل دیسک) و `%steal` (مشکل محیط مجازی/هایپروایزر) دقت کن — سؤال رایج است.
- ✅ حتماً بدان که `sysstat` باید نصب و enable شود تا `sar` کار کند؛ این نکته‌ی عملی زیاد در سؤالات سناریو‌محور می‌آید.
- ✅ اسم‌های دقیق فایل و مسیر لاگ‌های sar را بشناس: `/var/log/sa/saXX` (روی توزیع‌های مبتنی بر Debian) — روی Arch مسیر ممکن است متفاوت باشد؛ در آزمون معمولاً رفتار عمومی سؤال می‌شود نه مسیر توزیع‌محور.
- ✅ ابزارهای مانیتورینگ سطح‌بالاتر (Icinga2, Nagios, collectd, MRTG, Cacti) فقط باید **بشناسی که چه کاری می‌کنند** — آزمون جزئیات پیکربندی این‌ها را نمی‌پرسد، فقط سطح آگاهی (awareness).
- ✅ `w` و `uptime` هر دو load average نشان می‌دهند؛ `w` علاوه‌بر آن کاربران لاگین‌شده را هم نشان می‌دهد.

---

## ۶) تمرین عملی امن (روی Omarchy/Arch)

> ⚠️ **هشدار:** هیچ‌کدام از دستورات این بخش سیستم را تغییر ساختاری نمی‌دهند، اما دستور آخر (تولید بار CPU) باید محدود به زمان کوتاه اجرا شود تا مصرف باتری/حرارت لپ‌تاپ مشکل ایجاد نکند.

1. نصب ابزارهای لازم:
```bash
sudo pacman -S sysstat htop iotop
sudo systemctl enable --now sysstat
```

2. یک ترمینال باز کن و مصرف زنده‌ی سیستم را ببین:
```bash
htop
```

3. در ترمینال دوم، یک بار مصنوعی کوتاه روی CPU ایجاد کن (فقط ۱۰ ثانیه، امن):
```bash
timeout 10 bash -c 'while true; do :; done'
```
همزمان در `htop` مشاهده کن که کدام هسته به ۱۰۰% می‌رسد.

4. حالا با `mpstat` این بار را از زاویه دیگر ببین:
```bash
mpstat -P ALL 1 10
```

5. وضعیت حافظه و صف I/O را چک کن:
```bash
free -h
vmstat 1 5
```

6. در پایان، یک گزارش تاریخی از CPU همین چند دقیقه بگیر (چون sysstat هر ۱۰ دقیقه snapshot می‌گیرد، شاید لازم باشد کمی صبر کنی یا دستی جمع‌آوری کنی):
```bash
sudo /usr/lib/sysstat/sa1 1 1   # یک نمونه‌ی دستی فوری بگیر
sar -u
```

---

## ۷) خلاصه مخصوص دفتر (جدول مرور سریع)

| ابزار | منبع اندازه‌گیری | لحظه‌ای/تاریخی | نکته کلیدی |
|---|---|---|---|
| `top` / `htop` | CPU + Memory (به تفکیک پردازه) | لحظه‌ای | تعاملی، برای تشخیص سریع پردازه پرمصرف |
| `mpstat` | CPU (به تفکیک هسته) | لحظه‌ای | `%iowait` را از `%steal` تفکیک می‌کند |
| `vmstat` | Memory + CPU + I/O queue | لحظه‌ای | ستون‌های `si`/`so` = نشانه کمبود RAM |
| `iostat -x` | دیسک | لحظه‌ای | `%util` و `await` کلیدی‌اند |
| `iotop` | دیسک (به تفکیک پردازه) | لحظه‌ای | نیاز به sudo |
| `ss` / `netstat` | شبکه (سوکت‌ها) | لحظه‌ای | `ss` جایگزین مدرن `netstat` |
| `iptraf-ng` | شبکه (پهنای‌باند) | لحظه‌ای | نمای تعاملی زنده |
| `sar` | همه‌ی موارد بالا | **تاریخی** | تنها ابزار با قابلیت پیش‌بینی روند |
| `uptime` / `w` | Load average | لحظه‌ای | مقایسه با `nproc` معنا پیدا می‌کند |

---

## ۸) مایندمپ متنی

```
Topic 200: Capacity Planning
│
├── 200.1 Measure & Troubleshoot (وزن 6)
│   ├── CPU
│   │   ├── top / htop
│   │   ├── mpstat (%usr, %sys, %iowait, %steal)
│   │   └── uptime / w / nproc (load average)
│   ├── Memory
│   │   ├── free -h
│   │   └── vmstat (si/so = swap pressure)
│   ├── Disk I/O
│   │   ├── iostat -x (%util, await)
│   │   └── iotop (per-process)
│   ├── Network I/O
│   │   ├── ss / netstat
│   │   ├── iptraf-ng
│   │   └── lsof -i
│   └── Correlation
│       └── match symptoms ↔ likely root cause
│
└── 200.2 Predict Future Needs (وزن 2)
    ├── sar (تنها ابزار تاریخی)
    │   └── /var/log/sa/saXX
    ├── رصد نرخ رشد (growth rate)
    ├── رسم روند (graph the trend)
    └── آگاهی از ابزارهای بلندمدت
        (Icinga2, Nagios, collectd, MRTG, Cacti)
```
