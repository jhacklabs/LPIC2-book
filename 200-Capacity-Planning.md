---
title: "Topic 200 — Capacity Planning (نسخه عمیق و کامل)"
exam: LPIC-2 / 201-450
weights: "200.1 (6) + 200.2 (2)"
os_target: "Arch Linux / Omarchy"
tags: [lpic2, capacity-planning, monitoring, performance, deep-dive]
---

# Topic 200: Capacity Planning — راهنمای کامل و عمیق

> این اولین Topic کل LPIC-2 است و پایه‌ی ذهنی همه‌ی Topic های بعدی را می‌سازد: قبل از این‌که بتوانی سرور، شبکه، یا امنیت را مدیریت کنی، باید بتوانی **بفهمی سیستمت الان دقیقاً چه کار می‌کند** و چرا. وزن ۶ برای 200.1 نشان می‌دهد این مهارت چقدر در دنیای واقعی روزمره است.

---

# بخش اول — 200.1: اندازه‌گیری و عیب‌یابی مصرف منابع

## ۱.۱) چرا این مهارت اساساً با «دستور زدن» فرق دارد

در LPIC-1 یاد گرفتی چطور یک سیستم را اداره کنی — کاربر بسازی، سرویس نصب کنی، فایل ویرایش کنی. اما یک سؤال کاملاً متفاوت هست: «چرا این سرور کند شده؟» این سؤال را نمی‌شود با یک دستور ساده جواب داد؛ نیاز به **رویکرد تشخیصی سیستماتیک** دارد — دقیقاً مثل یک پزشک که علائم مختلف (تب، درد، خستگی) را می‌بیند و باید تشخیص دهد کدام عضو واقعاً مشکل دارد، نه فقط علامت سطحی را درمان کند.

چهار منبع بنیادین هر کامپیوتری که باید بتوانی اندازه بگیری و از هم تفکیک کنی:
- **CPU** — چقدر پردازنده مشغول است، و **چرا** (این «چرا» مهم‌تر از عدد خام است)
- **Memory** — چقدر RAM واقعاً آزاد است (که خیلی پیچیده‌تر از یک عدد ساده است، همان‌طور که می‌بینیم)
- **Disk I/O** — سرعت و تأخیر خواندن/نوشتن
- **Network I/O** — پهنای‌باند و تأخیر شبکه

## ۱.۲) CPU — فراتر از یک درصد ساده

### چرا «Load Average» تنها نمی‌تواند کل داستان را بگوید

```bash
uptime
```
خروجی چیزی مثل این می‌دهد:
```
14:23:01 up 5 days,  3:12,  2 users,  load average: 2.15, 1.98, 1.75
```
سه عدد به‌ترتیب میانگین بار سیستم در ۱، ۵، و ۱۵ دقیقه‌ی گذشته هستند. اما **«بار» دقیقاً یعنی چه؟** روی لینوکس، Load Average تعداد میانگین پردازه‌هایی است که یا در حال اجرا روی CPU هستند، یا **منتظر** CPU‌اند (در صف اجرا)، **و همچنین** پردازه‌هایی که منتظر I/O (مثل دیسک) هستند در حالت «uninterruptible sleep» — این نکته‌ی بسیار مهمی است که خیلی‌ها نمی‌دانند: **Load Average لینوکس فقط درباره‌ی CPU نیست**، بلکه ترکیبی از فشار CPU و I/O است. برای همین می‌بینی گاهی Load Average بالا می‌رود در حالی که CPU خودش عملاً بیکار است — این یعنی مشکل از دیسک است، نه پردازنده.

**قانون تفسیر:** اگر Load Average از تعداد هسته‌های منطقی CPU بیشتر شود، یعنی سیستم صف انتظار برای منابع دارد:
```bash
nproc          # تعداد هسته‌های منطقی موجود
```
اگر `nproc` عدد ۴ بدهد و Load Average عدد ۸ باشد، یعنی به‌طور میانگین ۴ کار «اضافه» همیشه منتظر بوده‌اند — این نشانه‌ی یک سیستم تحت‌فشار است.

### `top`/`htop` — نمای زنده، اما این ستون‌ها را عمیق بخوان

```bash
top
```
مهم‌ترین سطر خروجی `top` که اکثراً نادیده گرفته می‌شود:
```
%Cpu(s):  15.2 us,  3.1 sy,  0.0 ni, 78.5 id,  2.9 wa,  0.0 hi,  0.2 si,  0.0 st
```
- **`us`** (user) — زمان صرف‌شده روی برنامه‌های عادی کاربر
- **`sy`** (system) — زمان صرف‌شده داخل کرنل (سیستم‌کال‌ها، مدیریت حافظه) — اگر این عدد بالا باشد ولی `us` پایین، معمولاً یعنی برنامه‌ای دارد بیش‌ازحد سیستم‌کال می‌زند (مثلاً I/O بسیار خرد و مکرر)
- **`ni`** (nice) — زمان صرف‌شده روی پردازه‌هایی که اولویتشان دستی تغییر کرده (با `nice`/`renice`)
- **`id`** (idle) — واقعاً بیکار
- **`wa`** (iowait) — این یکی از مهم‌ترین اعداد کل این Topic است: پردازنده **بیکار مانده چون منتظر تکمیل یک عملیات دیسک بوده** — یعنی CPU خودش کاری ندارد، اما نمی‌تواند کار بعدی را شروع کند چون به داده‌ای نیاز دارد که هنوز از دیسک نیامده. **`wa` بالا یعنی مشکل از دیسک است، نه CPU** — این دقیقاً تله‌ای است که در آزمون و در دنیای واقعی، مدیران تازه‌کار می‌افتند (CPU قوی‌تر می‌خرند در حالی که مشکل واقعی دیسک کند بوده).
- **`hi`/`si`** (hardware/software interrupt) — زمان صرف‌شده روی رسیدگی به وقفه‌های سخت‌افزاری/نرم‌افزاری
- **`st`** (steal) — این فقط در محیط‌های **مجازی‌سازی‌شده** (VM) معنا دارد: زمانی که هایپروایزر (میزبان) منابع CPU را از این VM «دزدیده» و به یک VM دیگر روی همان سرور فیزیکی داده. اگر روی یک VM ابری هستی و `st` بالاست، یعنی مشکل از خودت نیست — همسایه‌های مجازی‌ات دارند منابع فیزیکی مشترک را می‌خورند (این پدیده گاهی «Noisy Neighbor» نامیده می‌شود).

### `mpstat` — تفکیک به ازای هر هسته

```bash
mpstat -P ALL 1 5
```
`top` معمولاً میانگین کل هسته‌ها را نشان می‌دهد؛ `mpstat -P ALL` هرکدام را جدا نشان می‌دهد. چرا این تفکیک مهم است؟ چون یک برنامه‌ی **تک‌رشته‌ای** (single-threaded) فقط می‌تواند از **یک** هسته استفاده کند — اگر سیستم ۸ هسته دارد و میانگین کلی CPU فقط ۱۲.۵٪ نشان می‌دهد، ممکن است در واقع **یک** هسته ۱۰۰٪ پر باشد (و برنامه‌ی مسئول کند اجرا می‌شود) در حالی که ۷ هسته‌ی دیگر کاملاً بیکارند — میانگین کلی این واقعیت را کاملاً پنهان می‌کند.

## ۱.۳) Memory — چرا «فضای آزاد» یک مفهوم گمراه‌کننده است

```bash
free -h
```
```
              total        used        free      shared  buff/cache   available
Mem:           15Gi       3.2Gi       1.1Gi       200Mi        11Gi        11Gi
```
اینجا رایج‌ترین سوءتفاهم کل دوره‌ی لینوکس اتفاق می‌افتد: ستون **`free`** خیلی کوچک است (۱.۱ گیگ)، اما این اصلاً به این معنا نیست که سیستم کم‌حافظه است!

**چرا؟** لینوکس فلسفه‌ای دارد که می‌گوید «RAM بیکار، RAM هدررفته است». هر بار فایلی از دیسک خوانده می‌شود، کرنل محتوایش را در حافظه به‌عنوان **cache** نگه می‌دارد — اگر دوباره همان فایل خوانده شود، از RAM (بسیار سریع‌تر) می‌آید، نه از دیسک (کندتر). ستون **`buff/cache`** (اینجا ۱۱ گیگ) این حافظه‌ی «قرض‌داده‌شده» است — به‌محض این‌که یک برنامه‌ی جدید واقعاً به حافظه نیاز داشته باشد، کرنل فوراً بخشی از این cache را **آزاد و بازپس** می‌گیرد (بدون هیچ هزینه‌ای، چون این داده فقط یک کپی از چیزی است که روی دیسک هم هست).

**ستون واقعاً مهم، `available` است** — این عدد تخمین می‌زند «اگر یک برنامه‌ی جدید همین الان بخواهد حافظه بگیرد، واقعاً چقدر بدون swap کردن در دسترس است» (شامل RAM واقعاً آزاد + بخشی از cache که به‌راحتی قابل‌بازپس‌گیری است). برای تشخیص «آیا این سیستم واقعاً کم‌حافظه است»، همیشه به `available` نگاه کن، نه `free`.

### `vmstat` — دیدن فشار واقعی حافظه

```bash
vmstat 1 5
```
```
procs -----------memory---------- ---swap-- -----io---- -system-- ------cpu-----
 r  b   swpd   free   buff  cache   si   so    bi    bo   in   cs us sy id wa st
 1  0      0 1130000 205000 8900000    0    0    45    12  200  350  5  2 92  1  0
```
ستون‌های حیاتی:
- **`si`/`so`** (swap in/out) — این دو عدد **مهم‌ترین** نشانه‌ی واقعی کمبود RAM هستند. اگر این‌ها مدام غیرصفر باشند، یعنی کرنل مجبور است داده را بین RAM و دیسک (swap) جابه‌جا کند چون RAM واقعاً کم آورده — این می‌تواند سیستم را به‌شدت کند کند، چون دیسک (حتی SSD) هزاران بار کندتر از RAM است.
- **`b`** — تعداد پردازه‌هایی که «blocked» هستند (منتظر تکمیل I/O)؛ اگر این عدد پیوسته بالاست، دقیقاً همان معنای `wa` بالا در `top` را دارد.
- **`r`** — تعداد پردازه‌هایی که آماده‌ی اجرا هستند اما منتظر نوبت CPU‌اند — این عدد باید تقریباً با `nproc` مقایسه شود، دقیقاً مثل Load Average.

## ۱.۴) Disk I/O — چرا فقط سرعت کافی نیست، تأخیر هم مهم است

```bash
iostat -x 1 5
```
دو ستون کلیدی که باید عمیق بفهمی:
- **`%util`** — چند درصد از زمان، این دیسک **مشغول** بوده (نه لزوماً «پر» بودن ظرفیت، بلکه اشغال‌بودن فیزیکی هد/کنترلر). نزدیک ۱۰۰٪ یعنی دیسک عملاً گلوگاه است.
- **`await`** — میانگین زمانی (به میلی‌ثانیه) که هر درخواست I/O طول کشیده تا کامل شود، **شامل زمان انتظار در صف**. این عدد تفاوت مهمی با «سرعت خام دیسک» دارد: یک دیسک ممکن است سرعت نظری بالایی داشته باشد، اما اگر صف درخواست‌ها طولانی باشد، `await` می‌تواند خیلی بالا برود — این دقیقاً چیزی است که کاربر واقعی حس می‌کند («سیستم هنگ کرده») حتی اگر خود دیسک خراب نباشد.

```bash
sudo iotop
```
`iostat` می‌گوید «کدام دیسک مشغول است»؛ `iotop` می‌گوید «**کدام پردازه**» مسئول آن است — این دو با هم، دقیقاً مثل `top`/`mpstat` برای CPU، تصویر کامل را می‌سازند: اول با `iostat` می‌فهمی مشکل دیسک است، بعد با `iotop` می‌فهمی کدام برنامه مقصر است.

## ۱.۵) Network I/O — دیدن ترافیک و مشکلات آن

```bash
ss -s                    # خلاصه‌ی آماری تمام سوکت‌ها (تعداد TCP/UDP باز، در چه وضعیتی)
ss -tunap                 # لیست کامل با پردازه‌ی مالک هر سوکت (نیاز به sudo برای دیدن پردازه‌های بقیه)
sudo iptraf-ng             # نمای زنده و تعاملی پهنای‌باند هر اینترفیس/اتصال
sudo lsof -i                # کدام پردازه‌ها پورت/سوکت شبکه باز کرده‌اند
```
از منظر امنیتی هم مهم است: `lsof -i` یکی از اولین ابزارهایی است که برای پیدا کردن یک سرویس مشکوک/بدافزار که در پس‌زمینه یک پورت باز نگه داشته استفاده می‌شود.

## ۱.۶) همبستگی (Correlation) — هنر واقعی تشخیص

نکته‌ی نهایی و مهم‌ترین مهارت این بخش: هیچ‌کدام از این ابزارها به‌تنهایی داستان کامل را نمی‌گویند. تشخیص واقعی یعنی **همبستگی بین چند علامت**:

مثال کامل: فرض کن یک کاربر می‌گوید «سایت کند شده». تو می‌بینی `top` نشان می‌دهد `%wa` بالاست (۴۰٪). این خودش یعنی مشکل دیسک است، نه CPU. حالا با `iostat -x` می‌بینی `%util` روی یک دیسک خاص ۱۰۰٪ است. با `iotop` می‌فهمی یک پردازه‌ی backup که قرار بود شب‌ها اجرا شود، به‌اشتباه هنوز در حال اجراست و کل I/O دیسک را اشغال کرده. **این زنجیره‌ی استدلالی — از یک شکایت مبهم تا یک علت ریشه‌ای دقیق — دقیقاً چیزی است که وزن ۶ این زیرمبحث را توجیه می‌کند.**

## ۱.۷) نکات مهم آزمون برای 200.1

- ✅ Load Average لینوکس شامل فشار I/O هم می‌شود، نه فقط CPU — این تله‌ی رایج آزمون است.
- ✅ `%wa` (iowait) بالا یعنی مشکل دیسک، نه CPU — رایج‌ترین سؤال مفهومی کل Topic.
- ✅ `%st` (steal) فقط در محیط مجازی معنا دارد.
- ✅ ستون `available` در `free`، نه `free` خام — برای تشخیص واقعی کمبود RAM.
- ✅ `si`/`so` در `vmstat` غیرصفر مداوم = نشانه‌ی قطعی کمبود RAM.
- ✅ `%util` (اشغال دیسک) و `await` (تأخیر واقعی شامل صف) را از هم تفکیک کن.
- ✅ ترتیب ابزار تشخیصی: اول ببین مشکل کدام منبع است (top/vmstat/iostat)، بعد ببین کدام پردازه مقصر است (iotop/lsof).

---

# بخش دوم — 200.2: پیش‌بینی نیاز آینده به منابع

## ۲.۱) چرا ابزارهای لحظه‌ای (real-time) برای این کار کافی نیستند

تمام ابزارهایی که در 200.1 دیدیم (`top`, `vmstat`, `iostat`, `mpstat`) یک ویژگی مشترک دارند: فقط **همین لحظه** یا چند ثانیه‌ی اخیر را نشان می‌دهند. اما سؤال Capacity Planning واقعی این است: «با این نرخ رشد مصرف، کِی دیسک/RAM/CPU ما تمام می‌شود؟» — این سؤال نیاز به **تاریخچه** دارد، نه یک اسنپ‌شات لحظه‌ای.

## ۲.۲) `sar` — تنها ابزار در این خانواده با حافظه‌ی تاریخی

```bash
sudo pacman -S sysstat
sudo systemctl enable --now sysstat
```
سرویس `sysstat` هر ۱۰ دقیقه (پیش‌فرض قابل‌تنظیم) یک «عکس فوری» از وضعیت سیستم می‌گیرد و در `/var/log/sa/saXX` ذخیره می‌کند (`XX` روز ماه است). این یعنی بعد از چند هفته، یک **تاریخچه‌ی کامل** از رفتار سیستم داری — دقیقاً چیزی که برای دیدن روند رشد لازم است.

```bash
sar -u 1 3         # CPU، لحظه‌ای (سه نمونه با فاصله‌ی ۱ ثانیه) — مثل mpstat
sar -u              # CPU، تاریخچه‌ی همین امروز (تمام نمونه‌های ذخیره‌شده)
sar -r               # حافظه، تاریخچه
sar -d               # دیسک، تاریخچه
sar -n DEV           # شبکه، تاریخچه
sar -r -f /var/log/sa/sa15     # حافظه، مربوط به روز پانزدهم همین ماه
```

### چطور واقعاً از این برای پیش‌بینی استفاده کنیم

فرض کن هر هفته `sar -r -f /var/log/sa/saXX` مصرف حافظه‌ی میانگین روزانه را استخراج می‌کنی و در یک جدول (یا اسپردشیت) ثبت می‌کنی:
```
هفته ۱: میانگین استفاده RAM = 60%
هفته ۲: میانگین استفاده RAM = 63%
هفته ۳: میانگین استفاده RAM = 67%
هفته ۴: میانگین استفاده RAM = 70%
```
این یک روند خطی تقریباً ۳٪ در هفته نشان می‌دهد. با یک محاسبه‌ی ساده (نه چیزی که آزمون از تو بخواهد محاسبه کنی، فقط برای فهم مفهوم): با این نرخ، حدود ۱۰ هفته‌ی دیگر به ۱۰۰٪ می‌رسی — این دقیقاً نوع استدلالی است که به یک مدیر اجازه می‌دهد **قبل از** بحران واقعی، درخواست ارتقای سخت‌افزار بدهد، با عدد و مدرک مستند، نه حدس.

## ۲.۳) ابزارهای مانیتورینگ سطح بالاتر — فقط در حد آگاهی

سرفصل رسمی LPIC-2 انتظار ندارد این ابزارها را پیکربندی کنی، فقط باید **بدانی چه کاری می‌کنند** و کِی مناسب‌اند:

- **collectd** — یک دیمون سبک که به‌صورت مداوم متریک‌های سیستم را جمع‌آوری و در یک پایگاه‌داده (اغلب time-series مثل RRD یا InfluxDB) ذخیره می‌کند — این عملاً `sar` را به یک سطح خودکارتر و قابل‌مصورسازی‌تر می‌برد.
- **MRTG** (Multi Router Traffic Grapher) — تاریخاً برای رسم نمودار ترافیک شبکه از روی داده‌های SNMP روترها ساخته شد؛ امروز نسبتاً قدیمی محسوب می‌شود اما هنوز در برخی محیط‌ها دیده می‌شود.
- **Cacti** — یک رابط وب کامل‌تر روی مفهوم مشابه MRTG، با نمودارهای تاریخی زیبا و قابل‌تنظیم.
- **Nagios** / **Icinga2** — این‌ها بیشتر ابزار **هشداردهی** (Alerting) هستند تا رسم نمودار: مثلاً «اگر فضای دیسک از ۹۰٪ رد شد، فوراً ایمیل/پیامک بفرست». Icinga2 نسخه‌ی fork‌شده و مدرن‌تر Nagios است.

**چرا این طیف از ابزارها اهمیت دارد؟** چون `sar` عالی است برای یک سرور تنها، اما وقتی ۵۰ سرور داری، نمی‌توانی دستی روی هرکدام `sar` بزنی — این ابزارهای سطح‌بالاتر داده را از همه‌ی سرورها **مرکزی** جمع می‌کنند، که خودش پلی به مفاهیم مدیریت مقیاس‌پذیر (که در Topic 210 با LDAP هم دیدیم) است.

## ۲.۴) نکات مهم آزمون برای 200.2

- ✅ `sar` تنها ابزار در کل خانواده‌ی ابزارهای منابع سیستم با قابلیت **تاریخی** — این تمایزش با `top`/`vmstat`/`iostat`/`mpstat` (که فقط لحظه‌ای‌اند) پایه‌ی اصلی این زیرمبحث است.
- ✅ سرویس `sysstat` باید فعال (enable) باشد تا `sar` بتواند تاریخچه جمع کند؛ بدون آن، `sar` هم فقط لحظه‌ای عمل می‌کند.
- ✅ مسیر لاگ‌های تاریخی: `/var/log/sa/saXX`.
- ✅ ابزارهای سطح‌بالاتر (collectd/MRTG/Cacti/Nagios/Icinga2) را فقط در حد «چه کاری می‌کنند» بشناس، نه پیکربندی — آزمون جزئیات این‌ها را نمی‌پرسد.

---

## خلاصه‌ی جدولی نهایی کل Topic 200

| زیرمبحث | مفهوم کلیدی | جزئیات |
|---|---|---|
| 200.1 | Load Average | شامل فشار I/O هم می‌شود، نه فقط CPU |
| 200.1 | `top` ستون‌ها | us/sy/ni/id/**wa**(دیسک!)/**st**(فقط VM) |
| 200.1 | Memory واقعی | `available` نه `free` خام؛ buff/cache = قابل‌بازپس‌گیری |
| 200.1 | swap فشار | `si`/`so` در vmstat غیرصفر = کمبود RAM قطعی |
| 200.1 | Disk گلوگاه | `%util`(اشغال) + `await`(تأخیر واقعی) |
| 200.1 | همبستگی | چند ابزار با هم = تشخیص علت ریشه‌ای |
| 200.2 | تنها ابزار تاریخی | `sar` (نیازمند سرویس `sysstat` فعال) |
| 200.2 | مسیر لاگ | `/var/log/sa/saXX` |
| 200.2 | ابزار سطح‌بالا | collectd/MRTG/Cacti (نمودار) vs Nagios/Icinga2 (هشدار) |

## مایندمپ متنی کامل

```
Topic 200: Capacity Planning
│
├── 200.1 Measure & Troubleshoot (وزن ۶ — سنگین‌ترین کل LPIC-2)
│   ├── CPU
│   │   ├── uptime/Load Average (شامل فشار I/O!)
│   │   ├── top: us/sy/ni/id/wa(⚠️دیسک)/st(⚠️فقط VM)
│   │   └── mpstat -P ALL (تفکیک هسته‌به‌هسته)
│   ├── Memory
│   │   ├── free -h: available≠free خام
│   │   └── vmstat: si/so(swap فشار)، b(blocked)
│   ├── Disk I/O
│   │   ├── iostat -x: %util + await
│   │   └── iotop (کدام پردازه مقصر)
│   ├── Network
│   │   └── ss/iptraf-ng/lsof -i
│   └── همبستگی: چند علامت → یک علت ریشه‌ای
│
└── 200.2 Predict Future Needs (وزن ۲)
    ├── چرا لحظه‌ای کافی نیست: نیاز به تاریخچه
    ├── sar (تنها ابزار تاریخی) ← نیازمند sysstat فعال
    │   └── /var/log/sa/saXX
    ├── روند رشد → پیش‌بینی با عدد و مدرک
    └── ابزار سطح‌بالا: collectd/MRTG/Cacti/Nagios/Icinga2 (آگاهی)
```
