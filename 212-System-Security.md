---
title: "Topic 212 — System Security (نسخه عمیق و کامل)"
exam: LPIC-2 / 202-450
weights: "212.1 (3) + 212.2 (2) + 212.3 (4) + 212.4 (3) + 212.5 (2)"
os_target: "Arch Linux / Omarchy"
tags: [lpic2, security, nftables, openvpn, ssh, gpg, deep-dive]
---

# Topic 212: System Security — راهنمای کامل و عمیق

> این آخرین Topic کل LPIC-2 و مستقیم‌ترین پل به مسیر Cyber Security توست. سعی کردیم عمیق‌ترین توضیح ممکن را اینجا بگذاریم چون این دانش پایه‌ی مطلق هر کار امنیتی بعدی خواهد بود.

---

# بخش اول — 212.1: پیکربندی یک سیستم به‌عنوان روتر

## ۱.۱) چرا یک سرور لینوکسی اصلاً می‌تواند روتر باشد؟

یک روتر فیزیکی (مثل مودم خانه‌ات) در اصل فقط یک کامپیوتر کوچک با لینوکس روی آن است که یک کار خاص انجام می‌دهد: بسته‌ها را از یک اینترفیس شبکه به اینترفیس دیگر **هدایت** (forward) می‌کند. کرنل لینوکس به‌طور بومی این قابلیت را دارد، فقط باید صریح فعالش کنی — چون به‌صورت پیش‌فرض، به دلایل امنیتی، یک ماشین لینوکسی معمولی بسته‌هایی که مقصدشان خودش نیست را **دور می‌ریزد**، نه فوروارد می‌کند.

```bash
sudo sysctl -w net.ipv4.ip_forward=1
```
این دقیقاً همان پارامتر `sysctl` است که در Topic 201 دیدیم — یادت باشد این تغییر **موقت** است. برای دائمی کردن:
```conf
# /etc/sysctl.d/99-forwarding.conf
net.ipv4.ip_forward = 1
```

## ۱.۲) NAT — چرا لازم است و چطور کار می‌کند

اگر یک سرور لینوکسی بین یک شبکه‌ی داخلی (با IP های خصوصی مثل `192.168.1.0/24`) و اینترنت عمومی قرار بگیرد، مشکلی پیش می‌آید: IP های خصوصی روی اینترنت عمومی **قابل‌مسیریابی نیستند** (هیچ روتری در اینترنت نمی‌داند `192.168.1.5` کجاست، چون میلیون‌ها شبکه‌ی خصوصی دیگر هم دقیقاً از همین بازه‌ی آدرس استفاده می‌کنند).

**NAT** (Network Address Translation) و به‌طور خاص **Masquerading** این را حل می‌کند: وقتی یک بسته از شبکه‌ی داخلی به سمت اینترنت عبور می‌کند، روتر آدرس مبدأ آن را با **آدرس عمومی خودش** جایگزین می‌کند (و یک نگاشت پورت داخلی نگه می‌دارد تا بداند پاسخ برگشتی را به کدام دستگاه داخلی برساند).

```bash
sudo nft add rule ip nat postrouting oif "wlan0" masquerade
```
این قانون می‌گوید: هر بسته‌ای که از اینترفیس `wlan0` (که فرض می‌کنیم به اینترنت وصل است) خارج می‌شود، آدرس مبدأش masquerade (پنهان و جایگزین با آدرس این اینترفیس) شود. کلمه‌ی «masquerade» به‌جای «SNAT» ساده معمولاً وقتی استفاده می‌شود که آدرس عمومی اینترفیس **پویا** (dynamic، مثل اکثر اتصالات خانگی) است — masquerade خودکار آدرس فعلی را تشخیص می‌دهد، برخلاف SNAT ساده که نیاز به تعریف یک آدرس ثابت دارد.

## ۱.۳) نکات مهم آزمون برای 212.1

- ✅ `net.ipv4.ip_forward=1` پیش‌نیاز مطلق هر روتر لینوکسی است.
- ✅ چرا NAT/Masquerading لازم است: آدرس‌های خصوصی روی اینترنت عمومی قابل‌مسیریابی نیستند.
- ✅ تفاوت مفهومی Masquerade (برای IP پویا) و SNAT ساده (برای IP ثابت).

---

# بخش دوم — 212.2: مدیریت امنیت با فایروال

## ۲.۱) فلسفه‌ی فایروال: Default Deny در برابر Default Allow

این مهم‌ترین اصل کل این بخش است و در تمام دنیای امنیت تکرار می‌شود: یک فایروال خوب همیشه با فلسفه‌ی **«همه‌چیز رد شود مگر آن‌که صریحاً مجاز شده باشد»** (Default Deny / Deny by Default) طراحی می‌شود، نه برعکس. چرا؟ چون در دنیای Default Allow، هر سرویس جدیدی که فراموش کنی صریح ببندی، به‌طور پیش‌فرض **باز** است — یعنی هر اشتباه سهوی، یک آسیب‌پذیری امنیتی می‌شود. در دنیای Default Deny، هر اشتباه سهوی فقط یعنی یک سرویس که باید کار کند، کار نمی‌کند (یک مشکل عملکردی قابل‌مشاهده و رفع، نه یک حفره‌ی امنیتی پنهان).

## ۲.۲) nftables — استاندارد امروزی

**nftables** جانشین رسمی `iptables` است (از کرنل‌های نسبتاً جدید به بعد) با یک زبان قانون‌نویسی یکپارچه‌تر و کارآمدتر (به‌جای چهار ابزار جدا برای IPv4/IPv6/ARP/Bridge که `iptables` داشت، `nftables` همه را در یک فریم‌ورک واحد جمع کرده).

### مفاهیم پایه: Table، Chain، Rule

```bash
sudo nft add table inet filter
sudo nft add chain inet filter input { type filter hook input priority 0 \; policy drop \; }
sudo nft add rule inet filter input ct state established,related accept
sudo nft add rule inet filter input iif lo accept
sudo nft add rule inet filter input tcp dport 22 accept
sudo nft add rule inet filter input tcp dport 443 accept
sudo nft add rule inet filter input icmp type echo-request accept
sudo nft add rule inet filter input counter log prefix "dropped: " drop
```

بیایید این ساختار را عمیق باز کنیم:
- **Table** (`inet filter`) — یک ظرف سازمانی که چندین Chain را گروه‌بندی می‌کند؛ `inet` یعنی این جدول همزمان برای IPv4 و IPv6 اعمال می‌شود (یکی از بهبودهای بزرگ nftables نسبت به iptables که مجبور بودی دو ابزار جدا `iptables`/`ip6tables` را جدا مدیریت کنی).
- **Chain** (`input`) — نقطه‌ای در مسیر عبور بسته که قوانین رویش اعمال می‌شوند؛ `hook input` یعنی این chain برای بسته‌هایی که **مقصدشان خود این ماشین** است اجرا می‌شود (در برابر `forward` برای بسته‌هایی که فقط از این ماشین عبور می‌کنند، یا `output` برای بسته‌هایی که این ماشین خودش می‌فرستد).
- **`policy drop`** — دقیقاً همان فلسفه‌ی Default Deny: اگر هیچ قانونی با یک بسته تطابق نداشت، آن بسته **دور ریخته** می‌شود.
- **`priority 0`** — وقتی چند chain به یک hook متصل‌اند، این عدد ترتیب اجرا را مشخص می‌کند.

### چرا ترتیب قوانین بالا این‌قدر مهم است

خط اول (`ct state established,related accept`) یک بهینه‌سازی حیاتی است: این می‌گوید «اگر این بسته بخشی از یک **اتصال از‌قبل‌برقرارشده** است (مثلاً پاسخ به یک درخواستی که خودمان فرستادیم)، بی‌درنگ قبولش کن». بدون این قانون در بالای لیست، هر پاسخ ورودی برای هر درخواست خروجی مجبور بود دوباره از تمام قوانین بعدی رد شود — این هم کارایی را بهتر می‌کند (چون اکثریت ترافیک اینجا فیلتر می‌شود، بدون نیاز به بررسی قوانین پایین‌تر) و هم منطقی است (چیزی که خودمان درخواستش کرده‌ایم را نباید دوباره سخت‌گیرانه بررسی کنیم).

```bash
sudo nft list ruleset          # نمایش کامل تمام قوانین فعلی، در قالب قابل‌اجرای مجدد
sudo nft flush ruleset          # پاک‌سازی کامل همه‌ی قوانین (⚠️ احتیاط!)
sudo nft -f /etc/nftables.conf   # بارگذاری قوانین از یک فایل
```

فایل تنظیمات دائمی: `/etc/nftables.conf`
```conf
table inet filter {
    chain input {
        type filter hook input priority 0; policy drop;
        ct state established,related accept
        iif lo accept
        tcp dport 22 accept
        tcp dport 443 accept
        icmp type echo-request accept
        counter log prefix "dropped: " drop
    }
}
```
```bash
sudo systemctl enable --now nftables
```

## ۲.۳) iptables — معماری کلاسیک که هنوز باید بشناسی

آزمون LPIC-2 هنوز `iptables` را می‌پرسد چون میلیون‌ها سرور در دنیای واقعی هنوز از آن استفاده می‌کنند.

```bash
sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT
sudo iptables -A INPUT -j DROP
sudo iptables -L -v -n
```
- `-A INPUT` — یک قانون به انتهای Chain به نام INPUT اضافه کن (append)
- `-p tcp --dport 22` — پروتکل TCP، پورت مقصد ۲۲
- `-j ACCEPT`/`-j DROP` — Jump یعنی چه کاری با بسته انجام شود

سه Chain پیش‌فرض `iptables` (بدون nftables): **INPUT** (بسته‌های ورودی به خود ماشین)، **OUTPUT** (بسته‌های خروجی از خود ماشین)، **FORWARD** (بسته‌هایی که فقط عبور می‌کنند، مثل یک روتر) — دقیقاً معادل مفهومی hookهای input/output/forward در nftables.

```bash
sudo iptables-save > /etc/iptables/iptables.rules      # ذخیره‌ی قوانین فعلی در یک فایل
sudo iptables-restore < /etc/iptables/iptables.rules     # بازیابی از فایل (معمولاً هنگام بوت خودکار اجرا می‌شود)
```

> ⚠️ **هشدار جدی‌ترین این بخش:** اگر روی یک سرور از‌راه‌دور کار می‌کنی و یک قانون `DROP` روی همه‌چیز اضافه کنی **قبل از** این‌که قانون `ACCEPT` برای پورت SSH را اضافه کرده باشی، بلافاصله خودت را از سرور **قفل می‌کنی** (lockout) — و چون تنها راه دسترسی‌ات همان SSH بود که الان بسته شده، هیچ راهی برای اصلاح از راه دور نداری (باید فیزیکی یا از طریق کنسول اضطراری دیتاسنتر وارد شوی). همیشه:
> 1. اول قانون Accept برای SSH را اضافه کن
> 2. تست کن یک session جدید SSH باز می‌شود (بدون بستن session فعلی!)
> 3. فقط بعد از تأیید، policy پیش‌فرض را به DROP تغییر بده

## ۲.۴) نکات مهم آزمون برای 212.2

- ✅ فلسفه‌ی Default Deny (`policy drop`) را همیشه به‌عنوان بهترین‌شیوه در نظر بگیر.
- ✅ ساختار nftables: Table → Chain (با hook و priority) → Rule.
- ✅ چرا `ct state established,related accept` باید همیشه اول لیست باشد.
- ✅ سه Chain کلاسیک iptables: INPUT/OUTPUT/FORWARD.
- ✅ هرگز روی سرور ریموت، DROP کلی را قبل از Accept برای SSH اعمال نکن.

---

# بخش سوم — 212.3: پیکربندی VPN

## ۳.۱) چرا VPN و چه مسئله‌ای را حل می‌کند

وقتی دو دفتر یک شرکت در دو شهر مختلف هستند، یا یک کارمند از خانه به شبکه‌ی داخلی شرکت نیاز دارد، اتصال مستقیم روی اینترنت عمومی هم ناامن است (هرکسی می‌تواند ترافیک را ببیند، دقیقاً مثل چیزی که با `tcpdump` در Topic 205 دیدیم) و هم اغلب به‌خاطر NAT/فایروال غیرممکن است. **VPN** (Virtual Private Network) یک تونل رمزنگاری‌شده روی اینترنت عمومی می‌سازد که به دو طرف اجازه می‌دهد طوری با هم حرف بزنند که انگار روی یک شبکه‌ی محلی خصوصی مشترک هستند.

## ۳.۲) OpenVPN — عمیق روی PKI

```bash
sudo pacman -S openvpn easy-rsa
```

### چرا PKI (زیرساخت کلید عمومی) برای VPN لازم است

OpenVPN از گواهی‌های TLS (دقیقاً همان مفهومی که در Topic 208 برای HTTPS دیدیم) برای احراز هویت **دوطرفه** استفاده می‌کند — یعنی نه‌تنها کلاینت باید مطمئن شود سرور واقعی است، بلکه سرور هم باید مطمئن شود کلاینت واقعاً مجاز است (برخلاف HTTPS معمولی که معمولاً فقط سرور گواهی دارد).

```bash
easyrsa init-pki               # شروع یک زیرساخت کلید عمومی جدید و خالی
easyrsa build-ca                 # ساخت یک Certificate Authority محلی (خودت CA خودت می‌شوی، برای سازمان داخلی)
easyrsa gen-req server nopass     # تولید یک درخواست گواهی (CSR) برای سرور
easyrsa sign-req server server     # امضای آن CSR با CA محلی‌ات — حالا سرور گواهی معتبر دارد
easyrsa gen-dh                     # تولید پارامترهای Diffie-Hellman (برای تبادل کلید امن اولیه)
```
برای هر کلاینتی که می‌خواهد وصل شود، یک فرآیند مشابه (`gen-req client1`, `sign-req client client1`) تکرار می‌شود — هر کلاینت گواهی خودش را دارد که توسط همان CA محلی امضا شده، یعنی سرور می‌تواند هر کلاینت را به‌صورت مجزا شناسایی/لغو (revoke) کند.

```conf
# /etc/openvpn/server.conf
port 1194
proto udp
dev tun
ca ca.crt
cert server.crt
key server.key
dh dh.pem
server 10.8.0.0 255.255.255.0
```
`dev tun` یعنی یک اینترفیس مجازی از نوع **tun** (لایه‌ی ۳، فقط بسته‌های IP، رایج‌ترین حالت برای VPN های Point-to-Point) در برابر **tap** (لایه‌ی ۲، کل فریم‌های Ethernet، برای زمانی که نیاز داری حتی پروتکل‌های غیر-IP یا broadcast های لایه‌ی پیوند داده را هم عبور بدهی، مثلاً برای شبیه‌سازی یک شبکه‌ی محلی کامل از راه دور).

```bash
sudo systemctl enable --now openvpn-server@server
```

## ۳.۳) IPsec — چارچوب استاندارد جایگزین

**IPsec** یک مجموعه‌ی استاندارد پروتکل (نه یک نرم‌افزار خاص) برای رمزنگاری در **لایه‌ی شبکه (IP)** است — برخلاف OpenVPN که در لایه‌ی کاربرد کار می‌کند. IPsec معمولاً بین **دو روتر/gateway** (Site-to-Site VPN) استفاده می‌شود، نه بین یک کاربر تکی و یک سرور. پیاده‌سازی‌های محبوب امروز **strongSwan** و **Libreswan** هستند (جانشین‌های مدرن FreeS/WAN قدیمی که در سرفصل رسمی نامش آمده اما امروز عملاً منسوخ شده).

```bash
sudo pacman -S strongswan
```

## ۳.۴) نکات مهم آزمون برای 212.3

- ✅ چرا OpenVPN به یک PKI کامل (نه فقط یک گواهی سرور) نیاز دارد — احراز هویت دوطرفه.
- ✅ ترتیب مراحل easy-rsa: `init-pki` → `build-ca` → `gen-req` → `sign-req` → `gen-dh`.
- ✅ تفاوت `tun` (لایه‌ی ۳، فقط IP) و `tap` (لایه‌ی ۲، کل فریم Ethernet).
- ✅ IPsec/strongSwan/Libreswan را به‌عنوان جانشین FreeS/WAN و ابزار رایج Site-to-Site بشناس.

---

# بخش چهارم — 212.4: امنیت خدمات شبکه‌ای

## ۴.۱) سخت‌سازی SSH — عمیق‌تر از فقط چند خط کانفیگ

```conf
# /etc/ssh/sshd_config
PermitRootLogin no
PasswordAuthentication no
PubkeyAuthentication yes
Port 2222
AllowUsers myuser anotheruser
MaxAuthTries 3
```

بیایید چرا هر خط مهم است را عمیق ببینیم:

- **`PermitRootLogin no`** — اگر این فعال (yes) باشد، مهاجم فقط باید رمز عبور **یک** حساب شناخته‌شده (root) را حدس بزند. با غیرفعال کردنش، مهاجم اول باید حدس بزند **کدام نام‌کاربری** اصلاً وجود دارد، بعد رمزش را — یک لایه‌ی دفاعی اضافه.
- **`PasswordAuthentication no`** + **`PubkeyAuthentication yes`** — این مهم‌ترین تغییر امنیتی SSH است. رمز عبور قابل brute-force است (حتی رمزهای قوی، در برابر بات‌نت‌های عظیم که میلیون‌ها تلاش در ثانیه انجام می‌دهند، در بلندمدت ریسک دارند)؛ کلید عمومی/خصوصی (که معمولاً ۲۵۶ بیت یا بیشتر آنتروپی دارد و هرگز از طریق شبکه منتقل نمی‌شود) عملاً غیرقابل حدس‌زدن با brute-force است.
- **`Port 2222`** — تغییر پورت پیش‌فرض «امنیت از طریق ابهام» (Security through Obscurity) است — به‌تنهایی امنیت واقعی نمی‌دهد (یک مهاجم مصمم به‌راحتی می‌تواند پورت‌های باز را اسکن کند)، اما **حجم بسیار زیادی از ترافیک اسکن خودکار سطحی و بی‌هدف بات‌ها** را که فقط پورت ۲۲ استاندارد را می‌زنند، فیلتر می‌کند — این باعث کاهش قابل‌توجه نویز در لاگ‌ها می‌شود، هرچند نباید تنها لایه‌ی دفاعی باشد.
- **`AllowUsers`** — لیست سفید صریح کاربران مجاز به SSH — حتی اگر یک حساب دیگر رمز عبور/کلید معتبر داشته باشد، اگر در این لیست نباشد، اصلاً اجازه‌ی تلاش هم ندارد.
- **`MaxAuthTries`** — محدود کردن تعداد تلاش در **یک اتصال TCP** (نه در طول زمان — این با fail2ban که در ادامه می‌بینیم فرق دارد).

```bash
ssh-keygen -t ed25519 -C "myuser@omarchy"
```
`ed25519` یک الگوریتم امضای دیجیتال مدرن مبتنی بر منحنی بیضوی (Elliptic Curve) است — کوتاه‌تر از کلیدهای RSA سنتی با امنیت معادل یا بهتر، و امروز الگوریتم پیش‌فرض توصیه‌شده برای کلیدهای SSH جدید.

```bash
ssh-copy-id -p 2222 myuser@server
```
این ابزار به‌طور خودکار کلید عمومی‌ات را به فایل `~/.ssh/authorized_keys` روی سرور اضافه می‌کند — بدون نیاز به کپی دستی و مدیریت permission های آن فایل (که باید دقیقاً `600` باشد، وگرنه SSH به دلایل امنیتی از آن استفاده نمی‌کند).

## ۴.۲) TCP Wrappers — روش قدیمی، هنوز در آزمون

```
# /etc/hosts.allow
sshd: 192.168.1.0/24

# /etc/hosts.deny
sshd: ALL
```
**ترتیب بررسی مهم است:** سیستم اول `/etc/hosts.allow` را بررسی می‌کند — اگر تطابقی پیدا شد، **فوراً** اجازه داده می‌شود (بدون نگاه کردن به `hosts.deny`). اگر در `allow` تطابقی نبود، سراغ `hosts.deny` می‌رود — اگر آنجا تطابق بود، رد می‌شود؛ اگر در هیچ‌کدام نبود، به‌طور پیش‌فرض **اجازه داده می‌شود** (این برخلاف فلسفه‌ی Default Deny فایروال است — برای همین TCP Wrappers به‌تنهایی کافی نیست و باید کنار فایروال واقعی استفاده شود، نه به‌جایش). این مکانیزم قدیمی و امروز عملاً کمتر رایج است (بسیاری سرویس‌های مدرن دیگر از کتابخانه‌ی libwrap که این را پیاده‌سازی می‌کند استفاده نمی‌کنند) اما در سرفصل و سؤالات آزمون هنوز حاضر است.

## ۴.۳) fail2ban — دفاع خودکار و پویا

این ابزاری است که مستقیماً محدودیت `MaxAuthTries` (که فقط در یک اتصال کار می‌کند) را حل می‌کند: **fail2ban لاگ‌های سرویس‌ها را پیوسته می‌خواند**، و اگر یک IP خاص در یک بازه‌ی زمانی مشخص، تعداد مشخصی تلاش ناموفق داشته باشد، آن IP را **کاملاً و خودکار در فایروال بلاک** می‌کند (نه فقط در همان اتصال، بلکه کاملاً از دسترسی به آن سرویس یا کل سرور).

```bash
sudo pacman -S fail2ban
```
```ini
# /etc/fail2ban/jail.local  ← هرگز مستقیم jail.conf را ویرایش نکن
[sshd]
enabled = true
port = 2222
maxretry = 3
bantime = 3600
findtime = 600
```
- **`maxretry`** — چند تلاش ناموفق قبل از بلاک
- **`findtime`** — این تلاش‌ها باید در چه بازه‌ی زمانی (ثانیه) اتفاق بیفتند تا شمرده شوند (اگر تلاش‌ها خیلی پراکنده در طول روزها باشند، شمارش از نو شروع می‌شود)
- **`bantime`** — مدت زمان بلاک شدن (ثانیه)؛ `-1` یعنی برای همیشه

```bash
sudo fail2ban-client status sshd            # نمایش لیست IP های فعلاً بلاک‌شده برای این jail
sudo fail2ban-client set sshd unbanip 1.2.3.4   # آزادسازی دستی یک IP (اگر اشتباهی مسدود شده)
```

## ۴.۴) اسکن پورت با nmap — دیدن سیستم از دید مهاجم

```bash
nmap -sV localhost
```
`-sV` یعنی علاوه‌بر پیدا کردن پورت‌های باز، تلاش کن **نسخه‌ی دقیق** نرم‌افزار پشت هر پورت را هم تشخیص بدهی — این دقیقاً همان تکنیکی است که مهاجمان برای شناسایی نرم‌افزارهای آسیب‌پذیر (با نسخه‌ی قدیمی و شناخته‌شده) استفاده می‌کنند؛ استفاده‌ی دفاعی آن این است که خودت باید **قبل از هر مهاجمی** بدانی چه چیزی از بیرون قابل‌مشاهده است.

> ⚠️ **هشدار قانونی جدی و تکرار همان اصل Topic 205:** اسکن پورت روی شبکه/سیستم‌هایی که مالکشان نیستی یا اجازه‌ی صریح نداری، در بسیاری قوانین جرم رایانه‌ای محسوب می‌شود. برای تمرین قانونی، فقط از سیستم‌های خودت یا محیط‌های آموزشی طراحی‌شده برای این کار (TryHackMe، HackTheBox، OverTheWire — که در پروفایل مطالعاتی‌ات هم به آن‌ها اشاره کرده بودی) استفاده کن.

## ۴.۵) نکات مهم آزمون برای 212.4

- ✅ چرا `PasswordAuthentication no` + کلید عمومی مهم‌ترین تغییر امنیتی SSH است.
- ✅ ترتیب بررسی TCP Wrappers: اول `allow`، اگر نبود `deny`، اگر هیچ‌کدام نبود پیش‌فرض **اجازه** (برخلاف فایروال).
- ✅ تفاوت `MaxAuthTries` در sshd_config (محدود به یک اتصال) با `fail2ban` (پویا، بر اساس لاگ، در طول زمان و بین اتصالات مختلف).
- ✅ همیشه تنظیمات fail2ban را در `jail.local`، نه `jail.conf`.
- ✅ ملاحظات قانونی اسکن پورت.

---

# بخش پنجم — 212.5: ایمن‌سازی داده با رمزنگاری

## ۵.۱) رمزنگاری نامتقارن در برابر متقارن — تفاوتی که کل این Topic را به هم وصل می‌کند

این بهترین جای دوره برای مرور یک تفاوت مفهومی است که چندین بار در کل LPIC-2 ظاهر شده:

- **رمزنگاری متقارن** (Symmetric) — **یک** کلید مشترک، هم برای رمزنگاری هم رمزگشایی. سریع‌تر، اما مشکل: چطور دو طرف بدون این‌که کسی کلید را ببیند، آن را رد و بدل کنند؟ این دقیقاً همان چیزی است که در **LUKS** (Topic 203، رمزنگاری دیسک) دیدیم.
- **رمزنگاری نامتقارن** (Asymmetric) — یک **جفت** کلید ریاضی مرتبط: هرچه با کلید عمومی رمزنگاری شود، فقط با کلید خصوصی متناظرش قابل‌رمزگشایی است (و برعکس، برای امضای دیجیتال). کندتر از متقارن، اما مشکل رد‌وبدل کلید را حل می‌کند چون کلید عمومی را می‌توانی آزادانه با همه به‌اشتراک بگذاری. این دقیقاً همان مکانیزمی است که در **SSH keys** (212.4)، **TLS/HTTPS** (Topic 208)، **DNSSEC** (Topic 207)، و **DKIM** (Topic 211) دیدیم.

**GPG** یک ابزار عملی است که هر دو نوع را همزمان استفاده می‌کند: برای رمزنگاری فایل‌های بزرگ، معمولاً یک کلید متقارن تصادفی و یک‌بارمصرف ساخته می‌شود (سریع)، و فقط همان کلید کوچک با رمزنگاری نامتقارن (کند ولی روی داده‌ی کوچک، پس مشکلی نیست) محافظت می‌شود — این ترکیب هوشمندانه به آن **Hybrid Encryption** می‌گویند.

## ۵.۲) استفاده‌ی عملی از GPG

```bash
gpg --full-generate-key
```
این یک جفت کلید (عمومی + خصوصی) برای هویت تو می‌سازد.

```bash
gpg --list-keys                          # کلیدهای عمومی که داری (خودت + دیگرانی که import کرده‌ای)
gpg --armor --export user@example.com > public.key    # export کلید عمومی به فرمت متنی قابل‌اشتراک (armor)
gpg --import someone-elses-public.key      # import کردن کلید عمومی یک نفر دیگر تا بتوانی برایش رمزنگاری کنی
```

```bash
gpg --encrypt --recipient user@example.com file.txt
```
این با کلید **عمومی** گیرنده رمزنگاری می‌کند — فقط کسی که کلید **خصوصی** متناظر را دارد (یعنی خود گیرنده) می‌تواند رمزگشایی کند.

```bash
gpg --decrypt file.txt.gpg > file.txt
```

```bash
gpg --sign file.txt
gpg --verify file.txt.sig file.txt
```
امضای دیجیتال برعکس رمزنگاری کار می‌کند از نظر جهت کلید: با کلید **خصوصی خودت** امضا می‌کنی (فقط تو می‌توانی این امضا را بسازی)، و هرکسی با کلید **عمومی تو** می‌تواند تأیید کند که این پیام واقعاً از طرف تو آمده و دستکاری نشده — این دقیقاً همان مکانیزمی است که DKIM (Topic 211) و DNSSEC (Topic 207) برایشان استفاده می‌کنند، فقط با کاربرد و مقیاس متفاوت.

## ۵.۳) نکات مهم آزمون برای 212.5

- ✅ تفاوت بنیادین متقارن (یک کلید، سریع، مشکل رد‌وبدل کلید — مثل LUKS) و نامتقارن (جفت کلید، حل مشکل رد‌وبدل — مثل SSH/TLS/DNSSEC/DKIM).
- ✅ GPG هر دو را با هم ترکیب می‌کند (Hybrid Encryption) برای بهترین ترکیب سرعت+امنیت.
- ✅ جهت استفاده از کلید در رمزنگاری (با کلید عمومی گیرنده) در برابر امضا (با کلید خصوصی خودت) — دقیقاً برعکس هم.

---

## خلاصه‌ی جدولی نهایی کل Topic 212

| زیرمبحث | مفهوم کلیدی | جزئیات |
|---|---|---|
| 212.1 | روتر شدن | `ip_forward=1`, Masquerade (برای IP پویا) |
| 212.2 | فلسفه فایروال | Default Deny (`policy drop`) |
| 212.2 | nftables | Table→Chain(hook+priority)→Rule |
| 212.2 | ترتیب حیاتی | `ct state established,related` اول لیست |
| 212.3 | OpenVPN PKI | init-pki→build-ca→gen-req→sign-req→gen-dh |
| 212.3 | tun vs tap | لایه۳(فقط IP) vs لایه۲(کل فریم) |
| 212.4 | سخت‌سازی SSH | PasswordAuth=no + PubkeyAuth + AllowUsers |
| 212.4 | TCP Wrappers | allow اول، سپس deny، پیش‌فرض=اجازه |
| 212.4 | fail2ban | پویا، بر پایه‌ی لاگ، `jail.local` |
| 212.5 | رمزنگاری | متقارن(LUKS) vs نامتقارن(SSH/TLS/GPG) |

## مایندمپ متنی کامل

```
Topic 212: System Security
│
├── 212.1 Router Configuration
│   ├── net.ipv4.ip_forward=1
│   └── NAT/Masquerade (IP پویا) vs SNAT (IP ثابت)
│
├── 212.2 Firewall
│   ├── فلسفه: Default Deny (policy drop)
│   ├── nftables: table→chain(hook,priority)→rule
│   ├── ⚠️ established,related اول لیست (کارایی+منطق)
│   └── iptables کلاسیک: INPUT/OUTPUT/FORWARD
│
├── 212.3 VPN
│   ├── OpenVPN: PKI کامل (احراز دوطرفه)
│   │   └── init-pki→build-ca→gen-req→sign-req→gen-dh
│   ├── tun(لایه۳) vs tap(لایه۲)
│   └── IPsec: strongSwan/Libreswan (Site-to-Site)
│
├── 212.4 Network Service Security
│   ├── sshd_config: PermitRootLogin no, PasswordAuth no
│   ├── Port تغییر = ابهام، نه امنیت واقعی
│   ├── TCP Wrappers: hosts.allow→hosts.deny→پیش‌فرض اجازه
│   ├── fail2ban: پویا+لاگ+زمان (jail.local)
│   └── nmap -sV (⚠️ فقط با اجازه)
│
└── 212.5 Data Encryption
    ├── متقارن(۱کلید، سریع) = LUKS
    ├── نامتقارن(۲کلید) = SSH/TLS/DNSSEC/DKIM
    └── GPG = Hybrid (هر دو با هم)
```

---

# پایان کتاب: از LPIC-2 به Cyber Security

با تکمیل این ۱۳ Topic (200 تا 212)، پایه‌ی کامل مدیریت پیشرفته‌ی لینوکس — سرور، شبکه، ذخیره‌سازی، و امنیت — رو داری. Topic 212 دقیقاً همون دری بود که به مسیر Cyber Security باز می‌شه: nftables/iptables (دفاع شبکه‌ای)، OpenVPN/IPsec (ارتباط امن)، سخت‌سازی SSH و fail2ban (دفاع سرویس)، و GPG (رمزنگاری داده) — همه‌ی این‌ها دقیقاً همون ابزارهایی هستن که در TryHackMe، HackTheBox و OverTheWire Bandit بهشون برمی‌خوری، چه از زاویه‌ی دفاعی (Blue Team) چه تهاجمی (Red Team، برای فهم اینکه چطور دور زده می‌شن).
