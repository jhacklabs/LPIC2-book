---
title: "Topic 206 — System Maintenance (نسخه عمیق و کامل)"
exam: LPIC-2 / 201-450
weights: "206.1 (2) + 206.2 (3) + 206.3 (1)"
os_target: "Arch Linux / Omarchy"
tags: [lpic2, backup, packages, cron, deep-dive]
---

# Topic 206: System Maintenance — راهنمای کامل و عمیق

---

# بخش اول — 206.1: طراحی و اجرای backup

## ۱.۱) فلسفه‌ی backup: قانون ۳-۲-۱

قبل از ابزارها، باید یک اصل پایه در دنیای backup را بشناسی که در هر متن حرفه‌ای (و اغلب در سؤالات مفهومی LPIC) تکرار می‌شود: **قانون ۳-۲-۱**:
- **۳** نسخه از داده‌ات داشته باش (اصل + ۲ بکاپ)
- روی **۲** نوع رسانه‌ی مختلف ذخیره کن (مثلاً هارد داخلی + یک NAS، یا دیسک + Cloud)
- **۱** نسخه را خارج از محل اصلی (off-site) نگه‌دار (مثلاً یک بکاپ در یک دیتاسنتر دیگر یا در فضای ابری)

چرا این آخری مهم است؟ چون بکاپ‌هایی که فقط در همان ساختمان هستند، در برابر خطرات فیزیکی مثل آتش‌سوزی، سرقت، یا سیل بی‌فایده‌اند — اگر خود ساختمان از بین برود، هم اصل و هم بکاپ با هم از بین می‌روند.

## ۱.۲) انواع استراتژی backup

- **Full Backup** — کپی کامل همه‌چیز، هر بار. ساده‌ترین برای بازیابی (فقط یک فایل لازم است) اما کندترین و حجیم‌ترین برای اجرا.
- **Incremental Backup** — فقط چیزهایی که از **آخرین backup (از هر نوعی)** تغییر کرده‌اند. سریع‌ترین برای اجرا و کمترین فضا، اما بازیابی پیچیده‌تر است چون باید backup کامل اول + **همه‌ی** incrementalهای بعدی به‌ترتیب اعمال شوند.
- **Differential Backup** — فقط چیزهایی که از **آخرین backup کامل** (نه از آخرین differential) تغییر کرده‌اند. بین این دو حالت میانه است: بازیابی ساده‌تر از incremental (فقط full + آخرین differential لازم است)، اما هر differential به‌مرور بزرگ‌تر می‌شود چون همیشه نسبت به همان نقطه‌ی مرجع اولیه حساب می‌شود.

### تشبیه برای تفاوت Incremental و Differential

فرض کن هر یکشنبه یک عکس کامل از اتاقت می‌گیری (Full). حالا هر روز دوشنبه تا شنبه:
- با **Incremental**: هر روز فقط عکس می‌گیری از چیزهایی که **از دیروز** تغییر کرده. برای بازسازی وضعیت پنج‌شنبه، باید یکشنبه + دوشنبه + سه‌شنبه + چهارشنبه + پنج‌شنبه، همه را به‌ترتیب روی هم بگذاری.
- با **Differential**: هر روز عکس می‌گیری از چیزهایی که **از یکشنبه** (همان مرجع اصلی) تغییر کرده. برای بازسازی پنج‌شنبه، فقط یکشنبه + پنج‌شنبه لازم است — اما عکس پنج‌شنبه احتمالاً بزرگ‌تر از عکس دوشنبه است، چون شامل تجمیع همه‌ی تغییرات از یکشنبه تاست.

## ۱.۳) ابزارهای عملی

### `tar` — بایگانی کلاسیک

```bash
tar -czvf backup-$(date +%Y%m%d).tar.gz /home/user/documents
```
- `-c` — ساخت (create) یک آرشیو جدید
- `-z` — فشرده‌سازی با gzip (برای فشرده‌سازی بهتر: `-j` برای bzip2، `-J` برای xz)
- `-v` — verbose، نمایش نام هر فایل حین پردازش
- `-f` — نام فایل خروجی (این پرچم همیشه آخرین است، چون آرگومان بعدی‌اش نام فایل است)

```bash
tar -xzvf backup.tar.gz -C /restore/location/     # استخراج (extract) به یک مسیر مشخص
tar -tzvf backup.tar.gz                             # فقط نمایش لیست محتویات، بدون استخراج (تست/بررسی)
```

**backup افزایشی با tar:** پرچم `--listed-incremental` یک فایل «snapshot» می‌سازد که وضعیت فایل‌ها را در زمان backup ثبت می‌کند؛ در اجرای بعدی، `tar` این فایل را می‌خواند تا بفهمد چه چیزی تغییر کرده:
```bash
tar --create --listed-incremental=/backup/snapshot.file -f full.tar /data/     # اولین اجرا: کامل
tar --create --listed-incremental=/backup/snapshot.file -f incr1.tar /data/    # اجرای بعدی: فقط تغییرات
```

### `rsync` — ابزار همگام‌سازی هوشمند

`rsync` برای backup های تدریجی و همگام‌سازی بین سیستم‌ها بسیار محبوب‌تر از `tar` است، چون فقط **بلوک‌های تغییریافته‌ی** یک فایل را منتقل می‌کند (نه کل فایل را دوباره)، که برای فایل‌های بزرگ با تغییرات کوچک (مثل دیتابیس یا لاگ) بسیار بهینه‌تر است.

```bash
rsync -avz /home/user/documents/ user@remote:/backup/documents/
```
- `-a` — حالت archive: مخفف مجموعه‌ای از پرچم‌ها (`-rlptgoD`) که یعنی: بازگشتی (recursive)، symlink ها را حفظ کن، permission ها را حفظ کن، زمان تغییر را حفظ کن، مالکیت گروه/کاربر را حفظ کن، فایل‌های دستگاه را حفظ کن — به‌طور خلاصه: «یک کپی کاملاً هویت‌دار (identical) بساز»
- `-v` — verbose
- `-z` — فشرده‌سازی داده حین انتقال (برای اتصالات کند شبکه مفید است)

```bash
rsync -avz --delete /source/ /destination/
```
پرچم `--delete` فایل‌هایی که در مبدأ حذف شده‌اند را از مقصد هم حذف می‌کند — این یعنی مقصد **دقیقاً آینه‌ی** مبدأ می‌شود (نه فقط «هرچی جدید بود اضافه کن»). این پرچم را با احتیاط استفاده کن؛ اگر مبدأ به‌اشتباه خالی شود، مقصد هم خالی می‌شود.

> ⚠️ **نکته‌ی مهم درباره‌ی اسلش انتهایی (trailing slash) در rsync — یکی از رایج‌ترین اشتباهات:**
> - `rsync -avz /source/ /dest/` → محتویات داخل `source` مستقیماً داخل `dest` کپی می‌شود
> - `rsync -avz /source /dest/` (بدون اسلش انتهایی روی مبدأ) → خود پوشه‌ی `source` (به‌عنوان یک زیرپوشه‌ی جدید) داخل `dest` کپی می‌شود، یعنی نتیجه می‌شود `/dest/source/...`
> این تفاوت ظریف می‌تواند نتیجه‌ی backup را کاملاً متفاوت کند و سؤال کلاسیک آزمون است.

### ابزارهای سطح‌فایل‌سیستم که در تاپیک‌های قبل دیدیم

یادت باشد از Topic 203، ابزارهای اختصاصی فایل‌سیستم هم نوعی backup در سطح بلاک/متادیتا انجام می‌دهند:
```bash
sudo xfsdump -f /backup/data.dump /mnt/data       # آگاه از ساختار داخلی XFS
sudo btrfs send /mnt/@snapshot | btrfs receive /backup/    # ارسال یک snapshot کامل Btrfs (شامل تاریخچه‌ی CoW) به مقصد دیگر
```
`btrfs send/receive` بسیار قدرتمند است چون می‌تواند **فقط تفاوت بین دو snapshot** را منتقل کند (`btrfs send -p <snapshot_قدیمی> <snapshot_جدید>`) — این عملاً backup افزایشی در سطح فایل‌سیستم است، بدون نیاز به اسکن کامل فایل‌ها مثل `rsync`.

### ابزارهای legacy: `dump`/`restore`

این‌ها ابزارهای بسیار قدیمی مخصوص ext2/3/4 هستند (که به همان فیلد `dump` در fstab که در Topic 203 دیدیم اشاره دارند) — امروز عملاً هیچ‌کس از این‌ها استفاده نمی‌کند (به نفع `rsync`/`tar`/راهکارهای مدرن‌تر)، اما آزمون LPIC ممکن است اسمشان را بپرسد.

## ۱.۴) نکات مهم آزمون برای 206.1

- ✅ تفاوت Incremental (نسبت به آخرین backup از هر نوع) و Differential (همیشه نسبت به آخرین Full) را با یک مثال حساب کن.
- ✅ پرچم‌های `tar`: `c`(create)/`x`(extract)/`t`(list)/`z`(gzip)/`j`(bzip2)/`J`(xz)/`v`(verbose)/`f`(filename).
- ✅ `rsync -a` معادل چه پرچم‌هایی است (`rlptgoD`).
- ✅ تفاوت حیاتی اسلش انتهایی در rsync (`/source/` در برابر `/source`).
- ✅ `btrfs send/receive` برای backup افزایشی در سطح snapshot.

---

# بخش دوم — 206.2: مدیریت اسکریپت‌ها و بسته‌ها برای اتوماسیون سیستم

## ۲.۱) زمان‌بندی کارها: `cron` — عمیق

### ساختار خط crontab

```
* * * * * command
│ │ │ │ │
│ │ │ │ └── روز هفته (0-7، هم 0 هم 7 یعنی یکشنبه)
│ │ │ └──── ماه (1-12)
│ │ └────── روز ماه (1-31)
│ └──────── ساعت (0-23)
└────────── دقیقه (0-59)
```

```bash
crontab -e          # ویرایش crontab کاربر فعلی (باز شدن در ویرایشگر پیش‌فرض)
crontab -l           # نمایش crontab فعلی
crontab -r            # حذف کامل crontab (بدون تأیید! با احتیاط)
sudo crontab -u username -e     # ویرایش crontab یک کاربر دیگر (نیاز به root)
```

مثال‌های عملی با توضیح دقیق سینتکس:
```
0 2 * * *        /scripts/backup.sh          # هر روز ساعت ۲ بامداد
*/15 * * * *      /scripts/check-health.sh    # هر ۱۵ دقیقه یک‌بار (پرچم / برای گام‌ها)
0 9 * * 1-5        /scripts/workday-report.sh  # هر روز کاری (دوشنبه تا جمعه) ساعت ۹ صبح
0 0 1 * *          /scripts/monthly-cleanup.sh # اولین روز هر ماه، نیمه‌شب
```

### سه محدوده‌ی مختلف Cron

- **System-wide**: `/etc/crontab` و `/etc/cron.d/*` — این‌ها یک ستون اضافه دارند: **نام کاربر** که کار باید با آن اجرا شود (چون فایل‌های سیستمی می‌توانند برای هر کاربری کار زمان‌بندی کنند، نه فقط صاحب فایل).
- **پوشه‌های میان‌بر ساده**: `/etc/cron.hourly/`, `/etc/cron.daily/`, `/etc/cron.weekly/`, `/etc/cron.monthly/` — هر اسکریپتی که در این پوشه‌ها بگذاری، خودکار در بازه‌ی زمانی مربوطه اجرا می‌شود، بدون نیاز به نوشتن سینتکس crontab.
- **User crontab**: هر کاربر crontab اختصاصی خودش را دارد (`crontab -e`)، ذخیره‌شده در `/var/spool/cron/crontabs/<username>` (مسیر دقیق بسته به توزیع فرق دارد).

### کنترل دسترسی به cron

```
/etc/cron.allow    # اگر این فایل وجود داشته باشد، فقط کاربران لیست‌شده در آن مجازند از cron استفاده کنند
/etc/cron.deny      # اگر cron.allow نباشد، همه به‌جز کاربران این لیست مجازند
```
این دقیقاً همان الگوی امنیتی allow/deny است که در Topic 212 (با `hosts.allow`/`hosts.deny`) دوباره می‌بینیم — یک الگوی رایج در کل دنیای لینوکس.

## ۲.۲) `at` و `anacron` — تفاوت با cron

`cron` عالی است برای کارهای **تکرارشونده** با زمان‌بندی دقیق، اما دو محدودیت دارد که ابزارهای دیگر حل می‌کنند:

```bash
echo "/scripts/one-time-task.sh" | at 14:30
at now + 1 hour
atq                # لیست کارهای زمان‌بندی‌شده‌ی معلق
atrm 3              # حذف یک کار زمان‌بندی‌شده با شماره‌ی مشخص
```
**`at`** برای اجرای یک کار **فقط یک‌بار** در یک زمان مشخص در آینده است — بر خلاف cron که همیشه تکرار می‌شود.

**`anacron`** مشکل دیگری را حل می‌کند: اگر یک سیستم (مثل یک لپ‌تاپ) در زمانی که یک کار cron باید اجرا می‌شد خاموش بوده باشد (مثلاً کار روزانه‌ی ساعت ۲ بامداد، ولی لپ‌تاپ آن موقع خاموش بوده)، `cron` معمولی آن کار را **برای همیشه از دست می‌دهد**. `anacron` این را حل می‌کند: به‌جای زمان دقیق، بر اساس «چند روز از آخرین اجرا گذشته» کار می‌کند، و اگر سیستم موقع زمان‌بندی خاموش بوده، به‌محض روشن شدن، کار را (با کمی تأخیر) اجرا می‌کند. فایل تنظیمات آن `/etc/anacrontab` است، با سینتکس متفاوت از crontab معمولی:
```
# period(days)  delay(minutes)  job-identifier  command
7                25               weekly-backup   /scripts/weekly.sh
```

### systemd timers — جانشین مدرن

```bash
systemctl list-timers
```
همان‌طور که در Topic 202 دیدیم، systemd معادل مدرن خودش برای زمان‌بندی دارد: یک فایل `.timer` که به یک فایل `.service` هم‌نام وصل می‌شود. مزیت نسبت به cron سنتی: لاگ کامل هر اجرا از طریق `journalctl` قابل‌بررسی است، و منطق شبیه `anacron` (اجرای کارهای ازدست‌رفته بعد از روشن شدن سیستم) با گزینه‌ی `Persistent=true` به‌سادگی فعال می‌شود.

## ۲.۳) به‌روزرسانی و مدیریت پکیج در مقیاس

هرچند مدیریت پکیج جزء اصلی LPIC-1 بود، در LPIC-2 این مبحث از منظر **اتوماسیون و مقیاس سیستم‌های چندگانه** دوباره مطرح می‌شود:

```bash
sudo pacman -Syu           # روی Arch/Omarchy: sync + به‌روزرسانی کامل
sudo pacman -Qtd            # پیدا کردن پکیج‌های "orphan" (نصب‌شده به‌عنوان وابستگی ولی دیگر هیچ‌چیزی به آن‌ها وابسته نیست)
sudo pacman -Rns $(pacman -Qtdq)    # حذف تمیز تمام orphan ها
```
برای مدیریت مقیاس‌پذیر (مثلاً ده‌ها سرور)، ابزارهایی مثل Ansible یا اسکریپت‌های ساده‌ی cron که به‌صورت خودکار بررسی/گزارش آپدیت‌های امنیتی معلق می‌دهند، رایج هستند — این‌ها فراتر از سرفصل مستقیم LPIC-2 هستند، اما مفهوم پشت آن‌ها (خودکارسازی نگهداری) دقیقاً هدف این زیرمبحث است.

## ۲.۴) نکات مهم آزمون برای 206.2

- ✅ ترتیب دقیق فیلدهای crontab: دقیقه، ساعت، روز ماه، ماه، روز هفته.
- ✅ فرق `cron` (تکرارشونده، دقیق) با `at` (یک‌بار) با `anacron` (تحمل خاموشی سیستم، بر اساس روز نه ساعت دقیق).
- ✅ محل‌های مختلف: `/etc/crontab` (+نام کاربر)، `/etc/cron.d/`، `/etc/cron.{hourly,daily,weekly,monthly}/`، `crontab -e` شخصی.
- ✅ الگوی `cron.allow`/`cron.deny` (اگر allow باشد، فقط اجازه؛ وگرنه deny مرجع است).
- ✅ systemd timers با `Persistent=true` معادل مدرن anacron است.

---

# بخش سوم — 206.3: مانیتورینگ روزانه‌ی سیستم برای مشکلات

## ۳.۱) لاگ‌های سیستمی — کجا باید دنبال بگردی

```bash
journalctl                        # کل لاگ systemd journal
journalctl -u sshd.service          # فقط لاگ یک سرویس خاص
journalctl -b                       # فقط از آخرین بوت
journalctl -b -1                     # از بوت قبلی (یک بوت عقب‌تر)
journalctl -k                        # فقط پیام‌های کرنل (معادل dmesg اما دائمی‌تر و قابل جست‌وجوتر)
journalctl -f                        # زنده، مثل tail -f
journalctl --since "2026-09-01" --until "2026-09-05"    # بازه‌ی زمانی مشخص
journalctl -p err                     # فقط پیام‌های با سطح خطا (error) یا بالاتر
```
لاگ‌های سنتی متنی هم هنوز در بسیاری سیستم‌ها وجود دارند (مخصوصاً اگر `rsyslog`/`syslog-ng` هم فعال باشد در کنار journald):
```bash
tail -f /var/log/syslog        # (Debian-family)
tail -f /var/log/messages       # (Red Hat-family)
```

## ۳.۲) بررسی خودکار سلامت با اسکریپت‌های cron

یک الگوی رایج در دنیای واقعی: نوشتن یک اسکریپت ساده که هر شب بررسی می‌کند آیا فضای دیسک، بار CPU، یا وضعیت سرویس‌های حیاتی مشکلی دارند یا نه، و در صورت مشکل ایمیل/پیام هشدار می‌فرستد. این دقیقاً پلی است بین این Topic و Topic 200 (Capacity Planning) که در ابتدای دوره دیدیم — ابزارهایی مثل `df`, `free`, `uptime` که آنجا یاد گرفتی، اینجا در قالب یک اسکریپت cron خودکار به کار می‌آیند:

```bash
#!/bin/bash
THRESHOLD=90
USAGE=$(df / | tail -1 | awk '{print $5}' | sed 's/%//')
if [ "$USAGE" -gt "$THRESHOLD" ]; then
    echo "هشدار: فضای دیسک ریشه ${USAGE}% پر است" | mail -s "هشدار فضای دیسک" admin@example.com
fi
```

## ۳.۳) نکات مهم آزمون برای 206.3

- ✅ `journalctl -b` برای بوت فعلی، `-b -1` برای یک بوت قبل.
- ✅ `journalctl -k` معادل دائمی و قابل‌جست‌وجوتر `dmesg`.
- ✅ ترکیب ابزارهای Topic 200 (`df`, `uptime`, `free`) با cron برای هشدار خودکار، یک الگوی رایج نگهداری روزمره است.

---

## خلاصه‌ی جدولی نهایی کل Topic 206

| زیرمبحث | مفهوم کلیدی | ابزار |
|---|---|---|
| 206.1 | قانون ۳-۲-۱ backup | ۳ نسخه، ۲ رسانه، ۱ off-site |
| 206.1 | Full/Incremental/Differential | نسبت به چه مرجعی حساب می‌شود |
| 206.1 | ابزارها | `tar`, `rsync -a`, `btrfs send/receive` |
| 206.2 | زمان‌بندی | `crontab -e`, فیلدهای ۵گانه |
| 206.2 | تفاوت ابزارها | `cron`(تکرار) vs `at`(یک‌بار) vs `anacron`(تحمل خاموشی) |
| 206.2 | کنترل دسترسی | `cron.allow`/`cron.deny` |
| 206.3 | لاگ زنده | `journalctl -b/-k/-f/-p` |

## مایندمپ متنی کامل

```
Topic 206: System Maintenance
│
├── 206.1 Backup Operations
│   ├── قانون ۳-۲-۱
│   ├── Full / Incremental(نسبت به آخرین) / Differential(نسبت به Full)
│   ├── tar: c/x/t/z/j/J/v/f
│   ├── rsync -avz --delete (⚠️ trailing slash!)
│   └── btrfs send/receive (افزایشی در سطح snapshot)
│
├── 206.2 Automating Maintenance
│   ├── crontab: دقیقه-ساعت-روزماه-ماه-روزهفته
│   ├── /etc/crontab + /etc/cron.d (+نام کاربر)
│   ├── /etc/cron.{hourly,daily,weekly,monthly}
│   ├── cron.allow/cron.deny
│   ├── at (یک‌بار) vs anacron (تحمل خاموشی، روزمحور)
│   └── systemd timers (Persistent=true ≈ anacron)
│
└── 206.3 Monitoring for Problems
    ├── journalctl: -b/-b -1/-k/-f/-p err
    └── ترکیب df/uptime/free (از Topic 200) با cron برای هشدار خودکار
```
